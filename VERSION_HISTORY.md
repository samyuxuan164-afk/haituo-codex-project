# Version History

| Version | Component | Date | Notes |
|---|---|---|---|
| 2.1.41 | DXM Automation V1 - NEW | 2026-07-04 | Narrow ASIN evidence-backed category-modal safety fix. `asin-aliexpress-evidence-store` plans now use strict DXM leaf matching for search-result scoring, category tree scoring, and selected-category readback. Expected leaves such as `收纳盒和收纳箱(Storage Boxes & Bins)` accept the full target path / Chinese leaf / English leaf, while wrong or broad text such as `Anti-Mosquito Incense`, `Home & Garden`, and `Storage Boxes` is rejected. Syntax, whitespace, and targeted assertions passed. No browser action, Dianxiaomi field edit/save, move-to-wait-publish, final publish, one-click publish, collection, reclaim, Product Development draft handling, or rule-file change executed. |
| 2.1.39 | DXM Automation V1 - NEW | 2026-07-04 | Narrow required-attribute preflight/readback correction. Known dynamic fields such as Use, Function, Feature, Material, and Frame Material now require a current-page visible red required marker before they can be filled as required attributes or reported as blockers. Hidden, absent, stale, or non-red-star fields are skipped, while native Dianxiaomi product-attribute validation remains a save blocker. Syntax check and targeted source assertions passed. No browser action, Dianxiaomi save, move-to-wait-publish, final publish, one-click publish, Product Development draft handling, recollection, or reclaim executed during this source update. |
| 2.1.16 | DXM Automation V1 - NEW | 2026-07-03 | Project-level required-attribute hardening for Dianxiaomi edit pages. Function / Use / Feature dynamic dropdowns now click real visible options without Enter/Tab second confirmation, final preflight scans all visible red-star product attributes instead of only fixed known fields, Function is included in required readback, and native `请选择产品属性` / visible form validation after `保存并移入待发布` is treated as a failed save rather than success. Syntax checks passed. No browser action, Dianxiaomi edit/save, move-to-wait-publish, final publish, one-click publish, Product Development draft handling, recollection, or reclaim executed during this source update. |
| aliexpress-evidence-threshold-50-v1 | Tools / Config / Rules | 2026-07-02 | Moved AliExpress category evidence thresholds into `config/aliexpress-evidence-thresholds.json` and changed the direct conditional pass line from hardcoded 60% to 50%. Added `tools/aliexpress-evidence-policy.js` and test coverage. New `dxm_category_validation_required` status routes semantic-consistent low-share or inconclusive detail evidence to Dianxiaomi readonly category validation instead of final product failure, while still blocking edit/save readiness. Wired the status through evidence schema, batch status, candidate manifest, execution gate, and exception queue. Verified syntax, JSON parse, policy test, synthetic classification, and temporary gate plan. No browser action, Dianxiaomi edit/save, move-to-wait-publish, publish, one-click publish, Product Development draft handling, recollection, or reclaim executed. |
| dxm-edit-preflight-gate-safety-v1 | Tools | 2026-07-02 | Fixed a batch-gate safety issue exposed by B0CZ7F61XN: when `check --edit-preflight` cannot read a real edit-page preflight because no edit tab is open/located, final gate now blocks with `edit_preflight_unavailable` instead of carrying local-ready status forward to a save suggestion. Missing current ASIN and current-ASIN mismatch also block explicitly. Verified syntax and reran readonly B0CZ check: final gate is blocked with next action `open_correct_edit_page_and_rerun_readonly_preflight`. No Dianxiaomi edit/save, move-to-wait-publish, publish, one-click publish, Product Development draft handling, recollection, or reclaim executed. |
| aliexpress-detail-evidence-low-confidence-v1 | Tools / Rules | 2026-07-02 | Added and live-validated low-confidence AliExpress detail-page evidence handling. `<60%` search-result consensus no longer has to stop at semantic mapping missing when titles are consistent: with explicit `--detail-pages`, `tools/aliexpress-evidence-capture.js` opens representative AliExpress detail pages, reads specification type/category fields, filters navigation noise such as `浏览/Browse`, retries additional candidates when one detail page lacks a usable type, and records `detail_verified/detail_low_confidence/detail_type_consensus` when 2 pages agree or are semantically equivalent. Added store/schema/browser-cache/batch/gate/userscript support for `detail_verified`, plus retryable `aliexpress_verification_required` routing for AliExpress verification pages. Live readonly B0CZ7F61XN capture checked 3 detail pages and matched 2 usable detail records (`专用工具类型=飞溅屏幕`, semantic `类型=专用工具` normalized to `飞溅屏幕`); formal evidence-store write/readback now marks B0CZ7F61XN `detail_verified`, and formal gate dry-run shows `readyForEditPreflight=true` / blockers 0. No Dianxiaomi edit/save, publish, one-click publish, Product Development draft handling, recollection, or reclaim executed. |
| aliexpress-evidence-confidence-gate-v1 | Tools / Rules / Runs | 2026-07-02 | Implemented the AliExpress category evidence confidence gate: `>=80%` + DXM candidate category becomes `aliexpress_verified/high_confidence`; `60%-79%` can pass as `conditional_verified/low_confidence`; `<60%` now enters semantic second-pass review instead of direct split. Semantically consistent results without DXM mapping become `semantic_consensus_needs_dxm_mapping`; semantically consistent results with a safe DXM category can become `conditional_verified/semantic_low_confidence`; true conflict remains split/review. Updated evidence capture, evidence store, batch status, schema, exception queue classification, and project rule docs. B0CZ7F61XN was reclassified to `semantic_consensus_needs_dxm_mapping`; remaining first-batch blockers are `aliexpress_dxm_category_map_missing=1` and `aliexpress_category_evidence_split=6`. Final `dxm-batch-pipeline plan` dry-run showed no new ready ASINs after excluding the three already in wait-to-publish. No Dianxiaomi edit/save, final publish, one-click publish, Product Development draft handling, recollection, or reclaim executed. |
| 2.1.14 | DXM Automation V1 - NEW | 2026-07-02 | B0FH7774VK live closed-loop pass and PC-description pipeline repair. The main sequential edit-page pipeline now calls the existing PC-description filler after title normalization and includes description status in early-stop/final reports. Syntax check passed. Live runtime injection verified current ASIN/price/evidence gates, final preflight passed with 0 blockers, native save-to-wait-publish succeeded, and `/web/smtlocalProduct/offline` readback passed for SKU `B0FH7774VK`, price `169.04`, stock `15`, category `收纳架(Racks & Holders)`. Ten-item pressure-test preparation found no remaining edit-ready ASINs in the original first batch after excluding the three completed closed loops. No final publish, one-click publish, Product Development draft handling, recollection, or reclaim executed. |
| 2.1.13 | DXM Automation V1 - NEW | 2026-07-02 | B0DPSJP47V live field-control repair and closed-loop pass. Added committed-category modal finalization, exact freight-template form-item selection/readback, standalone batch validation entries for category+price / shipping+postage / required attributes / variation+final preflight, and fixed variation Ships From status when global United States is already selected. Syntax check passed. Live runtime injection verified B0DPS in batches, final preflight passed with 0 blockers, native save-to-wait-publish succeeded, and `/web/smtlocalProduct/offline` readback passed for SKU `B0DPSJP47V`, price `108.39`, stock `15`, category `收纳架(Racks & Holders)`. No final publish, one-click publish, Product Development draft handling, recollection, or reclaim executed. |
| 2.1.6 | DXM Automation V1 - NEW | 2026-07-02 | Narrow recursion fix discovered during B0DPSJP47V live preflight. `v2.1.5` fixed stale ASIN priority, but DOM fallback SKU snapshot called full strict price preflight, which called product DOM fallback again and triggered `Maximum call stack size exceeded`. DOM SKU snapshot now computes price directly from the Amazon price store/default source price without calling full preflight, and current edit price ASIN lookup no longer calls DOM fallback product construction. Syntax check passed. Runtime-injected v2.1.6 correctly read B0DPS ASIN, source URL, AliExpress evidence, and Amazon price store. B0DPS was not saved because downstream field filling still failed freight 111, required attributes, Ships From, price, and variation-parameter preflight. |
| 2.1.5 | DXM Automation V1 - NEW | 2026-07-02 | Narrow stability fix from the first real small closed-loop validation. Current edit ASIN now prioritizes the active edit page/source URL over stale panel/cache state, strict price state can read the browser `dxm_amazon_price_store_v1` formal price cache by ASIN, and invisible locked required-attribute containers are not treated as blockers unless the native page save reports an error. Verified with syntax check. Live small-loop target `B0DXFB86J7` was saved to wait-to-publish and read back with SKU, price, stock, and category matching; browser may still need Tampermonkey overwrite if it shows v2.1.4. |
| task-screenshot-cleanup-v1 | Tools / Rules | 2026-07-02 | Added `tools/cleanup-task-screenshots.js` and project rules for end-of-task screenshot cleanup. Screenshots are now treated as temporary evidence, not long-term project state. The tool scans root-level screenshots and `runs/**/*.png|jpg|jpeg|webp`, keeps referenced screenshots, and marks unreferenced screenshots as cleanup candidates. Default mode is dry-run `plan`; deletion requires explicit `cleanup --write`. Updated `AGENT.md`, `AGENTS.md`, `docs/project-execution-rules.md`, and `docs/current-status.md`. Verified syntax, help output, and dry-run plan; no deletion executed. |
| dxm-batch-pipeline-v1 | Tools | 2026-07-02 | Added `tools/dxm-batch-pipeline.js` as the project-level batch pipeline entry. It combines candidate manifest, local execution gate, optional readonly browser checks, exception batch report, next-action groups, and recommended execution order into `dxm-batch-pipeline-report-v1`. Default `plan` is local dry-run; `check` uses browser/WebBridge only with explicit readonly flags. Report writes require `--write --out`; exception and price-store writes remain separate explicit flags. Verified syntax, help, fixture plan/check, Markdown output, temporary report write/readback, risk-first ordering, no task/store constants, and formal stores remained empty. No browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or formal store write executed. |
| candidate-manifest-v1 | Tools | 2026-07-02 | Added `tools/candidate-manifest.js` as the unified ASIN candidate manifest builder. It normalizes CSV/JSON candidate rows, computes expected CNY when formula inputs are available, merges risk, price, AliExpress evidence, and exception-queue status, and outputs per-ASIN `precheckRoutingStatus`, blockers, and next action. Default dry-run; writes require `--write --out`. Verified syntax, help output, temporary store fixtures, JSON/Markdown output, temporary write/readback, and formal stores remained empty. No browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or formal manifest write executed. |
| exception-queue-batch-report-v1 | Tools | 2026-07-02 | Added `tools/exception-queue.js report` for batch exception summaries. Reports include ASIN status, primary reason, category, severity, retryability, next action, grouped next-action ASIN lists, and JSON/Markdown output. Exception queue writes now use a lightweight lock to avoid concurrent upsert overwrite during parallel execution. Verified syntax, temporary queue fixtures, JSON/Markdown report output, concurrent upsert lock retention, and formal stores remained empty. No browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or formal store write executed. |
| dxm-wait-publish-readback-fieldcheck-v1 | Tools | 2026-07-02 | Strengthened wait-to-publish batch readback. `tools/dxm-wait-publish-readback.js` now supports local `analyze` from saved readback JSON, expected row overrides, field-level row/SKU/price/stock/category checks, numeric price comparison with tolerance, and non-cascading missing-row reports. `tools/dxm-batch-execution-gate.js` uses the strengthened analyzer for wait-readback, and `tools/exception-queue.js` classifies wait-publish page-state failures as retryable control issues. Verified syntax, pass/mismatch/missing-row fixtures, temporary exception queue write/readback, and formal stores remained empty. No browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or formal store write executed. |
| product-risk-filter-v1 | Tools / Config | 2026-07-02 | Added project-level machine risk filtering with `config/product-risk-rules.json` and `tools/product-risk-filter.js`. Candidate records can now be screened as `allow`, `needs_review`, or `blocked`; risk blockers are normalized through `tools/exception-queue.js`; `tools/dxm-batch-execution-gate.js` can include risk status via `--risk-file` / `--risk-json` and prioritizes risk review before category/price recovery. Verified syntax, JSON parse, safe/blocked samples, temporary exception-queue write/readback, no task/store constants, and formal stores remained empty. No browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or formal store write executed. |
| aliexpress-dxm-category-map-coverage-v1 | Tools / Config | 2026-07-02 | Enhanced `tools/aliexpress-dxm-category-map.js` with resolver-directory review, dry-run import, confirmation-token protected formal imports, and mapping coverage reports. New commands: `review-resolvers`, `import-resolvers`, and `coverage`. Resolver imports now accept both `recommended.dxmVisibleCategoryPath` and `recommended.dxmVisibleCategory`. Verified syntax, resolver review for 3 sample reports, temporary map import/readback where `211114 -> 笔筒(Pen Holders)` became active, coverage readback with 2 usable / 1 blocked resolver mappings, formal-map missing-token rejection, and formal price/evidence/exception stores remained empty. No browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or formal map write executed. |
| aliexpress-evidence-closure-v1 | Tools | 2026-07-02 | Added ASIN-level AliExpress evidence import/readback closure to `tools/aliexpress-evidence-batch.js`. New `closure` command combines resolver import preview/write, target-ASIN evidence store readback, effective evidence readiness, blockers, and next actions in one report. `tools/aliexpress-evidence-capture.js` now accepts resolver `recommended.dxmVisibleCategory` as a DXM candidate category, not only `dxmVisibleCategoryPath`, so older resolver reports are not misclassified as unmapped. Verified syntax, dry-run closure for resolver directory, temporary-store write/readback closure, formal-store missing-token rejection, and formal stores remained empty. No browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or formal evidence-store write executed. |
| dxm-batch-gate-price-capture-v1 | Tools | 2026-07-01 | Integrated Amazon displayed-price batch capture with `tools/dxm-batch-execution-gate.js`. Gate `plan` now includes `amazonPriceCapturePlan` with missing-price ASINs plus dry-run/write command suggestions. Gate `check` can explicitly run readonly Amazon batch capture with `--capture-missing-prices`; price writes require the additional `--write-price-captures` flag, and capture exception writes require `--write-price-capture-exceptions`. Verified syntax, local missing-price planning, temporary partial-price-store planning, live readonly gate-triggered capture dry-run, temporary price-store write/readback through gate, no task/store constants, and formal stores remained empty. No Dianxiaomi page action, save, publish, one-click publish, Product Development draft handling, Amazon cart/order/chat, or formal price/exception-store write executed. |
| amazon-displayed-price-batch-v1 | Tools | 2026-07-01 | Added batch Amazon displayed-price capture entry: `tools/amazon-displayed-price-batch.js`. It runs ASINs sequentially through the readonly single-capture tool, reuses one browser tab by default, supports `--asins`, `--asin-file`, `--limit`, `--start-index`, `--delay-ms`, `--write-prices`, and `--write-exceptions`, and keeps price-store / exception-queue writes explicit and separate. `tools/amazon-displayed-price-capture.js` now supports same-tab navigation for batch execution and handles localized Amazon USD text such as `17.99美元` / `17美元 . 99`, while filtering non-price contexts such as sales counts, free-shipping thresholds, and promo credits before applying the current highest displayed-price rule. Verified syntax, local single/range/localized parsing, simulated WebBridge failure to a temporary exception queue, live readonly batch capture for one and two ASINs, temporary price-store write, no task/store constants, and formal stores remained empty. No Dianxiaomi page action, save, publish, one-click publish, Product Development draft handling, Amazon cart/order/chat, or formal price/exception-store write executed. |
| amazon-price-capture-exceptions-v1 | Tools / Runs | 2026-07-01 | Added Amazon displayed-price capture failure classification. `tools/exception-queue.js` now classifies `amazon_page_captcha_or_robot_check`, `amazon_product_unavailable_no_displayed_price`, `amazon_price_selector_missing`, and generic Amazon price-capture failures. `tools/amazon-displayed-price-capture.js` now emits exception previews on failed captures and writes them only with `--write-exceptions`; price-store writes still require separate `--write`. Verified syntax, dry-run classification, simulated WebBridge failure dry-run, temporary exception-queue write, successful live Amazon capture unaffected, and formal stores remained empty. No Dianxiaomi page action, save, publish, one-click publish, Product Development draft handling, Amazon cart/order/chat, or formal price/exception-store write executed. |
| amazon-displayed-price-capture-v1 | Tools | 2026-07-01 | Added readonly Amazon displayed-price capture tool: `tools/amazon-displayed-price-capture.js`. Commands: `parse-text` parses local price text and `capture` opens an Amazon product page through WebBridge, reads the displayed USD price area, uses the highest value when a range or multiple displayed prices are present, and writes to the price store only with `--write`. Verified syntax, local single/range/from-price parsing, live readonly Amazon capture for `B0D65JFRX4`, temporary-store write/readback, price-store formula integration, gate integration, no task/store constants, and formal stores remained empty. The live page showed both `$9.99` and list price `$11.99`; per current displayed-price rule the captured value was `11.99`. No Dianxiaomi page action, save, publish, one-click publish, Product Development draft handling, Amazon cart/order/chat, or formal price-store write executed. |
| amazon-displayed-price-field-compat-v1 | Tools | 2026-07-01 | Added price-store field compatibility for Amazon displayed price. `tools/amazon-price-store.js` now prefers `amazonDisplayedPriceUsd` and falls back to legacy `amazonOriginalPriceUsd`; summaries and calculations output both fields for compatibility. CSV import accepts `amazonDisplayedPriceUsd`, `amazon_displayed_price_usd`, and current Chinese displayed-price headers while still accepting legacy original-price headers. `tools/exception-queue.js`, `tools/dxm-batch-execution-gate.js`, and `tools/dxm-wait-publish-readback.js` now understand `amazon_displayed_price_missing`. Verified syntax, new-only / old-only / both-fields priority behavior, upsert, CSV import/write to temporary store, gate integration, exception classification, and formal stores remained empty. No browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, or formal price-store write executed. |
| amazon-displayed-price-terminology-v1 | Rules / Tools | 2026-07-01 | Updated current price-source terminology from Amazon original price to Amazon displayed price. Business rule is now: open the Amazon product page and use the price displayed at that time; if the page displays a price range, use the highest value. Existing compatible storage field `amazonOriginalPriceUsd` is temporarily retained until the field migration step. Updated current task/rule docs and tool-facing descriptions only; no browser page action, Dianxiaomi save, publish, one-click publish, Product Development draft handling, or formal price-store write executed. |
| dxm-batch-execution-gate-v1 | Tools | 2026-07-01 | Added generic current-batch execution gate: `tools/dxm-batch-execution-gate.js`. The tool combines ASIN-level AliExpress evidence status, trusted Amazon displayed-price/formula status, optional readonly browser evidence sync, optional readonly edit-page preflight, optional readonly wait-to-publish readback, and optional exception-queue writes into one batch report. Default mode is dry-run; exception writes require `--write-exceptions`. Verified syntax, help output, local dry-run with empty formal stores, temporary pass/block fixture, temporary exception-queue write/readback, formal queue unchanged, and no hardcoded task/store/ASIN constants. No Dianxiaomi collection, claim, edit save, move-to-wait-publish, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or form submission executed. |
| dxm-wait-publish-readback-v1 | Tools / Runs | 2026-07-01 | Added readonly Dianxiaomi wait-to-publish readback checker: `tools/dxm-wait-publish-readback.js`. The tool opens/verifies `/web/smtlocalProduct/offline`, reads ASIN rows, compares SKU, expected price from `runs/amazon-price-store.json`, expected stock, and optional category evidence from `runs/aliexpress-evidence-store.json`, and can route readback blockers into the exception queue only with `--write-exceptions`. `tools/exception-queue.js` now classifies wait-publish row/SKU/price/stock/category readback blockers. Verified syntax, live readonly wait-to-publish page readback, temporary price-store expected-price check, temporary exception-queue write/readback, and formal stores remained empty. No Dianxiaomi collection, claim, edit save, move-to-wait-publish, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or form submission executed. |
| amazon-price-store-v1 | Tools / Runs | 2026-07-01 | Added trusted Amazon displayed-price store: `runs/amazon-price-store.json` and `tools/amazon-price-store.js`. The tool supports init/get/list/status/upsert/import-csv/compute, stores ASIN-level compatible field `amazonOriginalPriceUsd` as the current displayed-price value, source, evidence URL, status, and price-range validation, and computes expected CNY only from explicit runtime `--exchange-rate` and `--multiplier`. `tools/exception-queue.js` now supports `from-price-status` to route missing/out-of-range/formula blockers into the machine exception queue. Verified syntax, empty formal store status, temporary trusted-price upsert, expected CNY calculation, out-of-range classification, CSV dry-run, temporary CSV write/readback, price exception dry-run/writeback to temporary queue, and formal price store / formal exception queue remained empty. No browser page action, Dianxiaomi save, wait-to-publish move, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or form submission executed. |
| exception-queue-v1 | Tools / Runs | 2026-07-01 | Added machine-readable exception queue automation: `runs/exception-queue.json` and `tools/exception-queue.js`. The tool supports `classify`, `from-evidence-status`, `from-preflight`, `upsert`, `list`, `resolve`, and `ignore`. It normalizes common blockers such as `category_evidence_missing`, `aliexpress_category_evidence_split`, `aliexpress_dxm_category_map_missing`, `amazon_original_price_missing`, `price_mismatch`, `required_attribute_incomplete`, `collection_missing_current_unclaimed_row`, `not_edit_page`, and `environment_control_exception`. Verified syntax, empty formal queue listing, dry-run classification, dry-run formal evidence-status exceptions, temporary queue write/readback from temporary evidence store, readonly preflight JSON classification, resolve flow, and formal exception queue remained empty. No browser page action, Dianxiaomi save, wait-to-publish move, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or form submission executed. |
| aliexpress-evidence-confirmed-import-v1 | Tools | 2026-07-01 | Hardened `tools/aliexpress-evidence-batch.js` formal-store imports with a dry-run confirmation token. `import-resolvers` now returns a stable `confirmation.token`; writing the formal `runs/aliexpress-evidence-store.json` requires `--write --confirm-token <token>`. Missing or wrong confirmation token is rejected before any formal write. Temporary/custom stores can still be written without confirmation for test verification. Verified syntax, formal dry-run token generation, missing-token rejection, wrong-token rejection, temporary-store write/readback, stable confirmation token despite timestamp changes, and formal evidence store remained empty. No browser page action, Dianxiaomi save, wait-to-publish move, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or form submission executed. |
| aliexpress-evidence-preflight-check-v1 | Tools | 2026-07-01 | Added `tools/aliexpress-evidence-preflight-check.js` as the evidence sync/readback closure tool. Commands: `local` checks project evidence store and optional ASIN batch status, `sync-read` writes the evidence cache to a Dianxiaomi browser page and reads it back, `preflight` reads `DXM Automation V1` readonly edit-page preflight, and `check` combines local status, optional browser sync, and optional preflight readback. Also exported WebBridge sync/read helpers from `tools/aliexpress-evidence-browser-cache.js`. Verified syntax, local empty-store report, temporary-store report with 1 verified / 2 blockers, WebBridge sync/read of the formal 0-record store, readonly preflight readback on the current Dianxiaomi page, and full `check --sync --preflight` closure. No edit-rule click, save, wait-to-publish move, publish, one-click publish, claim, collect, Product Development draft handling, AliExpress cart/order/chat, or form submission executed. |
| aliexpress-evidence-batch-v1 | Tools | 2026-07-01 | Added `tools/aliexpress-evidence-batch.js` as a generic batch evidence ingestion/readback tool. Commands: `status` checks ASIN-level evidence state, `import-resolvers` converts resolver JSON files into evidence records and writes only with explicit `--write`, and `summary` reports store-level verified/blocker counts. Resolver import now benefits from the AliExpress-to-DXM category map in `tools/aliexpress-evidence-capture.js`. Verified syntax, dry-run resolver directory import, temporary-store write/readback, formal store unchanged at 0 records, and no task/store constants in the new batch code. No browser page action, Dianxiaomi save, wait-to-publish move, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or form submission executed. |
| aliexpress-dxm-category-map-v1 | Config / Tools | 2026-07-01 | Added generic AliExpress `postCategoryId` to Dianxiaomi category mapping layer: `config/aliexpress-dxm-category-map.json` and `tools/aliexpress-dxm-category-map.js`. The tool can list/get/resolve active mappings, upsert mappings, and import validated readonly resolver output. `tools/aliexpress-evidence-capture.js` now auto-fills `dxmCandidateCategory` from the active mapping when AliExpress evidence has a mapped top category, while still keeping split/low-confidence evidence as `evidence_split` instead of verified. Verified syntax, active mapping resolve for `200231151`, missing mapping behavior for `211114`, strong/split evidence scoring, temporary resolver import, and formal evidence store remained empty. No Dianxiaomi save, publish, one-click publish, Product Development draft handling, AliExpress cart/order/chat, or form submission executed. |
| aliexpress-evidence-capture-v1 | Tools | 2026-07-01 | Added `tools/aliexpress-evidence-capture.js` for readonly AliExpress category evidence capture and evidence-store writes. Commands: `open` opens a readonly AliExpress search page; `capture` extracts result titles and `postCategoryId` signals from the current AliExpress search page and writes only with `--write`; `from-resolver` imports existing resolver JSON reports; `manual` writes manually verified evidence. The tool only marks `aliexpress_verified` when a DXM candidate category is supplied and evidence is strong enough; split/low-confidence signals are stored as `evidence_split` or `needs_manual_review`. Verified syntax, help output, temporary resolver import, temporary manual write, live readonly AliExpress search/capture, hardcoded-task scan, formal store remains empty, and network safety (`save.json/publish/online/offline/cart/order` requests remained 0). |
| aliexpress-evidence-browser-cache-sync-v1 | Tools | 2026-07-01 | Extended `tools/aliexpress-evidence-browser-cache.js` with WebBridge-backed `sync` and `read` commands. The tool now reads `runs/aliexpress-evidence-store.json`, builds the browser cache payload, finds or opens a Dianxiaomi page, writes `localStorage.dxm_aliexpress_evidence_store_v1`, and can read the browser-side cache summary back. Verified syntax, inspect, direct WebBridge sync, elevated Node tool sync, readonly edit-page preflight after sync, hardcoded-task scan, and network safety (`save.json/publish/online/offline` requests remained 0). Formal evidence store currently has 0 records, so current ASIN still blocks as `category_evidence_missing`. |
| 2.1.4 | DXM Automation V1 - NEW | 2026-07-01 | Added readonly edit-page preflight readback for WebBridge verification. Exposes `window.__DXM_AUTOMATION_V1_READONLY_PREFLIGHT__()` and DOM JSON node `#dxm-automation-v1-readonly-preflight-json`, returning version, URL, product ID, ASIN, evidence-store status, category evidence, price, category, freight template, Origin/required attributes, publish-risk controls, blockers, and save-to-wait-publish safety status without clicking, filling, saving, or publishing. Syntax check and hardcoded-task scan passed. No Dianxiaomi business action executed. |
| 2.1.3 | DXM Automation V1 - NEW | 2026-07-01 | Added ASIN-level AliExpress evidence preflight integration for edit pages. The script now reads `dxm_aliexpress_evidence_store_v1` from browser localStorage / page globals / DOM JSON, turns verified records with `dxmCandidateCategory` into category-modal plans, keeps learned rules as fallback, and reports explicit preflight reasons such as missing evidence, unverified evidence, or missing DXM candidate category before any save. Added `tools/aliexpress-evidence-browser-cache.js` to export the project evidence store into the browser cache format. Syntax checks and temporary-store export checks passed. No Dianxiaomi save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, collection, or claim executed. |
| aliexpress-entry-v1 | Config / Tools | 2026-07-01 | Added AliExpress entry configuration and checker: `config/aliexpress-entry.json` and `tools/aliexpress-entry-check.js`. The automation primary entry is `https://www.aliexpress.com/` in Google Chrome with existing login state; the Chrome bookmark-bar `速卖通` entry is recorded as manual fallback only. The config explicitly allows search/image-search/read-evidence actions and forbids order/cart/favorite/chat/account/payment/address/message/personal-data submission actions. Verified with syntax, JSON parse, config check, and live readonly WebBridge open of `https://www.aliexpress.com/`; page title loaded and logged-in account text `您好，ae165012 帐户` was visible. No search/product/action submission was executed. |
| aliexpress-evidence-store-v1 | Tools / Config | 2026-07-01 | Added generic ASIN-level AliExpress evidence schema and store: `config/aliexpress-evidence.schema.json`, `runs/aliexpress-evidence-store.json`, and `tools/aliexpress-evidence-store.js`. The tool can initialize, list, get, upsert, and mark evidence statuses such as `aliexpress_verified`, `evidence_missing`, and `evidence_split`. Verified with syntax checks, JSON parse checks, empty store listing, and a temporary-file upsert/get test. No browser or Dianxiaomi business action executed. |
| 0.1.34 | DXM Amazon Crawlbox V1 | 2026-07-01 | Generic current-batch row selection hardening: status readback now includes `currentBatchSelectionPlan` with selectable current-batch rows, checked current rows, checked old/other rows, missing batch ASINs, and a machine-readable reason. Batch selection now clears non-current checked rows before selecting current rows and blocks if old/other rows remain checked. No task-specific ASIN, store, count, or date is hardcoded. Syntax check passed; live readonly verification returned `canAttemptSelection=false` when no current-batch rows were visible. |
| 0.1.33 | DXM Amazon Crawlbox V1 | 2026-07-01 | Readonly status terminology cleanup: adds `publishControlVisible` to clarify that the field means a publish-related control/text is visible, not that publish was executed. Keeps legacy `publishVisible` for compatibility. Syntax check passed; live readonly verification confirmed v0.1.33 injection before v0.1.34 work. |
| 0.1.32 | DXM Amazon Crawlbox V1 | 2026-07-01 | Readonly status-readback hardening: exposes structured page status through `window.__DXM_AMAZON_CRAWLBOX_STATUS_READBACK__` and `#dxm-amazon-crawlbox-status-readback-json`, including script version, URL/page, collection counts, auto-claim state, current-batch ASIN rows, checked current/old rows, and dangerous publish/Product Development signals. Added `tools/dxm-status-readback.js`. Syntax checks passed. Live readonly verification confirmed `未认领(1)` was old row `B07D5DN269`, unchecked, with 0 current-batch rows visible; no business action executed during this verification. |
| validation-claim-complete-edit-gated-2026-07-01 | Live Validation | 2026-07-01 | WebBridge continuation with `DXM Amazon Crawlbox V1 v0.1.31`: selected exactly 9 current-batch rows, kept claim modal top tab on `全部`, selected concrete `Halo Home Store` checkbox under left-side `速卖通海外托管`, read back `已选(1)`, and confirmed claim. Dianxiaomi returned success 9 / fail 0 / duplicate 0. `速卖通海外托管 > 采集箱` readback shows `采集箱(9)`, `发布中 (0)`, `发布失败 (0)`. First two edit attempts were blocked before save by `category_evidence_missing` and visible price mismatch against the task formula; no publish, one-click publish, Product Development draft handling, or save to wait-to-publish executed. |
| 0.1.31 | DXM Amazon Crawlbox V1 | 2026-07-01 | Claim-modal group-selection correction: keep the modal top tab on `全部`, select the concrete store checkbox inside the left-side `速卖通海外托管` group, read back `已选(1)`, then confirm. Store display name is diagnostic only and no longer blocks the flow when the required group has one concrete store checkbox. The script still refuses `全选`, Product Development / Draft, and wrong-channel selections. Syntax check passed. |
| validation-claim-blocker-2026-07-01 | Live Validation | 2026-07-01 | After user-installed `DXM Amazon Crawlbox V1 v0.1.30`, the 100-category first-batch claim step was retried. Page readback confirmed v0.1.30, 9 current-batch rows were selected and read back twice, and old `B07D5DN269` remained unchecked. The claim modal still did not render `Halo Home Store` after two 20-second waits, one page refresh, top-channel `速卖通海外托管` click, and exact store search. Stopped before `确定`; no claim confirmation, edit, save, wait-to-publish move, final publish, one-click publish, or Product Development draft handling executed. |
| 0.1.30 | DXM Amazon Crawlbox V1 | 2026-07-01 | Claim-modal data-read recovery: when the claim store list or page data cannot be read as concrete store rows, the pipeline waits up to 20 seconds, then refreshes the Dianxiaomi page once and resumes from current-batch claim opening. The guard still refuses `全选` as a target-store substitute and still blocks Product Development / wrong-channel selections. Syntax check passed. |
| validation-100x100-params-2026-07-01 | Documentation | 2026-07-01 | Recorded confirmed parameters for the 100-category stress test: `速卖通海外托管` / `Halo Home Store` / `A1`, Amazon search source, 100 products with one per category, USD 5-20, goods value `Amazon displayed price USD x 7 x 1.55`, AliExpress category evidence, safe adjacent category allowance, `auto_ready` execution gate, exception queue, and wait-to-publish readback. No userscript behavior change and no Dianxiaomi business action executed. |
| validation-preflight-100x100-2026-07-01 | Documentation | 2026-07-01 | Added `docs/validation-100-preflight-confirmation.md` with required business-parameter confirmations, 100-product list template fields, CSV header suggestion, launch checklist, and no-action defaults. No userscript behavior change and no Dianxiaomi business action executed. |
| validation-plan-100x100-2026-07-01 | Documentation | 2026-07-01 | Added `docs/validation-100-category-100-product-plan.md` for 100-category 100-product Validation preparation. Defines entry gates, no-publish boundaries, United States Origin rule, phased 10-item batches, stop conditions, report fields, and risk levels. No userscript behavior change and no Dianxiaomi business action executed. |
| rules-2026-07-01 | Documentation / Skills | 2026-07-01 | Exception-rule consolidation for Validation preparation. Added `docs/exception-rules.md` and linked AGENT/AGENTS, execution rules, category system, field-fill skill, three-step edit skill, and category resolver JSON. Tightened Origin for the current overseas-hosting batch to United States with no automatic Mainland China fallback. No userscript behavior change and no Dianxiaomi business action executed. |
| 2.1.2 | DXM Automation V1 - NEW | 2026-07-01 | B08 recovery hardening after successful wait-to-publish move: Use and Feature are now explicit required-attribute steps and preflight readback fields, native `请选择产品属性` blocks preflight, and Feature only prioritizes BPA-free when source evidence actually contains BPA-free. Syntax check passed. |
| 2.1.1 | DXM Automation V1 - NEW | 2026-07-01 | Same behavior as the direct recommended-dropdown release: Function/Use/Feature open the dropdown and choose from visible recommended options first, without default typing/searching and without Enter/Tab confirmation. Version renumbered per user request to start with 2 and avoid disallowed digits in the current script version. Syntax check passed. |
| 1.1.105 | DXM Automation V1 - NEW | 2026-07-01 | Simplifies dynamic required dropdown flow: Function/Use/Feature now open the dropdown and choose from Dianxiaomi's recommended visible options first, without typing/searching by default and without Enter/Tab confirmation. Search/filter is only a fallback when no safe recommended option is visible. Numeric/internal-ID retry from v1.1.104 remains. Syntax check passed. |
| 1.1.104 | DXM Automation V1 - NEW | 2026-07-01 | Dynamic attribute dropdown commit fix: Function/Use/Feature required dropdowns no longer use Enter/Tab keyboard confirmation after clicking a real option, because some DXM/Ant fields can display the backend option ID such as `23399620951` instead of the label. If a numeric/internal ID is read back, the field is cleared and reselected once with click-only commit. Save/publish controls are unchanged. Syntax check passed. |
| 1.1.103 | DXM Automation V1 - NEW | 2026-07-01 | Required dropdown no-data recovery: when a typed search such as `Storage` / `Other` filters a field to `暂无数据`, the script now clears the dropdown search text to restore Dianxiaomi recommended options, then chooses the safest real visible option before classifying the field as failed. This targets Function/Use/Feature and other required Ant/select fields; save/publish controls are unchanged. Syntax check passed. |
| 1.1.102 | DXM Automation V1 - NEW | 2026-07-01 | Generic safe-option fallback for required Feature/Function/Use dropdowns: exact obvious visible options are preferred, safe common options are used only when product evidence supports them, and `Other/其他` is a legal fallback when it commits. Unknown required Feature now uses evidence-based candidates such as BPA-free plastic, Waterproof, Reusable, Multiple Uses, and Adjustable Size only when supported, then falls back to Other. Current-field dropdown anchoring from v1.1.100 remains. Syntax check passed. |
| 0.1.29 | DXM Amazon Crawlbox V1 | 2026-06-30 | Claim modal fixed-channel guard: every claim must select a store under `速卖通海外托管`; candidates are scoped by DOM order between the `速卖通海外托管` header and the next channel header, wrong checked targets such as `产品开发 / 草稿箱` are unselected, and confirmation blocks with `store_selection_wrong_target` if the final target is not under the required channel. |
| 1.1.92 | DXM Automation V1 - NEW | 2026-06-30 | Replaces the old keyword-first category flow with AliExpress/DXM visible category evidence as the category gate. Active visible category rules from `skills/category-resolver/learned_rules.json` are now synced into the userscript even when they have no numeric categoryId. Edit-page category selection builds modal search plans from verified AliExpress/similar-product/DXM-visible evidence first; if no evidence rule exists, category selection blocks as `aliexpress-evidence-required` instead of falling back to legacy keyword search and saving. Adds verified visible evidence rules for Portable Soap Dishes, Coat Hooks, Cable Organizers, Pen Holders, and Kitchen Drains & Strainers. |
| 1.1.91 | DXM Automation V1 - NEW | 2026-06-30 | Strict price-source fix after `B0D65JFRX4` was saved with wrong CNY `1913.61`: supply price can only be calculated from current-task Amazon original USD price multiplied by the current task exchange rate and multiplier. Removed fallback to Dianxiaomi edit-page price, `product.price`, `minPrice`, `maxPrice`, cached Amazon batch price, UI numeric scans, and manual CNY supply-price override. Edit-page and payload preflight now block missing trusted Amazon original price or visible/SKU price mismatch. |
| 1.1.90 | DXM Automation V1 - NEW | 2026-06-30 | Material expansion fix after v1.1.89 live validation: before selecting required `Material`, the edit-page pipeline now expands the product-attribute section when `smtDynamicAttr10` exists but is hidden behind `+展开`. Kitchen sink strainer material candidates now prioritize `不锈钢(Stainless Steel)` / metal values. Category selection, save, move-to-wait-publish, one-click-publish, and publish logic are unchanged. |
| 1.1.89 | DXM Automation V1 - NEW | 2026-06-30 | AliExpress Category Resolver sink-strainer mapping integration: promotes the validated `postCategoryId=200231151` / Dianxiaomi visible path `厨房水槽水漏、过滤网(Kitchen Drains & Strainers)` into active category rules and narrows the edit-page category modal plan to search `水槽/Sink/Drain/Strainer` with exact Kitchen Drains & Strainers leaf terms. This targets category selection only; save/publish logic remains unchanged and edit-page preflight is still required before any save. |
| 1.1.88 | DXM Automation V1 - NEW | 2026-06-30 | Category-family scoring foundation: adds reusable allow/reject scoring for desk pen holders, adhesive cable clips, kitchen sink strainers, and adhesive wall hooks. Dianxiaomi category search API and edit-page category modal candidates now use family scoring; diagnostics include `familyScore`. Rejects wrong paths such as conductive glue paste for cable clips and commercial kitchen appliances for sink strainers. Other edit-page fields and save/publish controls are unchanged. |
| 1.1.87 | DXM Automation V1 - NEW | 2026-06-30 | Rotating title-risk false positive fix: `Rotating` is treated as a functional leading word, so titles such as `Rotating Pen Organizer...` are not blocked as suspected brand/trademark/platform risk solely because `Rotating` is first. Other edit-page fields and save/publish controls are unchanged. |
| 1.1.86 | DXM Automation V1 - NEW | 2026-06-30 | Color single-vs-multi rule: Color no longer defaults to `MULTI` whenever multiple options are visible. It selects a single visible option directly, uses source evidence for supported single colors, and only uses `MULTI/多色` for explicit multi-color evidence or unknown-color fallback. Other edit-page fields and save/publish controls are unchanged. |
| 1.1.85 | DXM Automation V1 - NEW | 2026-06-30 | Preflight readback fix: after v1.1.84 fixed Material to `硅胶(Silicone)` but preflight still read Material and Ships From as empty, selected-text readback now includes visible Ant combobox/search input values and Ships From accepts visible `美国/United States` section text. Selection behavior is unchanged. |
| 1.1.84 | DXM Automation V1 - NEW | 2026-06-30 | Material prefilter commit fix: after v1.1.83 left Material as search input `硅胶(Silicone)` but not a committed selected item, Material now filters the dropdown to exact `硅胶(Silicone)` before option lookup and keyboard confirmation. Other edit-page logic is unchanged. |
| 1.1.83 | DXM Automation V1 - NEW | 2026-06-30 | Material no-keyboard commit fix: after v1.1.82 clicked `硅胶(Silicone)` but ended with material input `木头(Wood)`, Material now prioritizes exact `硅胶(Silicone)` and skips Enter/Tab keyboard commit after option click. Other edit-page logic is unchanged. |
| 1.1.82 | DXM Automation V1 - NEW | 2026-06-30 | Material dropdown commit fix: after v1.1.81 fixed Origin but left Material only as a search input value, Material now uses the fixed dropdown option click/lock path with keyboard confirmation. Category, freight, custom attributes, title, High-concerned chemical, and Origin logic are unchanged. |
| 1.1.81 | DXM Automation V1 - NEW | 2026-06-30 | Fixed current-field dropdown opener: after v1.1.80 still stopped at Origin, fixed selection now closes previous dropdowns, clicks the current field once, confirms a visible dropdown rendered, then searches current visible options. Syntax check passed. |
| 1.1.80 | DXM Automation V1 - NEW | 2026-06-30 | Fixed required-attribute dropdown sequencing: after v1.1.79 selected High-concerned chemical but stopped at Origin even though `美国(Origin)(US(Origin))` is visible, fixed selection now closes previous dropdowns before opening the next field, waits longer for render, and retries exact visible option lookup. Syntax check passed. |
| 1.1.79 | DXM Automation V1 - NEW | 2026-06-30 | Fixed required-attribute dropdown lookup: after v1.1.78 achieved `Portable Soap Dishes` but left High-concerned chemical / Origin / Material unselected, fixed dropdown selection now falls back to all visible dropdown options and search-filter retry when active-dropdown matching misses. Syntax check passed. |
| 1.1.78 | DXM Automation V1 - NEW | 2026-06-29 | Soap-dish category priority fix: after v1.1.77 selected `皂盒(Soap Box)`, the soap-dish plan no longer accepts `Soap Box`, rejects that row, and search-result matching prioritizes earlier leaf terms before shorter text. Syntax check passed. |
| 1.1.77 | DXM Automation V1 - NEW | 2026-06-29 | Category search-result leaf click: after v1.1.76 blocked wrong fallback but still failed category, live diagnosis showed `Portable Soap Dishes` is present as a full-path `.search-result-item` after searching `浴室`; the old matcher filtered these path rows. v1.1.77 clicks a matching search-result row with reject terms before column fallback. Syntax check passed. |
| 1.1.76 | DXM Automation V1 - NEW | 2026-06-29 | Category wrong-fallback guard: after v1.1.75 live validation showed the specific soap-dish modal plan could still fall through into generic `category-button-modal` and select `Bathroom Accessories Sets`, a planned category failure now stops with `genericFallbackDisabled=true` instead of writing a wrong generic category. Syntax check passed. |
| 1.1.75 | DXM Automation V1 - NEW | 2026-06-29 | Category result exact-leaf priority fix: passes category plan reject terms into leaf matching so returned nodes containing wrong bathroom categories such as `Bathroom Accessories Sets / 浴室配件套装` cannot be clicked when targeting `Portable Soap Dishes`. Syntax check passed. |
| 1.1.74 | DXM Automation V1 - NEW | 2026-06-29 | Category modal search-submit fix: writes the search input without immediate blur, dispatches `beforeinput/input/change` plus Enter key events, and triggers the Search button through React mouse/click handlers plus normal click. Syntax check passed. |
| 1.1.73 | DXM Automation V1 - NEW | 2026-06-29 | Category-modal leaf click fix: matching now returns the deepest matching leaf node, the modal search plan tries direct leaf click after top search, and confirmation can use the global bottom `选择 / 确定` button when it sits outside the modal subtree. Syntax check passed. |
| 1.1.72 | DXM Automation V1 - NEW | 2026-06-29 | Narrow fix after v1.1.71 validation: category modal matching rejects dialog/search/close container text so the actual leaf item is clicked, Brand matching excludes non-required manufacturer/responsible-person fields, and the edit-page pipeline stops at category failure instead of continuing into category-dependent fields. Syntax check passed. |
| 1.1.71 | DXM Automation V1 - NEW | 2026-06-29 | Narrow fix after v1.1.70 validation: removes old automatic variation fill from `loadEditJson`, expands category modal plans for the five validation families, adds conservative first-token brand cleanup for Amazon titles such as `Micoyang ...`, strengthens input/checkbox event chains, and exposes `window.__DXM_AUTOMATION_V1_LAST_RESULT__` for read-only verification. Syntax check passed. |
| 1.1.70 | DXM Automation V1 - NEW | 2026-06-29 | Full edit-page sequential pipeline restored after workflow clarification: exits minimal two-field mode, runs one exclusive ordered pass for title, category, required attributes, custom attributes, marketing image, ships-from/variation, freight template 111, sanitization, and preflight; soap-dish category starts from `浴室` and targets `便携肥皂盒 / Portable Soap Dishes`; unknown required selects fall back to `其他(Other)`. Syntax check passed; live validation pending. |
| 1.1.69 | DXM Automation V1 - NEW | 2026-06-29 | Single-sample execution-right isolation: manual edit-rule action is limited to sample `167487782006885971`, temporarily blocks page scroll APIs during execution, maintains field locks, and only attempts High-concerned chemical = `天然未处理(None)` and Origin = `美国(Origin)(US(Origin))`. Other edit-page and batch logic is not run in this minimal mode. Syntax check passed; live validation pending after Tampermonkey overwrite. |
| 1.1.68 | DXM Automation V1 - NEW | 2026-06-29 | Single-sample edit-page stability repair: batch work paused; edit-page visible rules now use a sequential locked field pipeline instead of repeated scan/retry, auto edit-rule watcher is disabled, edit-page click helpers avoid `scrollIntoView`, high-concerned chemical is fixed to `天然未处理(None)`, origin to `美国(Origin)(US(Origin))`, material to silicone candidates, and multiple color options choose `MULTI`. Syntax check passed; clean live validation pending after Tampermonkey overwrite. |
| 1.1.56 | DXM Automation V1 - NEW | 2026-06-29 | Narrow fix after v1.1.55 live verification: dropdown search is scoped to the currently opened dropdown, legacy Ant option classes are supported, select acceptance keys are sent after search, broad multi-field attribute containers are penalized, and custom attribute cleanup targets all `属性名/属性值` rows and row-local `icon_close` delete controls. |
| 1.1.52 | DXM Automation V1 - NEW | 2026-06-29 | Edit-page visible-field correction guard: exact modal category plans now outrank drawer/storage fallback, soap-dish titles cannot be remapped to Home Office Storage, required attributes use fixed defaults (`Brand=None`, `Feature=Other`, `Function=Other`, unknown required selects prefer `Other`), existing marketing images are recognized, custom attributes are normalized before deletion fallback, and freight template selection reports explicit pass/fail status for preflight. |
| 1.1.51 | DXM Automation V1 - NEW | 2026-06-29 | Category modal selection fix based on the two-search-input mechanism: uses the top category search for broad category, column search inputs for narrowed terms, then clicks the matched leaf node before confirming. |
| 1.1.50 | DXM Automation V1 - NEW | 2026-06-29 | Edit-page category modal control fix: automatic edit-page rules no longer open the category modal on page load, and manual preflight category selection now attempts modal confirm/close/Escape plus a guarded DOM fallback after the category is visibly written. |
| 0.1.28 | DXM Amazon Crawlbox V1 | 2026-06-29 | Batch-flow optimization: controlled batch claim remains the default, single-store claim auto-selects the only store while multi-store uncertainty blocks as `store_uncertain`, Logo review sheet now classifies `allow_collect` / `needs_review` / `skip_logo_or_brand_risk`, and run metrics export includes quantified stage-duration summary with unmeasured stages marked explicitly. |
| 1.1.49 | DXM Automation V1 - NEW | 2026-06-29 | Fix edit-page visible-field completion after v1.1.48 validation: URL-change watcher re-runs edit-page rules, custom attributes are cleared or normalized to <=70 chars, low-risk category search terms added for cable clips, wall hooks, sink strainers, and soap dishes. |
| 1.1.48 | DXM Automation V1 - NEW | 2026-06-29 | Batch save quality: PC detailWeb now requires current-product images first, then description; preflight blocks plain-text-only PC descriptions. |
| 1.1.42 | DXM Automation V1 - NEW | 2026-06-27 | Mac migration recovery marker; confirms userscript execution before panel creation. |
| 1.1.41 | DXM Automation V1 - NEW | 2026-06-25 | Category mapping table for Pen Holders; prevents global Organizer mapping. |
| 1.1.40 | DXM Automation V1 - NEW | 2026-06-25 | Title brand filtering and category behavior updates before mapping refactor. |
| 1.1.37 | DXM Automation V1 - NEW | 2026-06-24 | Edit-page preflight, category selection, required attributes, custom attribute cleanup, freight 111 verification, one retry. |
| 0.1.22 | DXM Amazon Crawlbox V1 | 2026-06-27 | Mac migration panel-position recovery and root Dianxiaomi match rules. |
| 0.1.21 | DXM Amazon Crawlbox V1 | 2026-06-25 | Default target 100, category limit 15, duplicate policy, same-color/variant handling. |
| 0.6.2 | save Payload V3 | 2026-06-27 | Mac migration recovery marker and safer panel mounting. |
| 0.6.1 | save Payload V3 | 2026-06-25 | Active payload capture helper. |
| 0.3.0 | Interface Detector V2 | Existing | Interface discovery helper. |
| 0.2.5 | Single Submit Tester | Existing | Test helper. |
| 0.2.0 | Auto Executor | Existing | Auxiliary executor. |

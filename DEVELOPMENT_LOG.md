# Development Log

## 2026-07-06

- Prepared the first source-level split of deterministic logic out of `src/dianxiaomi-automation-v1-merged-new.user.js` without changing the installed single-userscript runtime artifact. Added `src/dxm-automation-core/text-rules.js`, `pricing-rules.js`, `pc-detail-rules.js`, and `index.js` as browser-effect-free CommonJS modules for text sanitization/title/description rules, Amazon USD-to-CNY price and dimension helpers, and PC detail image-first HTML/image analysis. Added `tools/dxm-automation-core.test.js` as the second explicit Node assertion test beside `tools/aliexpress-evidence-policy.test.js`. No live Dianxiaomi page action, collection, claim, edit, save, final publish, one-click publish, Product Development draft handling, or browser automation was executed.
- Added the documentation-drift control rule to `AGENT.md` as a long-term hard rule: development tasks must update relevant status, test, README, execution-rule, or development-log documents before completion when behavior or test surface changes. Updated `docs/current-status.md`, `docs/test-plan.md`, `docs/test-results.md`, `README.md`, and `README.zh-CN.md` to reflect the new pure-module test surface and the remaining no-unified-test-runner gap.
- Replaced the previous SVG architecture overview assets with the approved bilingual proven execution flow PNGs: `docs/assets/architecture-overview-en.png` and `docs/assets/architecture-overview-zh.png`. Updated README, architecture docs, audit notes, test results, and SuperPowers plan references so active docs no longer point at the removed SVG files.
- Added the project maintenance-language rule: Chinese is the default language for PR titles, PR bodies, review discussion, and maintenance notes. Updated README, Chinese README, architecture docs, current status, AGENT rules, and the GitHub PR template so future PRs default to Chinese while still allowing English code identifiers, exact logs, external platform terms, and bilingual summaries where needed.
- Local audit found and fixed a first-split pricing helper gap: Amazon displayed price ranges such as `$8.99 - $12.99` now parse to the highest displayed value before applying the USD x 7 x 1.55 goods-value formula. The regression is covered in `tools/dxm-automation-core.test.js`.

## 2026-07-05

- Live-verified the claim-modal rule after v0.1.50 using the existing two unclaimed products `B0G2RQLGQ9` and `B0FVF8CN9Z`. Because the unclaimed table readback showed total unclaimed `2` and both rows were exactly the current batch, the test safely used the table header selection to advance into native `批量认领` without mixing old products. The claim modal was then operated by the fixed business rule: click only platform `全部`, locate the `速卖通海外托管` group, select the single store under that group (`Halo Home Store` in this run), read back `已选(1)`, and click `确定`. Dianxiaomi returned `速卖通海外托管采集认领执行完成，成功 2 条，失败 0 条，跳过重复数据 0 条`. The result modal was closed. No edit page, save-to-wait-publish, final publish, one-click publish, Product Development draft handling, or rule-file change was executed. Remaining engineering gap: row-level current-batch checkbox selection still does not respond to normal DOM/CDP clicking and needs a later VXE-aware implementation before scaling to mixed old/new unclaimed tables.

- Live-validated `DXM Amazon Crawlbox V1 v0.1.50` after the current-batch row-selection entry change. Browser readback confirmed active version `0.1.50`; the current two unclaimed ASINs `B0G2RQLGQ9` and `B0FVF8CN9Z` were still present exactly once each, unclaimed count remained `2`, and the main edit automation was not operated. Calling the unified current-ASIN entry filled the link textbox but did not create/advance a claim pipeline. Calling the public segmented claim entry and dispatching the panel `claimCurrentBatchUnclaimed` button event also did not mutate either row checkbox or open the batch-claim modal. Therefore v0.1.50 did not verify the row-selection fix: the live blocker is now narrowed to the public/entry execution path not invoking `selectCurrentBatchUnclaimedRows()` / `openBatchClaimModal()` in the page, rather than VXE checkbox click reflection. The link input was cleared after the test. No claim confirmation, edit page, save-to-wait-publish, final publish, one-click publish, Product Development draft handling, or rule-file change was executed.

- Live-validated `DXM Amazon Crawlbox V1 v0.1.49` after the claim-modal store-selection rewrite. Active page version read back as `0.1.49`; main edit automation remained `DXM Automation V1 V2.1.75` and no edit/save/publish action was executed. Two new intended test products were used: `B0G2RQLGQ9` (office monitor stand / desk organizer) and `B0FVF8CN9Z` (pet grooming gloves). An operator input mistake first provided invalid truncated ASIN `B0FVF8CN9`, so only `B0G2RQLGQ9` was collected in that attempt. The correct `B0FVF8CN9Z` was then collected separately. After refresh, Dianxiaomi readback showed both products in the unclaimed table exactly once, unclaimed count `2`, and the link input was cleared at the end of the test.
- The v0.1.49 store-selection path was not reached in this validation because both the unified current-ASIN entry and the public segmented claim entry failed to advance from two visible current-batch rows to row checkbox selection / native `批量认领`. Read-only status confirmed the script recognized both batch rows, both row checkboxes were present and visible, native `批量认领` was visible, and WebBridge preflight allowed `本批批量认领` with no block reason. Therefore the current blocker is before the claim modal: current-batch claim entry / VXE row-selection execution did not fire or did not mutate row checkbox state. No claim confirmation, edit page, save-to-wait-publish, final publish, one-click publish, Product Development draft handling, or rule-file change was executed.

- Live-validated `DXM Amazon Crawlbox V1 v0.1.48` on the Dianxiaomi data-acquisition page with two new ASINs not used in the earlier validation set: `B0CCDJQ7MM` (office drawer organizer) and `B0FFG4BPJY` (pet glove wipes). Browser page confirmed the active crawlbox panel as `v0.1.48`; main edit automation was present as `DXM Automation V1 V2.1.75` and was not operated for edit/save/publish. Collection through the unified ASIN entry succeeded in Dianxiaomi with `已成功采集2条,失败:0`; both new ASINs appeared in the unclaimed table exactly once. No duplicate collection dialog appeared for these two products.
- The same live validation exposed three crawlbox-flow blockers before edit-page work: after the collection success dialog appeared, the script did not auto-close the real `链接采集` modal and did not advance into batch claim without manual help; the claim modal initially showed only the channel groups and required a click on the top `速卖通海外托管` platform filter before `Halo Home Store` rendered; because the claim confirmation was completed through segmented/manual rescue, the v0.1.48 automatic input cleanup did not run. Manual rescue selected only the two current ASIN rows, read back `已选中 2 条数据`, selected only `Halo Home Store` with `已选(1)`, clicked claim `确定`, and Dianxiaomi returned `成功 2 条，失败 0 条，跳过重复数据 0 条`. The link input was then manually cleared and read back empty for the current two ASINs. No edit page, save-to-wait-publish, final publish, one-click publish, Product Development draft handling, or rule-file change was executed.

## 2026-07-04

- Live-completed the remaining collection-box products after the browser userscript was overwritten with `DXM Automation V1 - NEW v2.1.75`. For `B01G5K1AM0` / editId `167487782012872463`, the collection-box recovery call did not return within about 2 minutes and was interrupted from the caller side, then page readback showed the new required-attribute blank-click confirmation worked for High-concerned chemical, Origin, and Material (`PET+PE材质(PET+PE)`). Segmented single-field retries repaired Brand / Function / Feature, and shipping/postage-only selected template `111`; final preflight passed with blockers `[]`. The exact native `保存并移入待发布` button was clicked once, and the success modal `产品已移入待发布，请在「待发布」中查看！` appeared. Authoritative wait-to-publish readback passed for `B01G5K1AM0`: category `收纳架(Racks & Holders)`, price `CNY 206.04`, stock `15`. The same wait-to-publish readback also confirmed `B01E2EYG4U` was already present with category `Storage Boxes & Bins`, price `CNY 216.57`, stock `15`; `速卖通海外托管`采集箱 count was `0`. No final publish, one-click publish, recollection, reclaim, Product Development draft handling, code change, or rule-file change was executed during this live completion.
- Live-validated installed browser userscript `DXM Automation V1 - NEW v2.1.60` on collection-box edit product `B01G5K1AM0` / editId `167487782012872463` for the user-confirmed postage arrow-click change. No save, final publish, one-click publish, recollect, reclaim, or Product Development draft action was executed. The browser confirmed active version `2.1.60`; initial readonly preflight showed the page still needed postage template `111`. A normal remaining-flow run did not return within 90 seconds and was interrupted from the caller side before `shipping-postage`. A shorter guarded remaining-flow run did reach `shipping-postage`: Ships From read back as `美国(United States)`, but postage remained `--- 请选择运费模板 ---`. The stage was non-fatal and continued after failure, but `postage.ok=false`, `postage.reason=stage timeout: shipping-postage.fast-postage`, and `postage.openResult=null`, so the arrow-click repair did not complete a successful open/select/readback in the live page. Final readonly preflight stayed blocked by `postage template is not 111`. Evidence screenshot saved as `runs/b01g5k1am0-v2160-postage-still-not-selected.png`.
- Prepared `DXM Automation V1 - NEW v2.1.60` for the user-confirmed `shipping-postage` click-target issue. Scope stayed narrow: only the main-flow fast postage selector was changed. The code now opens the `运费模板` control by first clicking the visible right-side arrow inside the field; if no arrow element is visible, it clicks the right edge of the same selector rectangle instead of the whole field/container. It still selects only template `111`, immediately reads back the selected text, and returns failure without blocking later stages when option `111` is not visible. Rules, category, price, required attributes, variation, marketing images, save-to-wait-publish, final publish, one-click publish, collection, reclaim, and Product Development drafts were not changed. Local verification passed with `node --check` and `git diff --check`. Browser validation is still pending after the userscript is overwritten.
- Live-validated installed browser userscript `DXM Automation V1 - NEW v2.1.59` on collection-box edit product `B01G5K1AM0` / editId `167487782012872463` for the third core card point: remaining-flow stage return and self-recovery. Browser scope was declared and kept narrow: open/refresh the exact edit page, verify ASIN/editId/version, run `window.__DXM_AUTOMATION_V1_APPLY_REMAINING_EDIT_RULES__` with the user-provided timeout options, and never save, final publish, one-click publish, recollect, reclaim, or touch Product Development drafts. Readonly preflight confirmed active version `2.1.59`. The first run advanced page state through category `收纳架(Racks & Holders)`, price `CNY 206.04`, PC-description images, Ships From, and reached progress stage `marketing-images`; it was externally interrupted after a long wait and then checked readonly. A guarded second run returned in about 22.5s with `ok=false`, but it successfully continued past `shipping-postage` into later stages: postage `111` read back selected, variation required fields passed (`logisticValue=0`, stock `15`, SKU `B01G5K1AM0`, weight `0.1`, size `13.21 x 13.21 x 14.99`, shipFrom United States), marketing images read back `2/2`, and `text-sanitization` ran. Final preflight correctly stayed blocked only by required `Material` still showing `请选择`, so no save was clicked. Evidence screenshot saved as `runs/b01g5k1am0-v2159-final-material-blocker.png`.
- Prepared `DXM Automation V1 - NEW v2.1.48` for the collection-box recovery blocker observed on `B07MHXF8H5` / editId `167487782012872539`, where the recovery flow stopped before Use at `required-attributes.high_concerned_chemical`. Scope stayed narrow: only the fixed `High-concerned chemical` execution path was hardened. The fixed value remains exactly `天然未处理(None)`, with a dedicated fast exact selector and readback before falling back to the existing Ant multi-select exact path. Use candidate/scoring, new collection, claim, category, price, variation, PC description, marketing image, save-to-wait-publish, final publish, one-click publish, and Product Development draft logic were not changed. Local verification passed with `node --check`, `git diff --check`, and targeted assertions for v2.1.48, fixed chemical single value, dedicated selector, label guard, bounded routing, original fallback, and unchanged Use wiring. No browser action, Dianxiaomi field edit, save, move-to-wait-publish, final publish, one-click publish, collection, reclaim, or Product Development draft action was executed during this source update.
- After the user overwrote the browser userscript manager with `DXM Automation V1 - NEW v2.1.44`, live-validated the installed browser version on wait-to-publish product `B0GKLXJCTZ` / editId `167487782012872503`. Browser action scope was declared before execution: open the existing wait-to-publish edit page, verify version/ASIN/editId, run the shared remaining edit-page recovery flow, and never save, final publish, one-click publish, recollect, reclaim, or touch Product Development drafts. Initial readonly preflight showed the expected PC-description image blockers while marketing images were readable. The installed `v2.1.44` flow completed with `ok=true`, PC description current product images `5/2`, image-first `5/2`, marketing images ready, and `risks=[]`. A final readonly preflight confirmed version `2.1.44`, pass `true`, and blockers `[]`. No save or publish action was executed, and the validation tab was closed.
- Prepared and live-validated `DXM Automation V1 - NEW v2.1.44` after the user installed the previous update and asked to verify on an existing wait-to-publish product while confirming the code path must remain the collection-box re-edit / recovery logic. Browser action scope was declared before execution: open wait-to-publish, choose an existing product only for validation, run the remaining edit-page flow, and never save, final publish, one-click publish, recollect, reclaim, or touch Product Development drafts. Selected `B0GKLXJCTZ` / editId `167487782012872503`, a product previously moved from collection-box recovery to wait-to-publish. Readonly browser state confirmed `v2.1.43`, ASIN/editId match, marketing images readable, and PC description image blockers present. Running the same remaining edit-page flow exposed a timing issue: the description write returned `imageState 5/2`, but immediate readback still saw `0/2`; three seconds later readonly preflight passed. The source was narrowly advanced to `v2.1.44` by adding PC-description image readback polling after CKEditor/hidden detail writes. Local verification passed with `node --check`, `git diff --check`, and targeted assertions for v2.1.44, recovery entry, PC readback wait, marketing stage, and marketing wait. Live validation with temporary v2.1.44 injection then completed `ok=true`, `pc-description-images` read back `5/2` with `waitedMs`, marketing images remained readable, and `risks=[]`. No save or publish action was executed. The two validation browser tabs were closed, and `v2.1.44` was copied to clipboard for browser userscript-manager overwrite.
- Prepared and live-validated `DXM Automation V1 - NEW v2.1.43` for the user-confirmed wait-to-publish quality gaps: missing marketing images and PC descriptions that were text-only instead of image-first. Scope stayed narrow: source code only, no rule files, no collection, claim, category, price, required-attribute, variation, final publish, one-click publish, or Product Development draft logic was changed. The remaining edit-page / recovery flow now uses current product main images as the primary PC-description image source, stops at `pc-description-images` if the description is not image-first with current product images, adds a bounded `marketing-images` stage that clicks `一键生成` when missing, and waits/polls for generated marketing images while reading back from the real marketing-image section. Local verification passed with `node --check`, `git diff --check`, and targeted assertions for v2.1.43, recovery entry, product-image source, PC-image stop, marketing stage, marketing wait, and preflight blockers. Live validation opened wait-to-publish product `B07M74SH62` / editId `167487782012872549` only for verification. Initial readonly preflight blocked the known bad state (`PC description` images 0/2 and marketing images 0/2). Running the remaining edit-page flow without saving repaired the PC description to image-first with current product images (`5/2`) and clicked marketing `一键生成`; after the v2.1.43 wait/readback repair, readonly preflight passed with marketing images `2/2`, PC description images `5/2`, and `risks=[]`. No save, final publish, one-click publish, collection, reclaim, or Product Development draft action was executed. The two validation browser tabs were closed, and the latest script was copied to clipboard for browser userscript-manager overwrite.
- Reran the existing collection-box recovery entry on the second current-run product `B07MHXF8H5` / editId `167487782012872539` from the already-open edit page, after confirming the code already has `window.__DXM_AUTOMATION_V1_RECOVER_TO_WAIT_PUBLISH__` for products that failed to save from the collection box. No code or rule change was made during this live validation; no recollection, reclaim, Product Development draft handling, final publish, one-click publish, or save click was executed. The rerun kept the v2.1.41 category and price fixes (`Storage Boxes & Bins`, `CNY 151.57`) and repaired additional fixed attributes: Brand read back as `NONE(AE存量)*******(None)` and Frame Material as `塑料(Plastic)`. The flow then stopped at `required-attributes.use` with a stage hard timeout. A direct field-only Use retry did not return a structured result and was interrupted; the automation lock was reset. Final preflight remained `pass=false`, so `保存并移入待发布` was not allowed. Remaining blockers include Use / Function / Material still showing `请选择`, postage template `111`, Ships From readback, variation row `logisticValue` / stock / SKU / required variation attribute, and invalid imported custom attributes. Screenshot saved as `runs/b07mhxf8h5-v2141-second-retry-use-timeout.png`.
- Live-validated `DXM Automation V1 - NEW v2.1.41` on the second current-run 10-category product `B07MHXF8H5` / editId `167487782012872539` after the user overwrote the browser userscript. Browser action scope was: open the exact edit page, verify ASIN/editId/version, run guarded recovery with `save:false`, and only save if final preflight passed; no final publish, one-click publish, new collection, reclaim, Product Development draft handling, or code/rule repair was executed. The v2.1.41 category fix worked live: the category search selected and read back `收纳盒和收纳箱（有关婴儿食品储存的请发布到婴儿食品存储盒下）(Storage Boxes & Bins)` and did not select the previous wrong `Anti-Mosquito Incense` category. Price was corrected to `CNY 151.57`. The product was not saved because final preflight remained `pass=false`, with blockers including postage template `111` missing, required red-star attributes `Use` / `Function` / `Material` / `Brand Name` / `Frame Material` still unselected, Ships From readback not accepted, variation row `logisticValue` / stock / SKU incomplete, and invalid imported custom attributes. Screenshot saved as `runs/b07mhxf8h5-v2141-blockers.png`.
- Prepared `DXM Automation V1 - NEW v2.1.41` for the user-confirmed category-modal safety boundary: only the ASIN evidence-backed category selection path was changed. `asin-aliexpress-evidence-store` plans now build strict leaf aliases from the verified DXM leaf and use `strictDxmCandidateOnly` during category search-result scoring, category tree scoring, and selected-category readback. For `B07MHXF8H5`-style evidence where the expected leaf is `收纳盒和收纳箱(Storage Boxes & Bins)`, the strict matcher accepts the full target path, Chinese leaf, or English leaf, and rejects wrong/broad text such as `Anti-Mosquito Incense`, `Home & Garden`, and `Storage Boxes`. Rules, collection, claim, price, SKU, required attributes, save-to-wait-publish, final publish, one-click publish, and Product Development draft handling were not changed. Local verification passed with `node --check`, `git diff --check`, and targeted strict-matcher assertions. No browser action, Dianxiaomi field edit, save, move-to-wait-publish, final publish, one-click publish, collection, reclaim, Product Development draft handling, or rule-file change was executed during this source update.
- Live-validated `DXM Automation V1 - NEW v2.1.40` on the user-confirmed collection-box recovery product `B0GKLXJCTZ` / editId `167487782012872503` after the user overwrote the browser userscript. Browser action scope was declared before execution: open the exact edit page, verify ASIN/editId/version, run recovery with `save:false`, click exact native `保存并移入待发布` only after final preflight passed, then readonly wait-to-publish readback; no final publish, one-click publish, new collection, reclaim, or Product Development draft handling. `save:false` returned normally and repaired the previous required-attribute blockers: Product application scenarios read back as `厨房(Kitchen) 餐桌用(Dining table)`, Theme read back as `其他(Others)`, required attributes `missing=[]`, unsafe displays `[]`, and final preflight `pass=true` / `risks=[]` / `blockers=[]`. Exact native `保存并移入待发布` was clicked once. Authoritative wait-to-publish readback on `/web/smtlocalProduct/offline` passed: row found, SKU `B0GKLXJCTZ`, price `CNY 58.48`, stock `15`, category `餐垫(Placemats)`, status `saved_to_wait_publish_readback_passed`. The task-opened edit and wait-to-publish pages were closed through WebBridge (`closed=2`).
- Prepared `DXM Automation V1 - NEW v2.1.40` for the user-confirmed collection-box recovery entry path: editing products that are still in `速卖通海外托管 > 采集箱` and have not yet been saved to wait-to-publish. This is a narrow repair inside `window.__DXM_AUTOMATION_V1_RECOVER_TO_WAIT_PUBLISH__`'s existing required-attribute stage, not a change to collection, claim, direct edit URL / `forceEdit`, final publish, one-click publish, or Product Development drafts. The required-attribute pipeline now recognizes current-page visible red-star `Material`, `Product application scenarios`, and `Theme`, supports checkbox/radio groups before Ant-dropdown fallback, selects kitchen/table scenarios for Placemats-style kitchen mats, defaults generic Theme to `其他(Other)`, and keeps Material evidence-first with a real visible `Other` fallback when Silicone is unavailable. Local verification passed with `node --check` and targeted assertions for v2.1.40, recovery entry presence, field ids/candidates, checkbox handling, multi-scenario selection, and exact `保存并移入待发布` save target. No browser action, save, move-to-wait-publish, final publish, one-click publish, new collection, reclaim, or Product Development draft action was executed during this source update.
- Live-validated `DXM Automation V1 - NEW v2.1.39` on the first current-run 10-category product `B07M74SH62` / editId `167487782012872549` after the user installed the version. Browser action scope was declared before execution: open the exact direct edit URL, verify ASIN/editId/version, run guarded recovery with `save:false`, then execute native `保存并移入待发布` only after final preflight passed; no final publish, one-click publish, new collection, reclaim, Product Development draft handling, or duplicate recollection. `save:false` returned `preflight.pass=true`, `risks=[]`, `requiredAttributes.missing=[]`, with `Use`, `Function`, `Feature`, and `Material` skipped because they were not current-page visible red-star required fields for `笔筒(Pen Holders)`. Native `保存并移入待发布` was then clicked. Although the recovery result included residual native validation text from the edit page, the platform displayed the success modal `产品已移入待发布，请在「待发布」中查看！`; the modal was closed with `X`, not `创建新产品` or `继续编辑`. Authoritative wait-to-publish readback confirmed SKU/ASIN `B07M74SH62`, price `CNY 151.79`, stock `15`, and category `笔筒(Pen Holders)`. Evidence screenshots were saved as `runs/b07m74sh62-save-error-v2139.png` and `runs/b07m74sh62-wait-publish-readback-v2139.png`.
- Prepared `DXM Automation V1 - NEW v2.1.39` for the required-attribute false-positive clarified from the Pen Holders screenshot. Final preflight and the required-attribute pipeline now require a current-page visible red required marker before `Use`, `Function`, `Feature`, `Material`, or `Frame Material` can be treated as a required field or blocker. Hidden, absent, stale, or non-red-star fields are skipped; real native Dianxiaomi product-attribute validation still blocks save. This is a narrow preflight/readback correction only: it does not change the v2.1.38 variation Ships From / Color logic, direct-edit recovery entry, price logic, collection, claim, save, final publish, or one-click publish controls. Local verification passed with `node --check` and targeted assertions for version 2.1.39, visible-red-star helper wiring, known field lookups requiring red-star markers, optional non-red-star Material skip, and unsafe-display scanning from red-star rows only. No browser save, final publish, one-click publish, new collection, reclaim, or Product Development draft action was executed during this source update.
- Live-tested `DXM Automation V1 - NEW v2.1.38` on a current collection-box product after the user installed the version. Browser action scope was declared before execution: open `速卖通海外托管 > 采集箱`, pick a current 2026-07-03 05:57 row, open its edit page, run guarded recovery with `save:false`, and never final publish / one-click publish / Product Development draft / recollect / reclaim. Selected `4 Pack Silicone Trivets...` with ASIN `B0GKLXJCTZ` and editId `167487782012872503`; ASIN/editId/version guard passed. The recovery entry returned normally rather than hanging. The v2.1.38 second-blocker changes passed live readback: variation `发货地` selected only `美国(United States)`, Color selected `多色(MULTI)` because the source contained multiple color terms (`black`, `gray`), and variation row fields read back OK for logistics `0`, stock `15`, SKU `B0GKLXJCTZ`, weight `0.1`, size, and shipFrom `United States`. Save was not attempted because final preflight remained blocked by newly surfaced required attributes: `material`, `产品适用场景(Product application scenarios)`, and `主题(Theme)`. The task-opened collection-box/edit pages were closed through WebBridge (`closed=2`).
- Prepared `DXM Automation V1 - NEW v2.1.38` for the second native-save blocker clarified by the user: variation parameters must be filled before save. The variation parameter Ships From stage now enforces only `美国(United States)` / `United States` by checking the United States checkbox and clearing any other country checkbox in the same parameter section. The Color parameter stage now runs whenever visible Color options exist, not only after Dianxiaomi shows `请选择必选变种属性`. Color inference still uses a real single color when exactly one supported color is evident, but unclear / no single-color evidence now defaults to `多色(MULTI)` only; `拼花色(Mixed Color)` is not used as the default. Checkbox-based Color controls are kept to a single selected option. Local verification passed with `node --check` and targeted assertions for version 2.1.38, Ships From only-US enforcement, visible Color handling, unknown Color -> `多色(MULTI)`, and no legacy “variation color is not currently required” skip path. No browser save, final publish, one-click publish, new collection, reclaim, or Product Development draft action was executed during this source update.
- Rechecked the same product `B0CZ7F61XN` after the user asked to validate the v2.1.37 fix on the original failure product instead of switching products. Browser action scope was declared before execution: open/refresh the `速卖通海外托管` collection box, locate the ASIN, and if absent open the exact editId readonly; no recollection, reclaim, save, final publish, or one-click publish. The collection box showed 9 rows and `B0CZ7F61XN` was not found. Exact editId `167487782009931563` opened with v2.1.37 and ASIN guard passed. Readonly preflight confirmed the product is already in wait-to-publish edit context, fixed attributes read back as Brand `NONE(AE存量)*******(None)`, High-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, custom attributes count 0 / invalid 0, and final preflight `pass=true` / `blockers=[]`. No save, publish, or one-click publish was clicked; the task-opened collection-box and edit pages were closed through WebBridge (`closed=2`).
- Prepared `DXM Automation V1 - NEW v2.1.37` after the user confirmed the first visible required-attribute blocker should use concrete fixed page options across both first-pass new collection edits and collection-box recovery. Brand is now locked to the real Dianxiaomi option `NONE(AE存量)*******(None)` instead of generic `None / No Brand` fallback; Origin is locked to `美国(Origin)(US(Origin))` and the visible required-attribute rule no longer falls back to `China / Mainland China`; High-concerned chemical remains locked to `天然未处理(None)`. Custom attributes remain non-required and default to clear/delete/skip. Local verification passed with `node --check` and targeted assertions for version, fixed constants, no Mainland China fallback, and custom-attribute skip. Copied the script to clipboard. Live validation opened exact `B0CZ7F61XN` / editId `167487782009931563`, injected v2.1.37, and verified ASIN/editId/version. The page readback showed `速卖通海外托管 > 待发布 > 编辑`, red-star attributes OK, custom attributes count 0, final preflight `pass=true`, and no `保存并移入待发布` button because the product was already in wait-to-publish edit state. The recovery save guard therefore stopped with `saveButton.found=false`, `saved=false`, and no save/publish click; no final publish or one-click publish was executed. The task-opened page was closed through WebBridge (`closed=1`).

## 2026-07-03

- Ran the user-authorized five-minute live save attempt for the previous collection-box recovery product `B0CZ7F61XN` / editId `167487782009931563` after the user installed `DXM Automation V1 - NEW v2.1.36`. Browser action scope was declared before execution: open the exact edit page, verify ASIN/editId/version, run the recovery entry with `save:true`, click only exact native `保存并移入待发布` if final preflight passed, and never final publish or one-click publish. The page identity and version matched. The recovery entry repaired title, description, category, postage `111`, Ships From United States, trusted price `130.09`, SKU/stock/logistics/weight/size, custom attributes, Brand, Material, High-concerned chemical, and Origin; script final preflight passed with `blockers=[]` and `safeToSaveToWaitPublish=true`. The native save click was attempted once, but Dianxiaomi blocked it with additional visible validation errors, mainly `表面加工(Surface Finishing) 请选择` and `请选择必选变种属性`, plus template/responsible-party/manufacturer placeholder warnings surfaced by the page. Result: `saved=false`, `stoppedAt=save_error`, no wait-to-publish move, no final publish, and no one-click publish. The task-opened browser page was closed through WebBridge (`closed=1`). Next safe step is a narrow native-save validation repair for only truly required save blockers, starting with Surface Finishing and the required variation attribute, while continuing to skip non-required fields.
- Continued the user-authorized 10 different-category Validation live run after the user clarified the recovery rule: if the claim modal does not show `Halo Home Store`, refresh the page, reselect the current batch rows, and retry before treating the store list as blocked. After the second refresh retry, `Halo Home Store` rendered under `速卖通海外托管`; selected only that concrete store, read back `已选(1)`, and clicked claim `确定`. Claim result readback: success 7 / failed 0 / skipped duplicate 0. The `速卖通海外托管`采集箱 readback showed 10 rows total and the 7 newly claimed rows under `Halo Home Store` with creation time `2026-07-03 05:57`. Opened all 7 newly claimed edit pages with ASIN guards and current userscript injection, set the trusted ASIN/price/rate/stock task values, ran edit/preflight attempts, and did not save any product because final preflight stayed `pass=false` for all 7. Main blockers: wrong category selection to `Anti-Mosquito Incense` on some products, incomplete SKU/variation fields, missing freight template `111`, uncommitted Ships From, missing required attributes, and invalid imported custom attributes. Wait-to-publish readback on `/web/smtlocalProduct/offline` confirmed no current-run ASIN was saved; no final publish, one-click publish, Product Development, draft-box handling, duplicate recollection, or old-row reclaim was executed. Report updated: `runs/validation-10-different-categories-20260703.md`.
- Advanced the collection-box recovery entry to `DXM Automation V1 - NEW v2.1.36` for the remaining variation `size` blocker. The fix keeps the earlier required-attribute and postage recovery, narrows variation behavior so non-required color is skipped unless Dianxiaomi explicitly shows the required-variation-attribute error, and corrects variation table header mapping so data-row validation text such as `请填写包装尺寸` cannot overwrite the real `尺寸(cm)` column index. Size filling now uses a dedicated three-input lookup for `length / width / height`. Local verification passed with `node --check` and targeted assertions for version, save=false default, exact save button, size helper, header-row-only mapping, and non-required color skip. Live browser validation on B0CZ7F61XN / editId `167487782009931563` with `save:false` returned in 37.3s with `ok=true`, `stoppedAt=ready_to_save`, `reason=preflight passed; save=false`, `safeToSaveToWaitPublish=true`, `afterBlockers=[]`, and size readback `13.21 x 13.21 x 14.99`. No save, move-to-wait-publish, final publish, or one-click publish was executed; the validation page was closed through WebBridge, discarding unsaved `save:false` edits.
- Advanced the collection-box recovery entry to `DXM Automation V1 - NEW v2.1.34` for the exact B0CZ7F61XN / editId `167487782009931563` validation blocker. The recovery flow now runs fixed `High-concerned chemical` and `Origin` before the generic required-attribute pipeline, uses Ant listbox `aria-label` / `title` option matching for both multi-select and single-select controls, skips fixed fields in the later generic pass, and treats hard-timeout stages as recoverable when current DOM readback proves the field group is already OK. Local verification passed: `node --check src/dianxiaomi-automation-v1-merged-new.user.js` plus targeted assertions for version, recovery entry, `save=false`, exact `保存并移入待发布` save target, fixed-field ordering, and dropdown option lookup. Live browser validation on the exact edit page with `save:false` returned in 37.5s: high concerned chemical, origin, brand/material required attributes, postage template `111`, and custom-attribute cleanup all passed; no save/move-to-wait-publish/final publish/one-click publish was executed. The current remaining blocker is `variation required fields incomplete: size`; result stayed `saved=false` and `safeToSaveToWaitPublish=false`. The validation page was closed through WebBridge, discarding unsaved `save:false` edits.
- Started the user-authorized 10 different-category Validation live run from collection to wait-to-publish, with the no-publish/no-one-click boundary preserved. Prepared `runs/validation-10-different-categories-20260703-candidates.json` and `runs/validation-10-different-categories-20260703-asins.txt`; product risk filter passed 10/10 after replacing initial blocked/review candidates; Amazon displayed-price capture passed 10/10 and wrote trusted prices; ASIN-level category evidence passed 10/10; and `dxm-batch-pipeline plan` reported gateReady 10 / blocked 0. Live Dianxiaomi action was limited to data acquisition and claim-modal attempts: inserted 10 current Amazon links and clicked only `开始采集`. Collection completed successful 7 / failed 1 (`B09QGCKMZS`), while `B088HGQSZT` and `B0B4QZD77M` were flagged as duplicate/already collected and skipped without duplicate recollection. Selected the 7 newly collected rows while leaving old row `B07D5DN269` unselected. Batch claim was attempted twice, but the claim modal never rendered the concrete `Halo Home Store` checkbox under `速卖通海外托管`; it stayed at `render-skeleton` / `已选(0)`, so no `确定` click was executed. No claim, edit, final preflight, save-to-wait-publish, final publish, one-click publish, Product Development, or draft handling was executed. Report: `runs/validation-10-different-categories-20260703.md`.
- Advanced the collection-box recovery timeout fix to `DXM Automation V1 - NEW v2.1.21` after live validation showed `v2.1.19` still exceeded the external 90-second wait. Browser action scope was declared before execution: open/reopen the exact B0CZ edit page, inject local source, run readonly preflight, and call recovery with `save:false` only; no save, move-to-wait-publish, final publish, one-click publish, collection, reclaim, or Product Development draft action was executed. `v2.1.20` added hard bounded stage wrappers plus progress readback for category-price, required-attributes, custom-attributes, shipping-postage, and variation; live call returned in 19.7s with `stoppedAt=required-attributes`. `v2.1.21` refined required-attributes hard-timeout reporting to use the current missing field, and live call returned in 19.1s with `stoppedAt=required-attributes.high_concerned_chemical`, `reason=stage hard timeout: required-attributes.high_concerned_chemical`, `saved=false`, and `safeToSaveToWaitPublish=false`. Page readback after the failed recovery showed category and price had been filled, but High-concerned chemical and Origin remained `请选择`; the task page was then closed through WebBridge (`closed=1`), intentionally discarding unsaved `save:false` validation edits. Local verification passed: `node --check src/dianxiaomi-automation-v1-merged-new.user.js` plus assertions for version, recovery entry, save=false default, exact save button, stage progress, bounded stages, and field-level stoppedAt.
- Prepared `DXM Automation V1 - NEW v2.1.19` for the collection-box remaining-product recovery validation blocker exposed by `B0CZ7F61XN / editId 167487782009931563`. The recovery entry `window.__DXM_AUTOMATION_V1_RECOVER_TO_WAIT_PUBLISH__` now runs with bounded stage timing: remaining-field recovery receives a per-stage timeout, required-attribute dropdowns receive cooperative `deadlineAt` checks through option scanning, filtering, scrolling, commit, and readback, postage template `111` receives the same deadline handling, and fixed recovery fields `High-concerned chemical` / `Origin` each have field-level timeouts. If a stage cannot finish, the entry returns structured `timedOut`, `stoppedAt`, `reason`, `timeoutDetail`, completed stages, and readonly preflight instead of letting the browser-side call hang until an external 90-second timeout. `save` remains false by default, the save button remains exact-text `保存并移入待发布`, and no final publish / one-click publish path was added. Verification passed: `node --check src/dianxiaomi-automation-v1-merged-new.user.js` and local assertions for version, recovery entry, `save=false`, exact save button, fixed values, and timeout wiring. No browser action, Dianxiaomi field edit, save, move-to-wait-publish, final publish, one-click publish, collection, reclaim, or Product Development draft action was executed during this source update.
- Added the user-approved clean Dianxiaomi edit-page recovery flow after Validation exposed stale edit-tab binding and Chrome `离开此网站？` interruption. `tools/dxm-live-edit-helper.js` now has `forceEdit`, which opens the target direct edit URL instead of matching any old Dianxiaomi tab, accepts the leave-site dialog only when the automation is intentionally discarding/reopening the old edit page, injects the current userscript, and verifies edit id, ASIN, expected plugin version, and readonly preflight readiness before any field edit/save command is allowed. `bind --url` now also navigates to the exact requested URL instead of preferring an arbitrary active Dianxiaomi tab. Updated `docs/project-execution-rules.md` and `docs/exception-rules.md` with the fixed rule: old page first, choose `离开` only for intentional clean-page recovery, then require a 20-second clean-page readback before continuing. No browser page action, Dianxiaomi field edit, save, move-to-wait-publish, final publish, one-click publish, collection, reclaim, or Product Development draft action was executed during this source update.

## 2026-07-02

- Prepared `DXM Automation V1 - NEW v2.1.16` for the required-product-attribute blocker exposed during Validation. This is a project-level edit-page hardening, not a product-specific fix: Function / Use / Feature dynamic dropdowns now avoid Enter/Tab second confirmation after clicking a real option; final preflight scans every visible red-star product-attribute row and blocks unknown missing required fields; Function is included in required readback; and the live edit helper no longer treats a native save click as success when Dianxiaomi still shows `请选择产品属性` or visible form validation errors. No browser page, Dianxiaomi edit/save, move-to-wait-publish, final publish, one-click publish, collection, reclaim, or Product Development draft action was executed during this source update.
- Implemented the user-approved AliExpress evidence threshold/routing optimization as a local code patch. Added `config/aliexpress-evidence-thresholds.json` and `tools/aliexpress-evidence-policy.js` so the direct conditional pass line is configurable and now set to `50%` instead of the previous hardcoded `60%`. `>=80%` remains `aliexpress_verified/high_confidence`; `50%-79%` can become `conditional_verified/medium_confidence` only when usage/form consistency, no obvious conflict, and non-risk checks pass; `<50%` semantic-consistent evidence now routes through detail-page consensus and then `dxm_category_validation_required` if detail evidence is inconclusive, instead of treating the product as finally failed. Wired the new `dxm_category_validation_required` status through evidence schema, evidence batch, candidate manifest, batch execution gate, and exception queue with next action `run_dxm_readonly_category_validation`. Verification passed: syntax checks, JSON parse, policy test, synthetic 50% and <50% evidence classification, temporary batch gate plan proving `dxm_category_validation_required` blocks edit readiness and does not suggest save, and real first-batch `dxm-batch-pipeline plan` local dry-run showing total 10, gateReady 6, blocked 4 with no browser actions or writes. Existing formal evidence records were not rewritten; blocked ASINs need recapture/re-evidence to apply the new threshold. No browser action, Dianxiaomi edit/save, move-to-wait-publish, final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed.
- Implemented the low-confidence AliExpress detail-page evidence optimization discussed for faucet mat / sink splash guard products. The rule is now project-level: when search-result `postCategoryId` consensus is `<60%` but titles are semantically consistent, the capture tool can open representative AliExpress product detail pages with explicit `--detail-pages`, scroll/read the specifications, and promote the record to `detail_verified / detail_low_confidence / detail_type_consensus` when 2 detail pages show the same or equivalent `类型` / `类别` / `产品类型` / `商品类型` / `专用工具类型` / `Product Type` / `Category` / `Special Tool Type`. Added invalid navigation-value filtering (`浏览/Browse`), faucet-mat detail-type normalization (`飞溅屏幕` / semantic `专用工具`), extra candidate retry up to 5 detail links, and `aliexpress_verification_required` as a retryable control blocker for AliExpress verification/CAPTCHA pages; the automation records verification pages and continues to the next candidate when possible instead of bypassing verification or blocking the whole batch. Updated `tools/aliexpress-evidence-capture.js`, `tools/aliexpress-evidence-store.js`, `tools/aliexpress-evidence-batch.js`, `tools/aliexpress-evidence-browser-cache.js`, `tools/dxm-batch-execution-gate.js`, `tools/candidate-manifest.js`, `tools/exception-queue.js`, `config/aliexpress-evidence.schema.json`, `src/dianxiaomi-automation-v1-merged-new.user.js`, `AGENT.md`, `AGENTS.md`, `docs/category-resolution-system.md`, and `docs/exception-rules.md`. Verification passed: syntax checks, JSON parse, temporary `detail_verified` evidence write/readback, temporary gate plan that allowed B0CZ7F61XN to readonly edit preflight only, temporary `aliexpress_verification_required` gate routing, live readonly AliExpress detail capture for B0CZ7F61XN with 3 checked detail pages and 2 matched usable detail records, formal `runs/aliexpress-evidence-store.json` write/readback for B0CZ7F61XN as `detail_verified` with `dxmCandidateCategory=飞溅屏幕`, and formal gate dry-run showing `readyForEditPreflight=true` / blockers 0. No Dianxiaomi edit/save, final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed.
- Fixed a batch-gate safety issue exposed by the B0CZ readonly edit-preflight attempt. `dxm-batch-pipeline check --edit-preflight` correctly failed because the WebBridge session had no open/located edit tab (`session "dxm-batch-pipeline" has no tab`), but `mergePreflight` previously left local-ready rows unblocked and could still show `save_to_wait_publish_only_after_final_visible_confirmation`. `tools/dxm-batch-execution-gate.js` now turns missing/failed edit preflight, missing current ASIN, and current-ASIN mismatch into explicit blockers (`edit_preflight_unavailable`, `edit_preflight_current_asin_missing`, `edit_preflight_current_asin_mismatch`) with next action `open_correct_edit_page_and_rerun_readonly_preflight`. Verification rerun shows B0CZ local evidence remains ready, but final gate is blocked by `edit_preflight_unavailable`; no save suggestion remains without a real edit-page preflight. No Dianxiaomi edit/save, move-to-wait-publish, final publish, or one-click publish was executed.
- Completed the `B0CTBQCKL9` closed-loop edit-to-wait-publish validation after repairing the project-level edit-page field control code. `src/dianxiaomi-automation-v1-merged-new.user.js` now supports single-field required-attribute recovery, variation-row required-field readback, stale Ant Design dropdown filtering, Origin United States short-token selection, and visible-only native validation errors. `tools/dxm-live-edit-helper.js` now supports `requiredAttrField --field` and readonly `inspectRequiredAttr --field` for recoverable field-level debugging. Live field readback passed for ASIN guard `B0CTBQCKL9`, category `收纳架(Racks & Holders)`, postage template `111`, Ships From United States, price `64.99`, stock `15`, SKU `B0CTBQCKL9`, Brand `NONE`, Feature `Other`, High-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, and Material `塑料(Plastic)`. Final preflight passed with `pass=true` and `blockers=[]`; native `保存并移入待发布` was executed once, then wait-to-publish readback passed for SKU `B0CTBQCKL9`, price `64.99`, stock `15`, and category `收纳架(Racks & Holders)`. Added `runs/b0ct-edit-close-loop-20260702.md`. No final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed.
- Repaired the project-level live edit execution control exposed by `B0CTBQCKL9`. `src/dianxiaomi-automation-v1-merged-new.user.js` now exposes a resettable segmented remaining-field pipeline (`__DXM_AUTOMATION_V1_APPLY_REMAINING_EDIT_RULES__`) that can continue from already-filled category/price/title state and only repair missing category/price, required attributes, custom attributes, Ships From/postage 111, variation, and final preflight blockers. `tools/dxm-live-edit-helper.js` now has readonly `resolveEditId --asin`, ASIN-guarded `apply`, `resume`, segmented field commands, and `save`; when `--asin` is provided, field edits/save are refused unless the edit page readback ASIN matches. This is a project-level control fix, not a product-specific rule change. Verification passed: syntax checks for both files and helper `help` output. No browser page, Dianxiaomi field edit, save, move-to-wait-publish, final publish, one-click publish, collection, reclaim, or Product Development draft action was executed during this source repair.
- Ran the requested five-minute edit attempt for the two new ready ASINs. `B0F1DDLKBB` completed: direct edit id `167487782009931631`, final preflight `pass=true` and `blockers=[]`, native `保存并移入待发布` executed, wait-to-publish readback passed with SKU variants `B0F1DDLKBB-1` through `B0F1DDLKBB-5`, price `CNY 216.89`, stock `15`, and category `收纳架(Racks & Holders)`. `B0CTBQCKL9` did not save: the candidate note id `167487782009931667` was verified as wrong (`B01E2EYG4U`), the correct row id is `167487782009931643`, and the correct page partially filled category/title/price/Brand before WebBridge timed out in the second-half field pipeline. Final preflight still blocked postage `111`, Feature/High-concerned chemical/Origin/Material, Ships From, variation required attribute, and one overlength custom attribute, so no save was clicked. Report: `runs/five-minute-edit-two-20260702.md`. No final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed.
- Added the project-level browser task cleanup rule after the B0F1DDLKBB/B0CTBQCKL9 live-validation tab-control issue: every work task must close task-opened or task-claimed browser pages at the end, pause, or interruption; the next execution must reopen a precise direct edit URL or authoritative list page and reread ASIN/source URL/category/price/stock before acting. If unsaved fields or unconfirmed save/readback results remain, the page must not be silently closed; the run report must record the unclosed reason. No Dianxiaomi business action, save, publish, one-click publish, collect, or claim was executed for this documentation update.
- Implemented the project-level AliExpress evidence confidence gate and completed the remaining first-batch evidence pass. `tools/aliexpress-evidence-capture.js`, `tools/aliexpress-evidence-store.js`, `tools/aliexpress-evidence-batch.js`, and `config/aliexpress-evidence.schema.json` now distinguish `>=80%` high-confidence `aliexpress_verified`, `60%-79%` `conditional_verified/low_confidence`, and `<60%` semantic second-pass handling. `<60%` no longer directly means split: if titles are semantically consistent but no safe DXM category exists, the status is `semantic_consensus_needs_dxm_mapping`; if semantic consistency plus a safe DXM category exists, the status can be `conditional_verified/semantic_low_confidence`; only true semantic/category conflict remains `evidence_split`. Updated `AGENT.md`, `AGENTS.md`, `docs/category-resolution-system.md`, and `docs/exception-rules.md`. Readonly AliExpress dry-run/writeback was executed for the remaining first-batch ASINs only; no AliExpress product click, cart, order, chat, or form submit was executed. `B0CZ7F61XN` was reclassified from split to `semantic_consensus_needs_dxm_mapping` because faucet-mat titles were consistent while DXM mapping was missing. The remaining six stayed `evidence_split`: `B0DZ14L38Y` 33%, `B0F1DDLKBB` 18%, `B0CTBQCKL9` 52%, `B01E2EYG4U` 50%, `B0GFV4N3K8` 42%, and `B0C1JY1C7F` 38%. Exception queue now has `aliexpress_dxm_category_map_missing=1` and `aliexpress_category_evidence_split=6`. Reran `dxm-batch-pipeline plan`; after excluding the three already wait-publish closed-loop ASINs (`B0DXFB86J7`, `B0DPSJP47V`, `B0FH7774VK`), new ready ASINs remained 0. Added/updated `runs/remaining7-aliexpress-evidence-20260702.md`. No Dianxiaomi edit/save, final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed.
- Reviewed the first blocked semantic-consensus product `B0CZ7F61XN` before deciding whether to batch-process the remaining six. Browser action was limited to Dianxiaomi collection-box/edit-classification readonly confirmation: located the B0CZ row, opened the edit classification prompt, and attempted `编辑分类`; no field editing, save, move-to-wait-publish, final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed. The page did not confirm a safe DXM leaf. Local category data showed the existing validated `Kitchen Drains & Strainers` leaf, but that mapping is scoped to drain/strainer/sink-stopper products and rejects faucet products, so it must not be reused for faucet mats / sink splash guards. Added `postCategoryId=100003261` to `config/aliexpress-dxm-category-map.json` as `needs_review`, not `active`, with blocked wrong categories including `Kitchen Drains & Strainers`, `Kitchen Faucets`, and `Kitchen Soap Dispensers`. `B0CZ7F61XN` remains blocked as `aliexpress_dxm_category_map_missing`.
- Continued the remaining-six evidence follow-up after the user approved applying the optimized semantic rule. Browser actions were readonly AliExpress search-page navigation/readback only; no AliExpress product click, cart, order, chat, or form submit was executed, and no Dianxiaomi edit/save/move-to-wait-publish/final publish/one-click publish/Product Development draft/recollection/reclaim was executed. `B0DZ14L38Y` was reclassified from `evidence_split` to `semantic_consensus_needs_dxm_mapping` with the same faucet-mat/sink-splash-guard family as `B0CZ7F61XN`; `postCategoryId=100003261` remains `needs_review`, not active. `B0F1DDLKBB` and `B0CTBQCKL9` were reclassified to `conditional_verified / semantic_low_confidence` because readonly AliExpress titles showed bag-holder/dispenser semantic consensus and the DXM candidate `收纳架(Racks & Holders)` exists. `B01E2EYG4U`, `B0GFV4N3K8`, and `B0C1JY1C7F` were not promoted: current recapture returned category IDs without readable titles, and the furniture-pad/slider rows also lack a DXM candidate mapping. Updated `runs/aliexpress-evidence-store.json`, resolved old split exceptions for `B0DZ14L38Y`, `B0F1DDLKBB`, and `B0CTBQCKL9`, and opened a new `aliexpress_dxm_category_map_missing` exception for `B0DZ14L38Y`. Reran `dxm-batch-pipeline plan` with exchange rate 7, multiplier 1.55, stock 15, origin United States, and freight 111: gate-ready total became 5; after excluding the three already wait-publish ASINs (`B0DXFB86J7`, `B0DPSJP47V`, `B0FH7774VK`), new ready ASINs are `B0F1DDLKBB` and `B0CTBQCKL9`.
- Completed the third ready-product closed loop for `B0FH7774VK`. Readonly gate confirmation matched the current edit ASIN, trusted Amazon displayed-price store (`USD 15.58`, expected `CNY 169.04`), and AliExpress verified category evidence (`收纳架/Racks & Holders`). Initial preflight blocked save correctly before field fill. During live validation, source advanced to `DXM Automation V1 - NEW v2.1.14` because the main sequential edit-page pipeline had a PC-description filler but did not call it; the pipeline now fills PC description after title normalization and includes the result in stop/final reports. Final preflight passed with `pass=true` and `blockers=0`, including category `收纳架(Racks & Holders)`, freight template `111`, Ships From United States, price `169.04`, stock `15`, required attributes selected/read back, and custom attributes cleared to 0. Executed native `保存并移入待发布` only after final preflight passed; Dianxiaomi returned `产品已移入待发布，请在「待发布」中查看！`. Wait-to-publish readback passed for SKU `B0FH7774VK`, price `169.04`, stock `15`, category `收纳架(Racks & Holders)`. Ten-item pressure-test preparation found three closed loops complete (`B0DXFB86J7`, `B0DPSJP47V`, `B0FH7774VK`) and no remaining edit-ready ASINs in the original first batch; remaining blockers are 6 `category_evidence_missing` and 1 `aliexpress_category_evidence_split`. No final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed.
- Repaired the B0DPSJP47V edit-page field-fill control issue in `DXM Automation V1 - NEW`, advancing local source to `v2.1.13`. Root cause was confirmed live: category could be written to `收纳架(Racks & Holders)` while the category modal still blocked downstream fields; later variation handling also misreported Ships From when the global checkbox was already selected. Added standalone validation entries for category+price, shipping+postage, required attributes, and variation+final preflight. Live validation passed in four batches, then final preflight passed with 0 blockers. Executed native `保存并移入待发布` only after preflight pass; Dianxiaomi returned `产品已移入待发布，请在「待发布」中查看！`. Wait-to-publish readback for `B0DPSJP47V` passed: row present, SKU `B0DPSJP47V`, price `108.39`, stock `15`, category `收纳架(Racks & Holders)`. No final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed.
- Completed the first real small closed-loop edit-to-wait-publish validation after the batch-control gate produced one ready ASIN. Wrote and read back 10 trusted Amazon displayed-price records in `runs/amazon-price-store.json`; captured/imported 3 AliExpress evidence records in `runs/aliexpress-evidence-store.json` with 1 verified (`B0DXFB86J7`) and 2 `evidence_split`; reran the batch pipeline and generated `runs/dxm-batch-pipeline-first-batch-after-price-evidence-20260702.json` plus `.md`, yielding 1 gate-ready ASIN and 9 blocked by category evidence. Selected `B0DXFB86J7`, executed edit-page field fill and one native `保存并移入待发布` action only, then read back `/web/smtlocalProduct/offline`: row present, SKU/ASIN `B0DXFB86J7`, price `CNY 108.39`, stock `15`, category `收纳架(Racks & Holders)`, 0 field mismatches. Added `runs/first-batch-small-loop-20260702.md` as the run report. No final publish, one-click publish, Product Development draft handling, recollection, or reclaim was executed. During the run, live browser script v2.1.4 exposed stale ASIN/price panel-state risk; source script `src/dianxiaomi-automation-v1-merged-new.user.js` was fixed to v2.1.5 so current edit ASIN prioritizes the active edit page/source URL, strict price can read `dxm_amazon_price_store_v1`, and invisible locked required attributes do not block by themselves. Syntax check passed. Current expansion remains blocked until remaining first-batch ASINs pass AliExpress category evidence gates.
- Continued the small-loop validation after the first wait-to-publish pass. Readonly AliExpress evidence dry-runs produced two additional verified records and they were written to `runs/aliexpress-evidence-store.json`: `B0DPSJP47V` (`postCategoryId:100007128`, 90%, `收纳架/Racks & Holders`) and `B0FH7774VK` (`postCategoryId:200044148`, 82%, `收纳架/Racks & Holders`). Other dry-runs were not promoted: `B01E2EYG4U` remained split, `B0CTBQCKL9` split 37%, `B0F1DDLKBB` split 48%, faucet-mat ASINs `B0CZ7F61XN` / `B0DZ14L38Y` split/unmapped, and `B0C1JY1C7F` split/unmapped; `B0GFV4N3K8` remains skipped for edit because the current-batch row is missing/not claimed. Reran the pipeline into `runs/dxm-batch-pipeline-first-batch-after-more-evidence-20260702.json` and `.md`: gate ready 3 (`B0DXFB86J7`, `B0DPSJP47V`, `B0FH7774VK`), blocked 7 (`category_evidence_missing=6`, `aliexpress_category_evidence_split=1`). Attempted B0DPS readonly/edit preflight only: v2.1.4 misread the ASIN as stale B0DX, v2.1.5 exposed recursive DOM price lookup, then source advanced to v2.1.6 to break that recursion and keep price-ASIN lookup off DOM fallback construction. v2.1.6 syntax check passed and runtime preflight correctly read ASIN/evidence/price for B0DPS. Field-rule execution partially changed the page (title shortened, category selected as `收纳架`) but timed out around category-modal / variation-parameter transition; B0DPS was not saved because freight 111, required attributes, Ships From, price, and variation parameters still failed preflight. No save/move-to-wait-publish, final publish, or one-click publish was executed for B0DPS.
- Continued the first real 10-ASIN batch-control validation with readonly Amazon displayed-price dry-runs only. Generated `runs/amazon-displayed-price-dryrun-first-batch-20260702.json`, repeat report `runs/amazon-displayed-price-dryrun-first-batch-20260702-repeat.json`, and tie-break report `runs/amazon-displayed-price-dryrun-B0F1DDLKBB-20260702-third.json`. All 10 ASINs captured successfully with 0 failures, 0 price-store writes, and 0 exception-queue writes. Nine ASINs were stable across two dry-runs; `B0F1DDLKBB` read `19.99` twice from `#corePriceDisplay_desktop_feature_div` and `18.99` once from `#desktop_buybox`, so the recommended write value is `19.99` with the selector inconsistency noted. Formal `runs/amazon-price-store.json`, `runs/aliexpress-evidence-store.json`, and `runs/exception-queue.json` remain 0 business records. Stopped at the required formal price-store write boundary; no AliExpress evidence capture, Dianxiaomi collection, claim, edit, save, move to wait-to-publish, publish, one-click publish, or Product Development draft handling was executed.
- Added project-level screenshot cleanup policy and tool. `tools/cleanup-task-screenshots.js` now scans root-level screenshots and `runs/**/*.png|jpg|jpeg|webp`, detects whether they are referenced by project text files, and reports unreferenced cleanup candidates. Default mode is dry-run with `plan`; deletion requires explicit `cleanup --write`. Updated `AGENT.md`, `AGENTS.md`, `docs/project-execution-rules.md`, and `docs/current-status.md` so task screenshots are treated as temporary evidence and must be cleaned after each task unless explicitly referenced by a report/doc/JSON. Verification passed: syntax check, help output, dry-run plan, and no deletion was executed.
- Ran the project-level batch pipeline `plan` on the real 2026-07-01 first validation batch without browser access or business actions. Added `runs/validation-100-first-batch-candidates-20260701.json` as a local input snapshot reconstructed from `runs/validation-100-live-execution-20260701-claim-blocked.md`, then generated `runs/dxm-batch-pipeline-first-batch-20260702.json` and `runs/dxm-batch-pipeline-first-batch-20260702.md`. The report schema is `dxm-batch-pipeline-report-v1`; total ASINs 10, gate ready 0, blocked 10, blockers `category_evidence_missing=10` and `amazon_displayed_price_missing=10`, readonly edit preflight eligible 0. Safety readback showed `businessActions=false`, `browserActions=false`, `writesExceptionQueue=false`, and `writesPriceStore=false`; formal Amazon price store, AliExpress evidence store, and exception queue remained 0 records. Syntax checks passed for `tools/dxm-batch-pipeline.js`, `tools/candidate-manifest.js`, `tools/dxm-batch-execution-gate.js`, and `tools/exception-queue.js`.
- Completed batch pipeline orchestration step. Added `tools/dxm-batch-pipeline.js` as the project-level batch pipeline entry that combines unified candidate manifest, local execution gate, optional readonly checks, exception batch report, grouped next actions, and recommended execution order into `dxm-batch-pipeline-report-v1`. Default `plan` is local dry-run and does not open browser pages; `check` only uses browser/WebBridge when explicit readonly flags are supplied. Report writes require `--write --out`; exception queue writes, price writes, and price-capture exception writes remain separate explicit flags. Verification passed: syntax check, help output, no hardcoded task/store/ASIN constants, local fixture plan, local fixture check, Markdown output, temporary report write/readback, risk-first recommended order, and formal price/evidence/exception stores remained empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal report write, AliExpress cart/order/chat, or form submission was executed.
- Completed unified candidate manifest step. Added `tools/candidate-manifest.js` as the project-level ASIN manifest builder. It normalizes candidate CSV/JSON rows into `dxm-candidate-manifest-v1`, preserving ASIN, Amazon title/url/displayed price, product family, target/store metadata, expected price/stock/origin/freight fields, lifecycle fields, and notes. It then merges machine gates from risk filtering, Amazon displayed-price status, AliExpress evidence status, and exception-queue batch reports to produce `precheckRoutingStatus`, `autoReady`, blockers, and next action per ASIN. Default mode is dry-run; writing a manifest requires `--write --out <path>`. Verification passed: syntax check, help output, temporary price/evidence-store fixtures, dry-run JSON manifest, Markdown manifest, temporary manifest write/readback, standardized `category_evidence_missing` routing, and formal price/evidence/exception stores remained empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal manifest write, AliExpress cart/order/chat, or form submission was executed.
- Completed batch exception report step. `tools/exception-queue.js` now has a `report` command that summarizes a batch by ASIN, open exception count, primary reason, category, severity, retryability, next action, and grouped next-action ASIN lists. It supports `--asins` / `--asin-file`, `--include-resolved`, `--format json|markdown`, and optional `--out`. The exception queue write path now uses a lightweight lock around read-modify-write operations to prevent concurrent upserts from overwriting each other during parallel tool execution. Verification passed: syntax check, temporary queue fixtures for price missing / category evidence missing / risk filter / wait-publish mismatch / retryable wait-publish control issue, JSON report, Markdown report written to `/private/tmp`, concurrent temporary upsert lock test with 5 retained exceptions, and formal stores remained empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal store write, AliExpress cart/order/chat, or form submission was executed.
- Completed wait-to-publish batch readback strengthening. `tools/dxm-wait-publish-readback.js` now supports local-only `analyze` from `--readback-json` / `--readback-file`, expected row overrides from `--expected-json` / `--expected-file`, field-level checks for row presence, SKU, numeric price with tolerance, stock, and category terms, and cleaner missing-row handling that reports `wait_publish_row_missing` without cascading fake price/stock/category mismatches. The batch gate now passes readback settings into the same analyzer, and `tools/exception-queue.js` classifies `not_wait_publish_page` / `wait_publish_page_not_loaded` as retryable wait-publish readback control issues. Verification passed: syntax checks, help output, pass/mismatch/missing-row offline fixtures, temporary exception-queue write/readback for price/stock/category mismatches, retryable page-state classification, and formal price/evidence/exception stores remained empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal store write, AliExpress cart/order/chat, or form submission was executed.
- Completed product risk filtering machine-code step. Added `config/product-risk-rules.json` and `tools/product-risk-filter.js` to screen candidate records into `allow`, `needs_review`, or `blocked` using project-level risk categories: brand/logo/IP, food/ingestible, liquid/cosmetic/chemical, medical/health claim, children/toy, battery/electric, weapon/hazardous, adult/sensitive, fragile/glass, and apparel/wearable. `tools/exception-queue.js` now classifies those risk reasons plus `product_risk_record_missing`; `tools/dxm-batch-execution-gate.js` can include risk screening with `--risk-file` or `--risk-json` and prioritizes risk blockers before category/price recovery. Verification passed: syntax checks, JSON parse, safe kitchen strainer allow, food-storage-container false-positive guard, edible candy block, medical block without the old `brace`/`bra` false positive, high-risk IP/children/battery block, temporary exception-queue write/readback through the gate, hardcoded-task scan, and formal price/evidence/exception stores remained empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal store write, AliExpress cart/order/chat, or form submission was executed.
- Completed AliExpress-to-Dianxiaomi category mapping coverage hardening. `tools/aliexpress-dxm-category-map.js` now supports `review-resolvers`, `import-resolvers`, and `coverage` across resolver directories/files. `review-resolvers` reports mapping candidates, existing mapping status, split/blocked resolver rows, and a confirmation token; `import-resolvers` is dry-run by default and formal `config/aliexpress-dxm-category-map.json` writes require `--write --confirm-token <token>`; `coverage` reports whether resolver `postCategoryId` values are active/usable for automation. Resolver imports now accept both `recommended.dxmVisibleCategoryPath` and `recommended.dxmVisibleCategory`. Verification passed: syntax check, resolver review for `runs/category-resolver/20260630-remaining3-poc`, temporary map write/readback with `211114 -> 笔筒(Pen Holders)` active, coverage report with 2 usable / 1 blocked resolver mappings, formal-map missing-token rejection, current formal map unchanged at 1 mapping, and formal price/evidence/exception stores remained empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal map write, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed AliExpress evidence batch-ingestion closure by adding the `closure` command to `tools/aliexpress-evidence-batch.js`. The command can import resolver evidence in dry-run or explicit write mode, read back target ASIN status from the evidence store, report effective readiness, classify blockers (`category_evidence_missing`, `aliexpress_category_evidence_split`, `aliexpress_dxm_category_map_missing`, `aliexpress_evidence_needs_manual_review`), and output next-action ASIN lists. Also fixed resolver compatibility in `tools/aliexpress-evidence-capture.js`: `recommended.dxmVisibleCategory` is now accepted as a DXM candidate category in addition to `recommended.dxmVisibleCategoryPath`. Verification passed: syntax checks, dry-run closure for `runs/category-resolver/20260630-remaining3-poc` with 2 verified / 1 split, temporary-store `--write` closure with readback, formal-store missing-token rejection, and formal price/evidence/exception stores remained empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal evidence-store write, AliExpress product click, cart, order, chat, or form submission was executed.

## 2026-07-01

- Completed price-source step 6 by integrating Amazon displayed-price batch capture into the batch execution gate. `tools/dxm-batch-execution-gate.js plan` now reports `amazonPriceCapturePlan`, including missing-price ASINs and copyable dry-run/write commands for `tools/amazon-displayed-price-batch.js`. `check` can explicitly run the readonly batch capture with `--capture-missing-prices`; captured price writes require the separate `--write-price-captures` flag, and capture exception writes require `--write-price-capture-exceptions`. Verification passed: syntax check, local plan on empty formal stores, temporary partial-price-store plan that selected only the missing ASIN, live readonly gate-triggered price capture dry-run, temporary price-store write/readback through gate, hardcoded-task scan, and formal price/evidence/exception stores remained empty. No Dianxiaomi page action, collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal price-store write, formal exception-store write, Amazon cart, order, chat, or form submission was executed.
- Completed price-source step 5 by adding `tools/amazon-displayed-price-batch.js`, a generic batch Amazon displayed-price capture entry. It processes ASINs sequentially, reuses one browser tab by default, supports list/file slicing with `--limit` and `--start-index`, writes prices only with `--write-prices`, and writes capture failures only with `--write-exceptions`. Also hardened `tools/amazon-displayed-price-capture.js` for batch use: same-tab navigation is supported, localized Amazon USD text such as `17.99美元` / `17美元 . 99` can be parsed, and non-price contexts such as purchase counts, free-shipping thresholds, and promo credits are filtered before applying the current highest displayed-price rule. Verification passed: syntax checks, local parsing for localized price/range/free-shipping/promo cases, simulated WebBridge failure to a temporary exception queue, live readonly batch capture for one and two ASINs, temporary price-store write, hardcoded-task scan, and formal price/evidence/exception stores remained empty. No Dianxiaomi page action, collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal price-store write, formal exception-store write, Amazon cart, order, chat, or form submission was executed.
- Completed price-source step 4 by adding Amazon displayed-price capture failure classification. `tools/exception-queue.js` now classifies `amazon_page_captcha_or_robot_check`, `amazon_product_unavailable_no_displayed_price`, `amazon_price_selector_missing`, and generic Amazon price-capture failures. `tools/amazon-displayed-price-capture.js` now returns exception previews on failed captures and writes those exceptions only with explicit `--write-exceptions`; price-store writes still require separate `--write`. Verification passed: syntax checks, dry-run classification for captcha/unavailable/selector-missing, simulated WebBridge failure dry-run, simulated failure write to a temporary exception queue, successful live readonly Amazon capture remained unaffected, and formal price/evidence/exception stores remained empty. No Dianxiaomi page action, collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal price-store write, formal exception-store write, Amazon cart, order, chat, or form submission was executed.
- Completed price-source step 3 by adding `tools/amazon-displayed-price-capture.js`, a readonly Amazon displayed-price capture tool. `parse-text` locally parses single prices, `From $x.xx`, and ranges; ranges use the highest value. `capture` opens an Amazon product page through WebBridge, reads the displayed price area, records selector/text/title/evidence URL, and writes to the price store only with explicit `--write`. Verification passed: syntax check, local parsing for `$10.99`, `$8.99 - $12.99` -> `12.99`, and `From $7.99`, live readonly Amazon capture for `B0D65JFRX4`, temporary-store `--write`, price-store status formula calculation, gate integration, hardcoded task/store scan, and formal price/evidence/exception stores remained empty. Live page readback selected `#corePriceDisplay_desktop_feature_div`; the text included `$9.99` and list price `$11.99`, and per current displayed-price rule the captured value was `11.99`. No Dianxiaomi page action, collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal price-store write, Amazon cart, order, chat, or form submission was executed.
- Completed price-source field-compatibility step 2. `tools/amazon-price-store.js` now prefers `amazonDisplayedPriceUsd` and falls back to legacy `amazonOriginalPriceUsd`; normalized records and summaries output both fields so new price collectors can write the correct displayed-price name while older data keeps working. CSV import now accepts `amazonDisplayedPriceUsd`, `amazon_displayed_price_usd`, and Chinese Amazon displayed-price headers, while still accepting legacy original-price headers. `tools/exception-queue.js`, `tools/dxm-batch-execution-gate.js`, and `tools/dxm-wait-publish-readback.js` now understand `amazon_displayed_price_missing`. Verification passed: syntax checks, new-only / old-only / both-fields priority behavior, expected CNY calculation, temporary upsert, temporary CSV import/write/readback, gate integration, exception classification, and formal price/evidence/exception stores remained empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal price-store write, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed price-source terminology step 1. Current business rule now uses Amazon page displayed price instead of Amazon original price: open the Amazon product page and use the price displayed at that time; if the page displays a range such as `$8.99 - $12.99`, use the highest value. Updated current task/rule documents and tool-facing descriptions. Existing compatible storage field `amazonOriginalPriceUsd` is intentionally retained until the next field-compatibility step; business meaning is now Amazon displayed price. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, formal price-store write, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.15 by adding `tools/dxm-batch-execution-gate.js`, a generic current-batch gate/orchestrator. It combines local ASIN evidence status, trusted Amazon displayed-price/formula status, optional readonly browser evidence sync, optional readonly edit-page preflight, optional readonly wait-to-publish readback, and optional exception-queue writes into one batch report. Default mode is dry-run; exception writes require `--write-exceptions`. Verification passed: syntax check, help output, local dry-run with empty formal stores, temporary fixture where one ASIN was ready and one was blocked, temporary exception-queue write/readback with 2 blockers, formal exception queue stayed empty, and hardcoded-task scan found no task/store/ASIN constants in the new tool. No browser business action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.14 by adding a readonly wait-to-publish readback checker: `tools/dxm-wait-publish-readback.js`. The tool verifies it is on `/web/smtlocalProduct/offline`, waits for the待发布 list to load, reads target ASIN rows, and checks SKU presence, expected CNY price from the trusted Amazon price store, expected stock, and optional category terms from the AliExpress evidence store. It does not click edit rules, save, move products, publish, one-click publish, claim, collect, delete, or submit forms. `tools/exception-queue.js` now classifies wait-publish readback blockers including `wait_publish_row_missing`, `wait_publish_sku_missing`, `wait_publish_price_mismatch`, `wait_publish_stock_mismatch`, and `wait_publish_category_mismatch`. Verification passed: syntax checks, live readonly `/web/smtlocalProduct/offline` readback with `第1-26条，共 26 条记录`, temporary trusted-price check for ASIN readback, temporary exception-queue write/readback with 4 wait-publish blockers, and formal price/evidence/exception stores remained unchanged. No Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.13 by adding a trusted Amazon displayed-price store: `runs/amazon-price-store.json` and `tools/amazon-price-store.js`. The tool stores ASIN-level compatible field `amazonOriginalPriceUsd`, currency, source, evidence URL, price-range status, and timestamps; the current business meaning is Amazon page displayed price until the field migration is completed. It supports init/get/list/status/upsert/import-csv/compute. Expected CNY is computed only when runtime formula inputs are supplied through `--exchange-rate` and `--multiplier`, so task formulas are not hardcoded into the store. `tools/exception-queue.js` now supports `from-price-status`, routing compatible missing/out-of-range/formula blockers into the same machine-readable exception queue. Verification passed: syntax checks, formal empty-store status, temporary trusted-price upsert, expected CNY calculation (`9.99 x 7 x 1.55 = 108.39`), out-of-range classification for a 5-20 USD range, CSV dry-run, temporary CSV write/readback, price-status exception dry-run, temporary queue writeback, and formal price store / formal exception queue stayed empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.12 by adding machine-readable exception queue automation: `runs/exception-queue.json` and `tools/exception-queue.js`. The queue normalizes blockers into reusable categories and next actions, including category evidence missing, AliExpress evidence split, category-map missing, Amazon original price missing, price mismatch, required attributes incomplete, collection/claim missing rows, not-edit-page control state, and WebBridge/environment control exceptions. Commands support `classify`, `from-evidence-status`, `from-preflight`, `upsert`, `list`, `resolve`, and `ignore`. Verification passed: syntax check, empty formal queue listing, dry-run single classification, dry-run formal evidence-status exception generation, temporary queue write/readback from the temporary evidence store, readonly preflight JSON classification to environment-control `not_edit_page`, resolve flow on the temporary queue, and formal exception queue stayed empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.11 by hardening formal evidence-store imports in `tools/aliexpress-evidence-batch.js` with a dry-run confirmation token. Dry-run `import-resolvers` now emits `confirmation.token` and suggested write args. Writing the formal `runs/aliexpress-evidence-store.json` requires `--write --confirm-token <token>`; missing or wrong confirmation tokens are rejected before any formal write. Temporary/custom stores remain writable without confirmation for test verification. Verification passed: syntax check, formal dry-run token generation, missing-token rejection, wrong-token rejection, temporary-store write/readback with 3 records / 1 verified / 2 blockers, stable confirmation token across repeated runs despite `updatedAt` changes, and formal evidence store stayed empty. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.10 by adding `tools/aliexpress-evidence-preflight-check.js`, a closure tool for evidence-store status, browser-cache sync/readback, and readonly edit-page preflight readback. Commands: `local` checks project evidence store and optional ASIN batch status, `sync-read` writes `dxm_aliexpress_evidence_store_v1` into a Dianxiaomi browser page and reads it back, `preflight` reads `window.__DXM_AUTOMATION_V1_READONLY_PREFLIGHT__()` / DOM JSON without running edit rules, and `check` combines the stages. Also exported WebBridge sync/read helpers from `tools/aliexpress-evidence-browser-cache.js` for reuse. Verification passed: syntax checks, local formal empty-store report, temporary-store report with 1 verified / 2 blockers, WebBridge daemon connection recovery classification, elevated WebBridge `sync-read` of the formal 0-record store, readonly preflight readback on the current Dianxiaomi page, and full `check --sync --preflight` report. Current browser page is the Dianxiaomi draft page, so readonly preflight correctly blocks as `not_edit_page`; evidence store is present in browser cache with 0 records. No edit-rule click, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.9 by adding a generic batch evidence ingestion/readback tool: `tools/aliexpress-evidence-batch.js`. It supports `status` for ASIN batch evidence readback, `import-resolvers` for converting resolver JSON files into evidence records, and `summary` for store-level counts. The default behavior is dry-run; formal evidence writes require explicit `--write`. Resolver import now also benefits from the category mapping layer through `tools/aliexpress-evidence-capture.js`, so mapped `postCategoryId` values can provide `dxmCandidateCategory` without manual per-run judgment. Verification passed: syntax checks, dry-run import of the resolver directory, formal evidence store stayed at 0 records, temporary-store `--write` wrote 3 records, temporary batch status read back 1 verified / 2 blockers, and no task/store constants were found in the new batch code. No browser page action, Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.8 by adding a generic AliExpress category mapping layer. New config `config/aliexpress-dxm-category-map.json` stores active/needs-review/blocked mappings from AliExpress `postCategoryId` to Dianxiaomi visible category paths. New tool `tools/aliexpress-dxm-category-map.js` supports list/get/resolve/upsert/from-resolver, so future category-ID-to-DXM-path expansion can be done through code instead of chat-only rules. `tools/aliexpress-evidence-capture.js` now loads this map and automatically fills `dxmCandidateCategory` when the top AliExpress category has an active mapping; confidence gates remain conservative, so split evidence still stays `evidence_split` instead of becoming verified. Verification passed: syntax checks, active resolve for `postCategoryId=200231151`, missing behavior for unmapped `211114`, strong-evidence auto-fill to `aliexpress_verified`, split-evidence auto-fill while preserving `evidence_split`, temporary resolver import, temporary evidence import, and formal evidence store remained empty. No Dianxiaomi collection, claim, field edit, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.7 by adding `tools/aliexpress-evidence-capture.js`. The tool supports readonly AliExpress evidence collection and evidence-store writes: `open` opens a readonly AliExpress search page, `capture` extracts current search-page titles and `postCategoryId` signals and writes only when `--write` is passed, `from-resolver` imports existing resolver JSON, and `manual` writes a manually verified record. Safety behavior is conservative: `aliexpress_verified` requires a DXM candidate category plus strong enough evidence; split/low-confidence search signals become `evidence_split` / `needs_manual_review` rather than verified. Verification passed: syntax check, help output, temporary resolver import, temporary manual write, live readonly AliExpress search open, live readonly capture from the search page, hardcoded-task scan, formal evidence store remained empty, and network safety showed 0 `save.json` / publish / online / offline / cart / order requests. No Dianxiaomi field edits, save, wait-to-publish move, final publish, one-click publish, collection, claim, Product Development draft handling, AliExpress product click, cart, order, chat, or form submission was executed.
- Completed code-execution hardening step 3.6 by extending `tools/aliexpress-evidence-browser-cache.js` with WebBridge-backed `sync` and `read` commands. The tool now reads the project evidence store, builds the browser cache payload, finds or opens a Dianxiaomi page, writes `localStorage.dxm_aliexpress_evidence_store_v1`, and reads the browser cache summary back. Formal evidence store remains empty, so sync writes 0 records. Verification: syntax check passed, inspect returned 0 records, direct WebBridge sync wrote schema `aliexpress-evidence-store-v1` / records `0` / raw length `100`, readonly edit-page preflight changed from cache-missing to cache-ok with current ASIN blocker `category_evidence_missing`, elevated Node tool sync passed, hardcoded-task scan had no task constants, and network safety check showed 0 `save.json` / publish / online / offline requests. No Dianxiaomi field edits, save, wait-to-publish move, final publish, one-click publish, collection, claim, or Product Development draft handling was executed.
- Prepared `DXM Automation V1 - NEW v2.1.4` for code-execution hardening step 3.5. Added a readonly edit-page preflight readback entry for WebBridge verification: `window.__DXM_AUTOMATION_V1_READONLY_PREFLIGHT__()` and `#dxm-automation-v1-readonly-preflight-json`. The readback returns script version, URL, product ID, ASIN, evidence-store status, category evidence, price, category, freight template, required attributes/Origin, publish-risk controls, blockers, `preflightPass`, and `safeToSaveToWaitPublish` without running edit rules, clicking, filling fields, saving, publishing, or moving products. Verification passed with userscript syntax check and hardcoded-task scan. No Dianxiaomi collection, claim, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, or product-level publish was executed.
- Prepared and locally verified `DXM Automation V1 - NEW v2.1.3` for code-execution hardening step 3.4. Edit-page category preflight now reads the ASIN-level AliExpress evidence cache from browser localStorage key `dxm_aliexpress_evidence_store_v1`, `window.__DXM_ALIEXPRESS_EVIDENCE_STORE__`, or the DOM JSON node `#dxm-aliexpress-evidence-store-json`. Verified evidence statuses `aliexpress_verified` / `learned_rule_matched` with a non-empty `dxmCandidateCategory` are converted into Dianxiaomi category-modal search plans; existing learned rules remain fallback. If evidence is missing, unverified, or lacks a DXM candidate category, preflight reports the exact blocker before save. Added `tools/aliexpress-evidence-browser-cache.js` to export the project evidence store into the browser cache format. Verification passed: userscript syntax check, tool syntax check, formal empty-store inspect, temporary-store browser-cache export, and hardcoded-task scan. The formal evidence store is still empty, so live edit-save remains blocked until ASIN evidence records are added/synced. No Dianxiaomi collection, claim, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, or product-level publish was executed.
- Added and live-verified AliExpress entry configuration for code-execution hardening step 3.3. New config `config/aliexpress-entry.json` records the automation primary entry as `https://www.aliexpress.com/` in Google Chrome with existing login state, and records the Chrome bookmark-bar `速卖通` item as manual fallback only. New checker `tools/aliexpress-entry-check.js` validates the URL, entry mode, login-state requirement, allowed actions, forbidden actions, and failure status names. Local checks passed (`node --check`, JSON parse, config check). Live WebBridge readonly verification opened `https://www.aliexpress.com/`, read title `AliExpress - 价格实惠的中国商店 & 免运费 - 在线购物`, found search input, and found logged-in account text `您好，ae165012 帐户`. No search, image search, product click, order, cart, favorite, chat, account/payment/address action, personal-data submission, Dianxiaomi action, collection, claim, edit-save, wait-to-publish move, final publish, or one-click publish was executed.
- Added the first generic AliExpress evidence store components for code-execution hardening step 3.1 and 3.2. New schema `config/aliexpress-evidence.schema.json` defines ASIN-level evidence records with status, product family, AliExpress keyword/category/url, DXM candidate category, safe-adjacent flag, source, reason, and timestamps. New store `runs/aliexpress-evidence-store.json` starts empty. New JavaScript tool `tools/aliexpress-evidence-store.js` supports init/list/get/upsert/mark and exported helper functions for later edit-page preflight integration. Verified `node --check`, JSON parse, empty store listing, and temporary-file upsert/get with a dummy ASIN. The formal store remains empty; no AliExpress page, Dianxiaomi page, collection, claim, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, or product-level publish was executed.
- Prepared and verified `DXM Amazon Crawlbox V1 v0.1.34` as a generic current-batch row selection hardening step. The readonly status JSON now includes `currentBatchSelectionPlan` with machine-readable fields for visible/selectable current-batch rows, checked current rows, checked old/other rows, missing batch ASINs, selected-row readback, and a reason such as `current_batch_rows_not_visible`. The actual current-batch selection path now attempts to clear checked non-current rows first, selects only rows matching the active batch ASIN set, returns `selectedAsins` / `matchedAsins` / `missingAsins`, and blocks if old/other rows remain checked. No task-specific ASIN, store, count, or date was hardcoded. Syntax check passed and live readonly verification confirmed v0.1.34 injection with `canAttemptSelection=false` when no current-batch rows were visible. No collection, claim, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, or product-level publish was executed.
- Prepared and verified `DXM Amazon Crawlbox V1 v0.1.33` as a terminology cleanup for the readonly status JSON. It adds `publishControlVisible` to mean a publish-related control/text is visible and keeps legacy `publishVisible` for compatibility. This was a code naming cleanup only, not a business rule change. Syntax check passed and live readonly verification confirmed v0.1.33 injection before v0.1.34 was prepared.
- Prepared and verified `DXM Amazon Crawlbox V1 v0.1.32` for the first step of code-execution hardening. The script now exposes a readonly structured status object via `window.__DXM_AMAZON_CRAWLBOX_STATUS_READBACK__` and `#dxm-amazon-crawlbox-status-readback-json`, covering URL/page, script version, counts, auto-claim state, current-batch ASIN row readback, checked current-batch rows, checked old/other rows, and dangerous publish/product-development signals. Added `tools/dxm-status-readback.js` as a local helper; syntax checks passed for both the userscript and helper. Live readonly WebBridge readback confirmed v0.1.32 on `https://www.dianxiaomi.com/web/productCrawl/dataAcquisition`, counts `全部(6513) / 未认领(1) / 已认领(6512)`, native auto-claim closed, current-batch visible rows 0, old/other checked rows 0, and the only visible unclaimed ASIN was old row `B07D5DN269`, unchecked. No collection, claim, edit save, wait-to-publish move, final publish, one-click publish, Product Development draft handling, or product-level publish was executed. Current Codex Node runtime cannot directly access `127.0.0.1:10086` (`EPERM`), while direct shell `curl` WebBridge calls work; use direct `curl` readback in this environment.
- Continued the 100-category live Validation with WebBridge after the user requested WebBridge. Confirmed `DXM Amazon Crawlbox V1 v0.1.31` on the Dianxiaomi data acquisition page, counts `全部(6513) / 未认领(10) / 已认领(6503)`, and the current page still had exactly 9 visible current-batch unclaimed rows plus the old row `B07D5DN269`. Selected the 9 current-batch rows only; `B07D5DN269` remained unchecked. Opened batch claim, kept the modal top tab on `全部`, verified `Halo Home Store` was the concrete checkbox under the left-side `速卖通海外托管` group, did not click `全选`, read back `已选(1)`, then clicked `确定`. Dianxiaomi returned `速卖通海外托管采集认领执行完成，成功 9 条，失败 0 条，跳过重复数据 0 条`. Opened `速卖通海外托管 > 采集箱` and read back `采集箱(9)`, `发布中 (0)`, `发布失败 (0)`, and `第1-9条，共 9 条记录`. No new collection, Product Development draft handling, final publish, one-click publish, or product-level publish was executed.
- Started edit-save continuation for the first two claimed products but stopped before save because the edit-page gates correctly blocked them. `B0C1JY1C7F` / edit id `167487782009931687`: Amazon original price readback `$8.99`, expected goods value `CNY 97.54`, but script stopped at `category_evidence_missing`; visible old goods value remained `74.51`, category and freight `111` were not selected, and the product was not saved. `B01E2EYG4U` / edit id `167487782009931667`: Amazon original price readback `$19.96`, expected goods value `CNY 216.57`, but script stopped at `category_evidence_missing`; visible old goods value remained `182.78`, category and freight `111` were not selected, imported custom attributes remained invalid, and the product was not saved. Current blocker is no longer claim modal selection; it is missing per-ASIN AliExpress category evidence / learned rule availability in the edit-page automation. Run report updated.
- Prepared `DXM Amazon Crawlbox V1 v0.1.31` after the user corrected the claim-modal business flow. The previous v0.1.30 logic treated the concrete store name (`Halo Home Store`) as the primary target and then tried the top `速卖通海外托管` tab / store-name search; this was wrong. Correct flow is: keep the modal top tab on `全部`, locate the left-side `速卖通海外托管` group, tick the concrete store checkbox inside that group regardless of display name, read back `已选(1)`, then click `确定`. v0.1.31 now selects from the required group container instead of blocking on store-name mismatch, preserves wrong-channel / Product Development / Draft guards, refuses `全选` as a substitute, and blocks as `store_selection_not_read_back` if the right side remains `已选(0)` after a concrete checkbox is checked. Syntax check passed. No Dianxiaomi business page action, claim confirmation, edit, save, wait-to-publish move, final publish, one-click publish, or Product Development draft handling was executed during this source correction.
- Continued the user-authorized 100-category live Validation after the user installed `DXM Amazon Crawlbox V1 v0.1.30`. Readback confirmed the page script was v0.1.30 and the collection page showed `全部(6513)`, `未认领(10)`, `已认领(6503)`. Re-selected the same 9 current-batch rows, read back `已选中9条数据`, and confirmed the old `B07D5DN269` row stayed unchecked. The claim modal still did not render `Halo Home Store` after a 20-second wait, so the page was refreshed once per the new recovery rule. After refresh, the 9 rows were selected again and batch claim reopened; another 20-second wait, top-channel `速卖通海外托管` click, and exact `Halo Home Store` search still showed only channel headers / `全选` with `已选(0)`. Stopped without clicking `确定` to avoid wrong-store or all-store claim. Classified as `claim_blocked_store_list_unavailable_after_refresh`, a global claim-store data/render blocker. No claim confirmation, edit, save, wait-to-publish move, final publish, one-click publish, or Product Development draft handling was executed.
- Prepared `DXM Amazon Crawlbox V1 v0.1.30` after the 100-category live run showed that the claim-store modal may visually render `Halo Home Store` after a delay while WebBridge initially reads only loading placeholders. The claim pipeline now treats unreadable claim-store data as an environment/page-load condition: it waits up to 20 seconds, then refreshes the Dianxiaomi page once and resumes from current-batch claim opening. Refresh recovery only applies to data-not-readable cases such as missing configured store / no concrete overseas-managed store rows; it does not bypass multi-store uncertainty, wrong-channel selection, Product Development / Draft guards, or the requirement to read back `Halo Home Store` under `速卖通海外托管`. Syntax check passed. No new Dianxiaomi collection, claim confirmation, edit, save, wait-to-publish move, final publish, one-click publish, or Product Development draft action was executed during this source update.
- Started the user-authorized 100-category live Validation execution. Generated Amazon candidates and AliExpress evidence, then executed the first Dianxiaomi collection batch. Candidate metrics: 1512 raw Amazon records, 91 first-pass auto_ready, 44 strict auto_ready after brand/title tightening, and 44 / 44 AliExpress evidence verified. First batch attempted 10 ASINs; 9 appeared as current-batch unclaimed rows and were selected for batch claim. The pre-existing old unclaimed `B07D5DN269` row was read back unchecked and skipped. Stopped before claim confirmation because the claim-store modal never rendered concrete store checkboxes: the `速卖通海外托管` store list stayed at `render-skeleton`, `Halo Home Store` was not present in DOM/text, and `已选(0)` remained after search, clear/reselect, long wait, and reopen. Classified as `claim_blocked_store_list_skeleton` / `store_uncertain`; no claim confirmation, edit, save, wait-to-publish move, final publish, one-click publish, or Product Development draft handling was executed after the blocker. Run report: `runs/validation-100-live-execution-20260701-claim-blocked.md`.
- Recorded the confirmed 100-category stress-test parameters in `TASK.md`, `docs/validation-100-preflight-confirmation.md`, and `docs/validation-100-category-100-product-plan.md`. Scope remains Validation preparation: no Dianxiaomi page operation, collection, claim, edit, save, publish, one-click publish, Product Development draft handling, or business userscript modification was executed.
- Confirmed target business setup: `速卖通海外托管` / `Halo Home Store` / `businessLicenseGroup=A1`; objective is edit/save to wait-to-publish only.
- Confirmed sourcing and execution gates: Amazon search source, new collection and claim allowed only after live start confirmation, 100 products with one per category, Amazon original USD 5-20, price formula `original USD x 7 x 1.55`, AliExpress category evidence required, safe adjacent category allowed, pre-judgment routing first, and only `auto_ready` products proceed.
- Confirmed risk exclusions and failure handling: exclude brand/logo, food, medical, children, electric/battery, infringement high risk, missing original price, unclear main image, and over-complex variations; failures enter an exception queue with field-level reasons and no repeated deadlock.
- Completed 100-category 100-product stress-test preflight confirmation without starting real Dianxiaomi business action. Read AGENT/AGENTS, current status, development log, version history, exception rules, validation plan, execution rules, category system, field-fill skill, three-step edit skill, and category resolver JSON. Added `docs/validation-100-preflight-confirmation.md` with the business parameters that must be confirmed before execution, the 100-product list template fields, the CSV header suggestion, and a YES/NO launch checklist. No business script was modified and no Dianxiaomi page operation, collection, reclaim, edit, save, publish, one-click publish, or Product Development draft action was executed.
- Completed the requested exception-rule review and 100-category 100-product Validation planning pass without any Dianxiaomi business action. Confirmed `docs/exception-rules.md` covers dropdowns, category, native-save exposed fields, price source, and WebBridge/tab-control interruption classification. Removed current-scope Freeze / Release wording from the exception-rule closeout text and version-history rule entry, marking the work as exception-rule consolidation for Validation preparation instead. Tightened Origin: the current Amazon US -> AliExpress overseas-hosting batch must use United States / `美国(Origin)(US(Origin))` equivalent real options and must not automatically fall back to `Mainland China/中国大陆`. Added `docs/validation-100-category-100-product-plan.md` with phased execution, entry gates, stop conditions, reporting fields, and risk boundaries. No business scripts were modified and no Dianxiaomi page operation, collection, reclaim, save, publish, one-click publish, or Product Development draft action was executed.
- Exception-rule consolidation completed without any Dianxiaomi business action. Added `docs/exception-rules.md` as the unified exception-rule library and linked it from `AGENT.md`, `AGENTS.md`, `docs/project-execution-rules.md`, `docs/category-resolution-system.md`, `skills/field-fill-rule/SKILL.md`, and `skills/dxm-three-step-product-edit/SKILL.md`. Consolidated five rule families: Ant/select dropdowns require real option click and selected-label readback; AliExpress/similar-product/verified-rule evidence gates category choice while safe adjacent DXM categories are allowed when same use/form and no obvious mismatch; native save can expose additional required dropdown/checkbox/radio fields after script preflight; goods value can only come from current-task Amazon original USD x task formula; WebBridge/tab-control failures are environment control exceptions and save navigation to `/web/smtlocalProduct/offline` is a success path when authoritative readback confirms it. Updated category resolver JSON with the final B08 Makeup Organizers evidence and safe dropdown/native-save caveats. No recollection, reclaim, Product Development draft handling, save, wait-to-publish move, final publish, or one-click publish was executed during this documentation/skills-only pass.
- Completed the final collection-box recovery product `B08PB79YXV` (`167487782008788157`) to wait-to-publish with the user-installed `DXM Automation V1 V2.1.1` and a focused manual/native repair path. No recollection, reclaim, Product Development draft handling, final publish, one-click publish, or save with unresolved known blockers was executed. Category `化妆品收纳盒(Makeup Organizers)` was selected from the verified result row; the row required real coordinate double-click to write back. Script preflight then passed for category, Brand, Function, High-concerned chemical, Origin, Material, Ships From, freight `111`, goods value `CNY 72.26`, stock `15`, SKU `B08PB79YXV`, and description, but the first native save exposed additional required `用途(Use)` and `特性(Feature)` errors (`请选择产品属性`). These were repaired with real dropdown options: Use `其他(others)` and Feature `其他(Other)` because the product had no explicit BPA-free evidence and `Adjustable Size` was not appropriate for a fixed jar. The second `保存并移入待发布` succeeded by navigation to `/web/smtlocalProduct/offline`. Authoritative readback showed `采集箱(0)`, `第1-26条，共 26 条记录`, `发布中 (0)`, `发布失败 (0)`, and the B08 row in wait-to-publish with category `化妆品收纳盒(Makeup Organizers)`, CNY `72.26`, stock `15`.
- Prepared `DXM Automation V1 - NEW v2.1.2` after the B08 recovery exposed a script preflight blind spot. Use and Feature are now explicit locked required-attribute steps and readback fields; native page text `请选择产品属性` blocks preflight even if the script's mapped fields look complete. Feature candidate generation no longer treats acrylic/plastic as BPA-free evidence by itself; BPA-free is preferred only when source evidence says BPA-free, then safe visible choices such as Multiple Uses or `Other/其他` remain available. No additional Dianxiaomi page action was executed during this source update.
- Renumbered the current userscript to `DXM Automation V1 - NEW v2.1.1` per user request. This is a version-label change only; it preserves the direct recommended-option dropdown behavior prepared in the prior source update. No Dianxiaomi page action, save, wait-to-publish move, publish, one-click publish, recollection, reclaim, or Product Development draft action was executed.
- Prepared `DXM Automation V1 - NEW v1.1.105` after the user simplified the desired dropdown behavior. Function/Use/Feature required dropdowns now default to direct selection from Dianxiaomi's recommended visible options: open the field dropdown, clear any search text, read the recommended option list, and choose the safest real visible option. The script only searches/filters when no safe recommended option is visible. The v1.1.104 click-only/no Enter-Tab commit and internal-ID retry remain in place. No Dianxiaomi page action, save, wait-to-publish move, publish, one-click publish, recollection, reclaim, or Product Development draft action was executed during this source update.
- Prepared `DXM Automation V1 - NEW v1.1.104` to address the real B08 Function root cause discovered after the v1.1.103 retry. `23399620951` is a backend option value/id for a dynamic attribute option, not a valid customer-facing selected label. For Function/Use/Feature dynamic attribute dropdowns, the script now clicks a real option and commits by blur/change only, without Enter/Tab keyboard confirmation; if the field still reads back a numeric/internal ID, it clears the field and retries once with click-only commit. This is a submit-method fix, not a new candidate-word fix. No Dianxiaomi page action, save, wait-to-publish move, publish, one-click publish, recollection, reclaim, or Product Development draft action was executed during this source update.
- Retried `B08PB79YXV` with user-installed `DXM Automation V1 V1.1.103` under a 2-minute execution limit. No recollection, reclaim, Product Development draft handling, save, wait-to-publish move, final publish, or one-click publish was executed. The edit page loaded v1.1.103. Category `化妆品收纳盒(Makeup Organizers)` again required the real coordinate double-click recovery to write back. After category writeback, v1.1.103 set Brand, freight `111`, Ships From United States, Color MULTI, and supply price `CNY 72.26`, but `功能(Function)` again ended as unsafe internal display `23399620951`. A focused manual attempt to clear Function and reopen recommended options did not expose any `.ant-select-item-option`; the field still showed internal ID text. Stopped without save because preflight did not pass.
- Prepared `DXM Automation V1 - NEW v1.1.103` after the user corrected the B08 Function dropdown diagnosis with a screenshot. The issue is not necessarily that Function has no options; a typed search value such as `Storage` / `Other` can filter the dropdown to `暂无数据`. The correct recovery is to delete/clear the dropdown search text so Dianxiaomi's recommended options reappear, then choose the safest real visible option from that list. Updated the select helper so no-data after search triggers `resetDropdownSearchToRecommendedOptions()` and safe visible-option fallback before field failure. Updated `VERSION_HISTORY.md`, `docs/current-status.md`, `skills/field-fill-rule/SKILL.md`, and `skills/dxm-three-step-product-edit/SKILL.md`. No Dianxiaomi save, wait-to-publish move, publish, one-click publish, recollection, reclaim, or Product Development draft action was executed during this source update.
- Retried the remaining collection-box product `B08PB79YXV` (`167487782008788157`) after the user confirmed `DXM Automation V1 V1.1.102` was installed and limited execution to 5 minutes. No recollection, reclaim, Product Development draft handling, final publish, one-click publish, or save with failed preflight was executed. The edit page loaded v1.1.102. Category search for `化妆品收纳盒(Makeup Organizers)` initially found the correct search-result row but did not write back via DOM click; a real coordinate double-click on the search-result row successfully wrote `化妆品收纳盒(Makeup Organizers)` to the main form. Rerunning v1.1.102 then set Brand `NONE(AE存量)*******(None)`, freight template `111`, Ships From United States, Color MULTI, supply price `CNY 72.26`, and custom attributes/description checks. The run stopped before save because `功能(Function)` became an unsafe internal display `23399620951`; after clearing it, the Function dropdown rendered only `暂无数据`, and searching `Other` still produced no real option objects. This is recorded as field-level `product_attribute_dropdown_selection_failed` for Function on B08 after category writeback. The product was not saved or moved to wait-to-publish.
- Clarified the required-dropdown wording after user review: the project does not ban text input in ordinary input fields. The rule is field-type based: normal text/number fields can be typed according to their field rules, while Ant/select dropdown inputs are only search/filter boxes and must end with a real page option selected and read back. Updated `AGENT.md`, `docs/project-execution-rules.md`, `docs/current-status.md`, `skills/field-fill-rule/SKILL.md`, `skills/dxm-three-step-product-edit/SKILL.md`, and `skills/dxm-edit-page-automation/SKILL.md`. No Dianxiaomi page action was executed.
- Deposited the user-corrected required-dropdown policy into code and project memory as `DXM Automation V1 - NEW v1.1.102`. The core correction is that `Other/其他` is a valid, safe submitted option for generic required dropdowns such as `Feature`, `Function`, and `Use`; the automation should not block a product just because a more specific option exists but is hard to commit. The new generic priority is: exact obvious option from product evidence and visible options, then safe common options only when evidence supports them, then `Other/其他` as a legal fallback, while rejecting obviously wrong options such as `Adjustable Size` for fixed-size jars. Unknown required `Feature` candidate generation is now evidence-based across product types: qtip/acrylic/plastic can try `无BPA塑料(Bpa-free plastic)`, waterproof/reusable/multiple-use/adjustable candidates are added only with matching source evidence, and `Other/其他` remains the final fallback. Syntax check passed. No Dianxiaomi collection, reclaim, save, wait-to-publish move, publish, one-click publish, or Product Development draft action was executed during this deposition.
- Executed the user-authorized remaining-3 Recovery continuation from existing `速卖通海外托管 / Halo Home Store` collection-box drafts only. No recollection, reclaim, final publish, one-click publish, Product Development draft handling, or save with a known failed preflight was executed. Authoritative wait-to-publish readback at `/web/smtlocalProduct/offline` now shows `第1-25条，共 25 条记录`, `采集箱(1)`, `发布中 (0)`, and `发布失败 (0)`. `B0DQ3X91R7`, `B0DFPHVNHG`, and prior `B0B2DH7J1Y` are present in wait-to-publish; `B08PB79YXV` is not present.
- Completed `B0DQ3X91R7` (`167487782008788133`) to wait-to-publish. The category was corrected to `收纳盒和收纳箱(Storage Boxes & Bins)`, freight template `111`, goods value `CNY 72.26`, stock `15`, SKU `B0DQ3X91R7`, and Ships From United States were confirmed. Native save exposed additional required `Plastic Type`, which was repaired with a real option, and `销售方式/打包出售` was confirmed unchecked before save. Dianxiaomi returned `产品已移入待发布，请在「待发布」中查看！`; the modal was closed without clicking `创建新产品继续编辑`.
- Attempted `B08PB79YXV` (`167487782008788157`) without saving. Category `化妆品收纳盒(Makeup Organizers)` was selected and the following fields were read back: Brand `NONE`, Use `其他(others)`, Function `其他(Other)`, High-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, and Material `亚克力(Acryl)`. The product was skipped after 3 focused attempts on `特性(Feature)` because its own active dropdown did not render visible real options and JS input, keyboard accept, and CDP trusted input did not commit a selected label. Field-level failure: `product_attribute_dropdown_selection_failed`.
- Completed `B0DFPHVNHG` (`167487782008788277`) to wait-to-publish using the user-approved safe-adjacent category strategy. AliExpress evidence constrained the product family to silicone heat-resistant trivet / hot pad / pot holder mats; Dianxiaomi selected `餐垫(Placemats)` because exact `隔热垫/锅垫/Pot Holders/Trivets` leaves were not visible. Category-specific native required fields were repaired after save feedback: Theme `其他(Other)`, Product application scenarios `厨房(Kitchen)` and `餐桌用(Dining table)`, Material `其他 (自行填写)(Other)` as a real checkbox option because no Silicone option existed. PC description was rewritten with product images first, 4 images retained, about 1238 English characters, freight `111`, goods value `CNY 72.26`, stock `15`, SKU `B0DFPHVNHG`, and Ships From United States. Dianxiaomi returned `产品已移入待发布，请在「待发布」中查看！`; the modal was closed without clicking `创建新产品继续编辑`.
- Deposited reusable execution rules: native save can expose required category-specific checkbox groups after script/page preflight; checkbox-group Material fields must be handled separately from Ant select Material fields; safe adjacent DXM category selection can still require native-save repair for category attributes; B08-style stale Ant overlay options must not be used for a different field. Updated `docs/current-status.md`, `skills/dxm-three-step-product-edit/SKILL.md`, `skills/field-fill-rule/SKILL.md`, and `skills/category-resolver/learned_rules.json`. WebBridge/environment note: B08 field recovery saw stale dropdown overlays and prior evaluate hangs; these are environment/control interruptions, not product business failures. Direct edit URLs/new tabs were stable.
- Prepared `DXM Automation V1 - NEW v1.1.100` as the cleanup version after identifying that new rules were not fully replacing old execution paths. The generic select helper now delegates to the locked real-option dropdown selector instead of keeping its old keyboard/text-entry path, postage fallback no longer searches all visible page options, and generated save payloads force `packageType=0` with blank `lotNum` so old package-sale values cannot be inherited. Added an execution cleanup rule: close each product edit tab after it is saved, skipped, or blocked; do not accumulate old edit pages during batch recovery. No Dianxiaomi save, publish, one-click publish, recollection, or reclaim was executed during this source update.
- Prepared `DXM Automation V1 - NEW v1.1.99` to fix two system-level edit-page safety defects before continuing the remaining-3 recovery. Dropdown option discovery is now anchored to the current field's nearest active dropdown instead of any visible page option, and required select readback rejects pure numeric/internal-id display values or `请选择` as unsafe. The pipeline also cancels `销售方式/打包出售` if already checked and preflight blocks any checked package-sale state. No Dianxiaomi save, wait-to-publish move, publish, one-click publish, recollection, or reclaim was executed during this fix.
- Prepared `DXM Automation V1 - NEW v1.1.98` after the user corrected the remaining-3 recovery strategy. The script now treats AliExpress category evidence as a product-family constraint rather than an identical-leaf requirement: when no exact DXM leaf exists, a safe adjacent visible category is allowed. Added `silicone-trivet-placemats-adjacent-dxm` for silicone heat-resistant pot holders / trivet mats, preferring `餐垫(Placemats)` over `杯垫(Coaster)` and `茶壶底座(Teapot Trivets)` unless the product is cup-only or teapot-only. Required Ant/select dropdown handling now opens the target dropdown, reads the unfiltered real option list before typed search, supports safe visible-option fallback for `Frame Material`, `Function`, `Material`, `Use`, and `Feature`, and still requires selected-label readback. Synced active category rules into the userscript and updated `skills/category-resolver/learned_rules.json`, `skills/category-resolver/rules.json`, `skills/field-fill-rule/SKILL.md`, `skills/dxm-three-step-product-edit/SKILL.md`, `docs/category-resolution-system.md`, `docs/project-execution-rules.md`, and `docs/current-status.md`. No Dianxiaomi collection, reclaim, save, wait-to-publish move, one-click publish, final publish, or Product Development draft action was executed.
- Continued the remaining-3 recovery after confirming live scripts `DXM Automation V1 - NEW v1.1.97`, `DXM Amazon Crawlbox V1 v0.1.29`, and `save Payload V3 v0.6.3`. Authoritative wait-to-publish readback stayed at `第1-23条，共 23 条记录`, `采集箱(3)`, `发布中 (0)`, and `发布失败 (0)`. No recollection, reclaim, final publish, one-click publish, Product Development draft handling, or save with failed preflight was executed.
- Attempted `B0DQ3X91R7` from stable edit id `167487782008788133`. The v1.1.97 category rule first selected the wrong broad-search result `电池收纳盒(Battery Storage Boxes)` from `收纳盒`; the category was manually corrected to `收纳盒和收纳箱(Storage Boxes & Bins)`. Title cleanup removed `Shenee`, custom attributes were cleared, freight `111`, goods value `CNY 72.26`, and US ships-from were visible. The product was skipped because red-star `框架材质(Frame Material)` / material-family dropdown could not commit a real option: stale `塑料(Plastic)` cleared, searches for `Metal` / `金属` rendered no real option objects, and keyboard accept left the field at `请选择产品属性`.
- Attempted `B08PB79YXV` from stable edit id `167487782008788157`. The v1.1.97 category search found `化妆品收纳盒(Makeup Organizers)` but did not write it back; manual exact-result selection succeeded. Brand `NONE`, freight `111`, goods value `CNY 72.26`, and US ships-from were visible. The product was skipped because required `功能(Function)` dropdown rendered no real option objects after searching `Other`, and keyboard accept did not commit a selected label; preflight consequently still reported high-concerned chemical, Origin, and Material missing.
- Ran AliExpress category evidence for `B0DFPHVNHG` before DXM category search. Evidence was clear for silicone heat-resistant trivet mats / pot holders / hot pads. DXM searches found no exact leaf for `隔热垫`, `锅垫`, or `Pot Holders`; adjacent results were `杯垫(Coaster)`, `餐垫(Placemats)`, and `茶壶底座(Teapot Trivets)`. The product was skipped as `dxm_visible_category_not_found` after clear AliExpress evidence, rather than forcing an adjacent category.
- Deposited reusable rules: `drawer-organizer-storage-boxes-visible-dxm` must search exact `收纳盒和收纳箱` / `Storage Boxes & Bins` before broad `收纳盒`; `qtip-makeup-organizers-visible-dxm` must search `化妆品收纳盒` / `Makeup Organizers` before `棉签盒`; red-star Ant dropdowns with no rendered real option objects after focused recovery are valid `product_attribute_dropdown_selection_failed` blockers. Updated `skills/category-resolver/learned_rules.json`, `skills/dxm-three-step-product-edit/SKILL.md`, `skills/field-fill-rule/SKILL.md`, and `docs/current-status.md`.
- Continued remaining-4 recovery. `B0B2DH7J1Y` was opened from the existing Halo Home Store collection-box draft (`167487782008788033`), selected the verified tree path `家装（硬装） > 五金 > 家具五金 > 柜门消音垫(Cabinet Bumpers)`, passed edit preflight with `risks=[]`, and clicked only `保存并移入待发布`. Dianxiaomi returned the success modal `产品已移入待发布，请在「待发布」中查看！`; no final publish, one-click publish, recollection, or reclaim was executed. Key fields were Brand `NONE`, High-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, Material `塑料(Plastic)`, Ships From `美国(United States)`, freight template `111`, and goods value `CNY 72.26`.
- Prepared `DXM Automation V1 - NEW v1.1.97` after `B0DQ3X91R7` stopped at `category_evidence_missing` despite project memory containing verified category outcomes. Added active evidence-backed category resolver rules `drawer-organizer-storage-boxes-visible-dxm` and `qtip-makeup-organizers-visible-dxm` to `skills/category-resolver/learned_rules.json` and the userscript `CATEGORY_RESOLVER_RULES`. Syntax / JSON / diff whitespace checks passed, and the v1.1.97 userscript was copied to the clipboard for Tampermonkey overwrite. `B0DFPHVNHG` was not hard-coded because it still requires AliExpress category verification before DXM category search.
- Added `docs/multi-platform-listing-development-flow.md` to consolidate the multi-platform listing automation development flow, missing business scenarios, exception failure points, stage gates, and minimal refactor plan. The requested global skills `request-refactor-plan` and `grill-me` were not installed, so the document follows their intended methods using current project rules and local project skills. No Dianxiaomi business action was executed; no collection, claim, edit, save, one-click publish, or publish action was performed.
- Prepared `DXM Automation V1 - NEW v1.1.96` after reviewing the `B0DQ3X91R7` required-attribute blocker screenshot. The root issue is now encoded as a hard rule: red-star Ant/select product attributes are not complete when text is merely typed into the search input; a real dropdown option object must be clicked and a selected label/item must be read back. The script now treats locked required dropdown completion as requiring a committed option, adds explicit direct handling for `框架材质(Frame Material)` (`smtDynamicAttr906`) and `功能(Function)` (`smtDynamicAttr43`), uses metal/steel/iron/alloy candidates for metal mesh drawer organizers, and uses storage/organizer/office candidates before `Other/其他` for Function. Updated `skills/field-fill-rule/SKILL.md`, `skills/dxm-three-step-product-edit/SKILL.md`, `skills/dxm-edit-page-automation/SKILL.md`, `AGENT.md`, `docs/project-execution-rules.md`, and `docs/current-status.md`. No Dianxiaomi save, move-to-wait-publish, publish, one-click publish, recollection, or reclaim was executed.
- Prepared `DXM Automation V1 - NEW v1.1.95` to harden the category-evidence gate after reviewing the `B0DFPHVNHG` silicone pot holders failure. The previous `dxm_visible_category_not_found` classification was invalid because the record only showed Dianxiaomi search terms (`隔热垫`, `锅垫`, `Pot Holders`) and did not include an AliExpress evidence object or a matching success-verified learned rule. The script now requires learned rules to carry evidence, attaches a structured `categoryEvidence` object to category plans, blocks already-selected categories when no evidence plan exists, and reports `category_evidence_missing` / `needs_aliexpress_category_verification` instead of allowing a direct DXM-search failure. Updated `skills/category-evidence-execution/SKILL.md`, `skills/dxm-three-step-product-edit/SKILL.md`, `skills/batch-continuation-control/SKILL.md`, and `docs/current-status.md`. No Dianxiaomi business action, save, publish, recollection, or reclaim was executed.
- Corrected the `B0B2DH7J1Y` failure classification after user review and screenshot verification. The previous `number_of_pcs_option_missing_200` blocker was invalid because `Number of Pcs` has no visible red `*` in the Dianxiaomi variation-parameter section; only `发货地` is required there. Updated `skills/dxm-three-step-product-edit/SKILL.md` so variation value matching is evaluated only after confirming a visible red `*` or a native save error naming the field. Updated `docs/current-status.md`: `B0B2DH7J1Y` is now `ready_for_retry`, with `Color`, `Number of Pcs`, and `Size` to be left blank unless explicitly required by the page.
- Continued the user-authorized Recovery execution after the user confirmed Tampermonkey overwrite for `DXM Automation V1 - NEW v1.1.94`. Continued only from existing `速卖通海外托管 / Halo Home Store` drafts; no recollection, reclaim, final publish, one-click publish, Product Development draft cleanup, or new validation batch was executed. Authoritative Dianxiaomi readback at `/web/smtlocalProduct/offline` now confirms `第1-22条，共 22 条记录`, `采集箱(4)`, `发布中 (0)`, and `发布失败 (0)`. The 20-category batch result is now 16 / 20 in wait-to-publish: first-stage `B0DYZQHGM5`, `B0FWKSDJZ5`, `B0B4QZD77M`, plus recovered `B0D8W72LHR`, `B0FHPSJN7R`, `B0C6PDLYHF`, `B0C1G78CY2`, `B0DWDVSNX3`, `B0DXFJW9SJ`, `B0G1BKHG2J`, `B0FVXMVDMG`, `B0DL9VHZ14`, `B0B93HTY9Y`, `B0BJZDJ25F`, `B0D8T9TBVZ`, and `B0H25DKDPS`.
- Completed additional 17-draft recovery items in this continuation. `B0DL9VHZ14` selected `花盆托盘(Pot Trays)`, passed preflight at `CNY 64.99`, and saved. `B0DQ3X91R7` verified AliExpress desk/drawer organizer evidence and selected `收纳盒和收纳箱(Storage Boxes & Bins)`, but was skipped after 3 attempts because native attributes `框架材质(Frame Material)` and `功能(Function)` could not be truly selected even though typed text appeared. `B0B93HTY9Y` verified refrigerator/kitchen food-storage evidence, selected `食品保鲜盒(Food Storage Container)`, supplemented PC description, and saved at `CNY 130.09`. `B0BJZDJ25F` verified makeup brush storage evidence, selected `化妆刷收纳(Makeup Brush Storage)`, repaired native `规格(Specification)`, and saved at `CNY 75.62`. `B08PB79YXV` verified cotton-swab / bathroom makeup-organizer evidence, selected `化妆品收纳盒(Makeup Organizers)`, but was skipped after 3 attempts because native `用途(Use)`, `功能(Function)`, and `特性(Feature)` remained unselected. `B0D8T9TBVZ` verified toilet-paper-holder evidence, selected `纸巾架(Paper Holders)`, and saved at `CNY 101.88`. `B0H25DKDPS` verified laundry lint-catcher evidence, selected `洗衣球,洗衣片(Laundry Balls & Discs)`, and saved at `CNY 72.26` despite conservative script preflight reporting invisible `Material`; native save passed.
- Final unresolved 4 drafts are: `B0B2DH7J1Y` (`ready_for_retry`; previous `number_of_pcs_option_missing_200` was invalid because `Number of Pcs` is not red-star required), `B0DFPHVNHG` (`needs_aliexpress_category_verification`; previous `dxm_visible_category_not_found` was invalid because AliExpress evidence was not recorded), `B0DQ3X91R7` (`product_attribute_dropdown_selection_failed` for Frame Material / Function), and `B08PB79YXV` (`product_attribute_dropdown_selection_failed` for Use / Function / Feature). Highest-time-cost steps were AliExpress category evidence lookup, DXM category modal search, and native Ant dropdown recovery. WebBridge/tab-control remains a partial control risk: `find_tab` matched older edit tabs, while opening fresh direct edit URLs remained stable. Updated `docs/current-status.md` and `skills/dxm-three-step-product-edit/SKILL.md` with reusable category paths, native-dropdown failure handling, exact-result category writeback, authoritative readback requirements, and the non-red-star variation skip rule.
- Continued the user-authorized Recovery execution for the existing 17 remaining `速卖通海外托管 / Halo Home Store` drafts from the 20-category batch. No recollection, reclaim, final publish, one-click publish, Product Development draft cleanup, or new validation batch was executed. Applied the requested three-step principle: confirm root blocker, attempt the minimum fix, verify target; stopped after 3 attempts for the same blocker.
- Live attempted 3 products before stopping further business execution for a script-activation blocker. `B0B2DH7J1Y` selected verified category `柜门消音垫(Cabinet Bumpers)` by real double-click on the DXM category search result; the later user screenshot review corrected the old skip classification because `Number of Pcs` is not red-star required and should be left blank unless native save explicitly reports it. `B0FHPSJN7R` selected `桌面理线器(Cable Organizers)` and recovered Brand, high-concerned chemical, Origin, US ships-from, white Color, price `CNY 56.20`, stock `15`, and SKU `B0FHPSJN7R`, but was skipped after 3 freight-template attempts because direct `111` search returned stale/empty dropdown states. `B0D8W72LHR` selected `笔筒(Pen Holders)`, recovered required attributes, US ships-from, white Color, freight `111`, price `CNY 57.40`, stock `15`, SKU `B0D8W72LHR`, dimensions, and PC description, but 3 save attempts did not move the product to wait-to-publish because custom-attribute row validation persisted after clearing/deleting rows; final blocker `custom_attribute_validation_persisted`.
- Prepared `DXM Automation V1 - NEW v1.1.94` as the narrow source fix required before continuing the remaining 17-draft execution. Changes: source price now prioritizes current task `dxm-single-submit-default-source-price` instead of stale panel values; category plans now cover `Cable Management Clips`, cabinet bumpers, toothbrush holders, shower caddies, and kitchen rack/holder families; an already selected verified category is accepted before the `aliexpress-evidence-required` gate; custom attribute row deletion attempts increased from 3 to 8. Syntax check passed using bundled Node. The fixed userscript was copied to the clipboard for Tampermonkey overwrite.
- Authoritative wait-to-publish readback after the attempts still showed `/web/smtlocalProduct/offline` count `第1-9条，共 9 条记录`; `B0B2DH7J1Y`, `B0FHPSJN7R`, and `B0D8W72LHR` were not present. Capability deposition completed. Added `skills/dxm-three-step-product-edit/SKILL.md` with executable input/process/output/rules/efficiency notes for the confirm-fix-verify-stop workflow. Updated `skills/dxm-batch-save-recovery/SKILL.md` with freight-template full-list selection, custom-attribute row deletion, and script-version activation rules. Updated `docs/current-status.md`. Current live result remains `0 / 17` newly moved to wait-to-publish in this continuation; next step is to activate v1.1.94 and continue the same draft box, not start a new batch.

## 2026-06-30

- Executed the user-authorized 20-category / 20-product batch with Amazon original price range `USD 5-20`, final publish and one-click publish prohibited. Collection succeeded for 20 / 20 and batch claim succeeded 20 / 20 to `速卖通海外托管 / Halo Home Store`; no recollection or reclaim was performed after claim. Edit/save execution moved 3 new products to wait-to-publish: `B0DYZQHGM5` soap dish (`便携肥皂盒（非安装，非金属）`, `CNY 64.99`, stock `15`), `B0FWKSDJZ5` adhesive wall hooks (`衣帽挂钩`, `CNY 64.99`, stock `15`), and `B0B4QZD77M` sink strainer (`厨房水槽水漏、过滤网`, `CNY 63.91`, stock `15`). Wait-to-publish readback at `/web/smtlocalProduct/offline` confirmed `第1-9条，共 9 条记录` and the three new ASINs above; old 6 wait-to-publish rows remained present. The collection box now shows `速卖通海外托管 采集箱(17)`.
- The remaining 17 newly claimed drafts were not saved because the edit-page category gate blocked them before safe field completion. Sixteen returned `aliexpress-evidence-required` or equivalent category blank cascades; `B0D8W72LHR` had a specific `Pen Holders` visible category attempt but the selected text remained blank, recorded as `dxm_visible_category_selection_failed`; `B0DQ3X91R7` also had title risk `Shenee`. These were not treated as independent price/freight/material failures because category blank caused the downstream field cascade. No publish, one-click publish, Product Development draft cleanup, or old wrong-target product handling was executed.
- Execution bottlenecks and fixes learned: `B0DYZQHGM5` exposed that `销售方式 / 每包 = 0` prevents the native save from sending any request and must be fixed before save; Material AutoComplete can show a value while the userscript preflight reads it as missing, so visible field error state plus accessibility/readback must be checked; WebBridge `find_tab` can incorrectly match older edit tabs when many similar `/edit?id=` tabs exist; large batch `evaluate` payloads returned `webbridge_error` while short per-product commands worked. Added executable Skill `skills/dxm-batch-save-recovery/SKILL.md` with short-command recovery, category-first skip classification, package-count repair, success-modal handling, and authoritative wait-to-publish readback rules.
- Corrected the previous assistant-side execution summary after authoritative Dianxiaomi wait-to-publish readback. The earlier statement that the 5 remaining products did not enter wait-to-publish was wrong because it relied on the agent's own WebBridge/edit-tab operation log rather than the platform list state. Read-only verification at `https://www.dianxiaomi.com/web/smtlocalProduct/offline` confirmed `第1-6条，共 6 条记录` under `速卖通海外托管 > 待发布产品`, store `Halo Home Store`, with `采集箱(0)`, `发布中 (0)`, and `发布失败 (0)`. Confirmed rows: `B0BCQ3Y81H` category `收纳架(Racks & Holders)`, `CNY 97.43`, stock `15`; `B09ZYBQX8B` category `收纳架(Racks & Holders)`, `CNY 140.51`, stock `15`; `B0FK51H5TL` category `沐浴篮(Bath Baskets)`, `CNY 173.49`, stock `15`; `B07QQKJC1X` category `牙膏架/牙刷架(Toothbrush & Toothpaste Holders)`, `CNY 61.74`, stock `15`; `B0GDRKKWRC` category `柜门消音垫(Cabinet Bumpers)`, `CNY 107.42`, stock `15`; and `B0CNTG2PD9` category `衣帽挂钩(Coat Hooks)`, `CNY 173.49`, stock `10`. Corrected result: all 6 correctly claimed Halo Home Store products are now in wait-to-publish; the 5 remaining products were completed outside the agent's visible operation log/right-side execution surface. Updated `docs/current-status.md` and `skills/execution-efficiency-deposition/SKILL.md` so future final summaries must verify the authoritative platform list page before scoring/reporting. No final publish or one-click publish was executed.
- Added mandatory execution efficiency deposition after the user clarified the required post-task workflow. New Skill: `skills/execution-efficiency-deposition/SKILL.md`. It requires every meaningful task to record task summary, successful steps, failed steps, reusable operations, highest-time-cost step, capability extraction, Skill write/update, project memory update, and efficiency notes. For the latest 5-product recovery attempt, the highest-time-cost steps were classified as `dxm_category_modal_search_high_cost` and `field_dropdown_recovery_high_cost`: repeated Dianxiaomi category modal searches for unverified product families, repeated Ant dropdown recovery after stale/cross-field overlays, and repeated live WebBridge DOM reads. Efficiency rules now require category-search result caching by product family/search term, Skill/Rule lookup before WebBridge exploration, direct new edit tabs for unsaved-page continuation, one batched DOM extraction per product where possible, and `select_overlay_cross_field_stale` skip classification instead of repeated manual clicking. Updated `docs/current-status.md`. No Dianxiaomi collection, claim, edit-page save, wait-to-publish move, one-click publish, final publish, release, or online action was executed.
- Updated the category-decision and required-attribute fallback rules per the finalized 7-point optimization. Category direct reuse now requires a product-family category that has succeeded to wait-to-publish or listing success; all other unverified products must verify category on AliExpress before Dianxiaomi editing. Split handling now distinguishes `dxm_candidate_category_split` as a non-skip intermediate state from `aliexpress_category_evidence_split` as the only category-split skip reason. Function and Feature required fields now use exact obvious options or `Other/其他`; required Material now uses exact material, similar material, then any available material option, or direct Amazon-material input when no dropdown option exists. Updated `AGENT.md`, `TASK.md`, `skills/category-evidence-execution/SKILL.md`, `skills/batch-continuation-control/SKILL.md`, `skills/failure-recovery/SKILL.md`, `skills/field-fill-rule/SKILL.md`, `skills/dxm-edit-page-automation/SKILL.md`, `docs/current-status.md`, and this log. No Dianxiaomi business action was executed.
- Updated the current field-fill rules after user clarification on required `Material`: when Material has a red `*`, it must not be blocked just because the exact Amazon material is missing from the Dianxiaomi dropdown. The execution rule is now exact material match first, similar material fallback second, and direct Amazon-material text input when the dropdown has no options but the field accepts input. Only if both selection and input fail should the product be skipped as `material_required_unfillable`. Updated `AGENT.md`, `TASK.md`, `skills/field-fill-rule/SKILL.md`, `skills/dxm-edit-page-automation/SKILL.md`, `docs/current-status.md`, and this log. No Dianxiaomi business action was executed.
- Established `AGENT.md` as the project business execution truth source after the recovery-batch failure review. Future execution must read `AGENT.md` first, then `TASK.md`, relevant `skills/*/SKILL.md`, `docs/current-status.md`, and `DEVELOPMENT_LOG.md`; chat history is not allowed to be the only source of execution rules.
- Added `TASK.md` for the current 10-category recovery batch. It records the 4 wrong-target products that must not be handled without explicit authorization, the 6 correctly claimed Halo Home Store draft products, the current wait-to-publish result, forbidden actions, required read order, field rules, and product-level skip reasons.
- Added executable Skill `skills/price-processing/SKILL.md` with the fixed goods-value formula `Amazon original USD x 7 x 1.55`, 2-decimal rounding, and skip reason `amazon_original_price_missing`.
- Added executable Skill `skills/field-fill-rule/SKILL.md` with current edit-page rules: fill only red-star required product attributes; clear custom attributes; click marketing image `一键生成`; select only required Ships From as United States; leave `销售方式/打包出售` unchecked; set stock to `15`; set SKU to ASIN; select freight template `111`.
- Added executable Skill `skills/failure-recovery/SKILL.md` to classify product-level failures, allow only one focused correction cycle, continue the batch on field/category failures, and stop browser operations only on unsafe WebBridge/tab-control interruption.
- Updated `skills/dxm-edit-page-automation/SKILL.md` to remove the incorrect blockers for non-required `is_customized` and non-red-star `Material`, add the marketing image generation requirement, and enforce stock `15`.
- Updated `skills/batch-continuation-control/SKILL.md` to require reading `AGENT.md` / `TASK.md` first and to apply the new field-fill skill before visible preflight.
- Updated `AGENTS.md` and `docs/current-status.md` so future agents do not rely on stale chat memory. No Dianxiaomi business action was executed: no collection, claim, edit-page save, wait-to-publish move, one-click publish, or final publish.

- Continued the 10-different-category Recovery batch from the 5 remaining `速卖通海外托管 / Halo Home Store` draft edit pages only. No recollection, reclaim, Product Development draft cleanup, one-click publish, final publish, release, or online action was executed. This continuation moved 0 additional products to wait-to-publish; the batch total remains 1 / 6 completed (`B0CNTG2PD9`). `B0GDRKKWRC` selected `柜门消音垫(Cabinet Bumpers)` and partially corrected price/attributes, but was skipped because `is_customized` returned no visible options, variation and freight remained unresolved, and Ant select overlays crossed fields after the timed-out edit pipeline. `B07QQKJC1X` selected `牙膏架/牙刷架(Toothbrush & Toothpaste Holders)` and set price `61.74`, High-concerned chemical, and Origin, but was skipped because Brand and Material dropdowns returned no safe selectable options. `B0FK51H5TL` was skipped as `category_evidence_split` between `Bathroom Storage & Organization / Storage Shelves & Racks` and `Bathroom Hardware / Bathroom Shelves`. `B09ZYBQX8B` selected `厨房收纳用品 / 收纳架(Racks & Holders)` and set price `140.51`, Function, High-concerned chemical, Origin, Feature, and Material, but was skipped because Brand did not expose `NONE/None` even after sync and the select overlay crossed into Material options. `B0BCQ3Y81H` was skipped as `category_evidence_split` because kitchen countertop paper towel holder mapped only to bathroom `纸巾架(Paper Holders)`, kitchen `纸巾盒(Tissue Boxes)`, or non-matching toilet-paper paths. Recovery learned operations were deposited into `skills/dxm-edit-page-automation/SKILL.md`: use a new edit tab when leaving an unsaved edit page, restore scroll functions from a temporary iframe after timed-out rules, hide stale category modal overlays only after category writeback, treat stale Ant dropdown cross-field options as field-level skip reasons, and continue the batch instead of waiting for manual intervention.
- Prepared `DXM Automation V1 - NEW v1.1.93` after the user corrected the category-evidence workflow. Clarified that rule deposition is an after-success reuse step, not a pre-execution human gate: unknown product families must trigger AliExpress image-search / similar-product / structured category evidence lookup, then search and select the closest visible Dianxiaomi category. If AliExpress evidence is split, Dianxiaomi visible category cannot be found, or preflight fields cannot be auto-filled, the product is recorded with a field-level skip reason and the batch continues to the next item. Logo / brand image risk is treated as an Amazon collection-stage prefilter and should not become an edit-stage manual wait reason. Upgraded `bumpers-200291142` into an active visible-category rule with the DXM path `家装（硬装） > 五金 > 家具五金 > 柜门消音垫(Cabinet Bumpers)` and synced the rule into the v1.1.93 userscript. No Dianxiaomi collection, claim, edit-page save, wait-to-publish move, one-click publish, final publish, release, or online action was executed.
- Completed capability deposition for this correction. Added executable Skills: `skills/category-evidence-execution/SKILL.md` for learned-rule-first then AliExpress evidence lookup and DXM visible category selection; `skills/batch-continuation-control/SKILL.md` for record-and-continue batch behavior; `skills/capability-deposition/SKILL.md` for mandatory post-task summary, capability extraction, Skill write/update, and project memory updates. Updated `skills/dxm-edit-page-automation/SKILL.md` so preflight fields that cannot be auto-filled, split category evidence, and missing DXM visible categories are recorded and skipped without waiting for manual intervention. Updated `docs/category-resolution-system.md`, `docs/project-execution-rules.md`, and `docs/current-status.md`.
- Continued the interrupted 10-different-category batch from the existing `速卖通海外托管 / Halo Home Store` draft box only. Opened `https://www.dianxiaomi.com/web/smtlocalProduct/draft` directly and confirmed the correct recovery state: `采集箱(6)`, DXM Automation V1 V1.1.92, DXM Amazon Crawlbox V1 v0.1.29, and save Payload V3 0.6.3. No recollection, reclaim, final publish, one-click publish, release, or online action was executed, and the 4 Product Development / Draft mis-claims were not touched. Recovered batch Amazon source prices from `dxm_amazon_crawlbox_public_batch_v1`.
- Saved `B0CNTG2PD9` (`167487782008416285`) from Halo Home Store draft to wait-to-publish. The page rule selected `衣帽挂钩(Coat Hooks)`, set Brand `NONE`, high-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, Material `ABS(ABS)`, Ships From `United States`, freight template `111`, and price `15.99 x 7 x 1.55 = CNY 173.49`. The first preflight was blocked only by `PC description too short: 483`, so the PC description was extended with neutral non-brand text and preflight then passed. The first save attempt exposed Dianxiaomi visible validation `请输入2~100000之间的数值` because `打包出售` was checked for a single-piece listing; it was unchecked. A second save attempt exposed `挂钩数(No. of Hooks) 请选择`; the hidden attribute section was expanded and `4个(4)` was selected for the 4-pack product. The final save showed `产品已移入待发布`; the success popup was closed with `X`, not `创建新产品继续编辑`. Wait-to-publish readback confirmed `B0CNTG2PD9`, category `衣帽挂钩(Coat Hooks)`, price `CNY 173.49`, stock `10`.
- Attempted `B0GDRKKWRC` (`167487782008416291`) but did not save. Although the local learned rule `bumpers-200291142` has historical categoryId / save-payload evidence, v1.1.92 requires an active visible category modal plan and blocked automatic category selection as `aliexpress-evidence-required`. A manual visible category search found and selected `家装（硬装） > 五金 > 家具五金 > 柜门消音垫(Cabinet Bumpers)`, but the current rule gate still did not treat this as active evidence. Preflight remained blocked by missing freight template `111`, incomplete required attributes (`brand`, `high_concerned_chemical`, `origin`, `material`), Ships From not accepted, price mismatch expected `107.42` vs visible `60.44`, missing variation attribute, and invalid custom attributes. The remaining draft rows after this run are `B0GDRKKWRC`, `B07QQKJC1X`, `B0FK51H5TL`, `B09ZYBQX8B`, and `B0BCQ3Y81H`.
- Classified the remaining unprocessed product families `toothbrush holder bathroom`, `shower caddy bathroom`, `kitchen utensil holder`, and `paper towel holder countertop` as blocked before save by the v1.1.92 category-evidence-first gate because `skills/category-resolver/learned_rules.json` has no active AliExpress / DXM-visible category evidence rules for those families. The current list state is draft `采集箱(5)` and wait-to-publish contains the newly saved `B0CNTG2PD9`; `发布中 (0)` and `发布失败 (0)` remained visible. No WebBridge / tab-control interruption occurred; direct `/web/smtlocalProduct/edit?id=...` recovery was stable.
- Prepared `DXM Amazon Crawlbox V1 v0.1.29` after the user clarified that the Dianxiaomi claim modal channel must be a hard rule: every claim must select a store under `速卖通海外托管`, regardless of source link or target store, and `产品开发 / 草稿箱` must never be accepted. Root cause of the 4-product mis-claim was the old store-selection code finding the `速卖通海外托管` text but sometimes climbing to a broad modal container, allowing `产品开发 / 草稿箱` checkboxes to enter the candidate set or remain selected by default. v0.1.29 now scopes candidates by DOM order: only checkboxes after the `速卖通海外托管` header and before the next channel header are valid, wrong checked targets are unselected, and confirmation blocks with `store_selection_wrong_target` if the final target is not under `速卖通海外托管`. Updated execution, crawlbox, install, and agent rules. No Dianxiaomi collection, claim, edit, save, one-click publish, final publish, release, or online action was executed during this source/rule update.
- Started the user-authorized 10-different-category execution toward wait-to-publish with final publish and one-click publish still prohibited. Prepared 10 Amazon candidates with Amazon original USD prices. Four products (`B0D141F3F5`, `B0BZCVQ77H`, `B0CY9FWV19`, `B0D2WCNK3K`) were collected but accidentally claimed to Product Development / Draft because the native claim modal selected `草稿箱`; they are marked failed for this batch as `store_selection_wrong_target` and were not edited. Six products (`B0CNTG2PD9`, `B0GDRKKWRC`, `B07QQKJC1X`, `B0FK51H5TL`, `B09ZYBQX8B`, `B0BCQ3Y81H`) were then collected and correctly claimed to `速卖通海外托管 / Halo Home Store`; the success modal reported `速卖通海外托管采集认领执行完成，成功 6 条，失败 0 条`, and the draft list showed `采集箱(6)`. Editing did not start because WebBridge could not reliably focus the correct draft tab after old sample edit tabs entered the same session, row-level `编辑` did not open via DOM/CDP click, Computer Use focused an older `/web/smtlocalProduct/offline` tab, and subsequent WebBridge tab close/focus commands timed out. This is classified as a WebBridge / tab-control interruption, not an edit/save business failure. No save to wait-to-publish, final publish, one-click publish, release, or online action was executed.
- Documented the business-license-group deduplication rule after the user clarified the actual store structure: one business license currently maps to about 6 stores, and the current validation is only one store under that license. Deduplication is now defined at `businessLicenseGroup + productKey` scope, not global ASIN scope. The same product must not be recollected or reclaimed by another store under the same business-license group, but different business-license groups may intentionally reuse the same product, especially good-selling or high-potential products, as long as Logo/brand, category, attributes, price, preflight, and save gates still pass. Added `docs/store-license-dedup-rules.md` as the knowledge base and updated project execution / crawlbox / agent rules. Code-level license-group registry and dedup ledger remain pending; until implemented, each new task must explicitly state `businessLicenseGroup` and `targetStore`.
- Prepared `DXM Automation V1 - NEW v1.1.92` after the user corrected the project direction: the next batch must not continue the old DeepSeek/local-keyword-first category flow. v1.1.92 makes AliExpress / similar-product / DXM-visible category evidence the edit-page category gate. The sync tool now includes active visible category rules from `skills/category-resolver/learned_rules.json` even when they do not have a numeric categoryId, so evidence rules such as `Pen Holders`, `Cable Organizers`, `Portable Soap Dishes`, `Coat Hooks`, and `Kitchen Drains & Strainers` are present in the userscript. Edit-page category modal plans are now generated from these verified evidence rules first. If no AliExpress/DXM evidence rule exists, category selection blocks with `aliexpress-evidence-required`; the old hardcoded keyword fallback is retained only as disabled diagnostic/reference code. Updated project rules to state that DeepSeek is a product-understanding layer only, local keyword rules cannot auto-save, and the next 10-different-category batch must run "category evidence first, then Dianxiaomi filling." No Dianxiaomi business action, collection, claim, save, one-click-publish, final publish, release, or online action was executed during this source update.
- Continued from the post-save modal workflow after the user clarified that the `产品已移入待发布` popup should be closed with the `X` instead of choosing `创建新产品继续编辑`. Re-opened the remaining draft product `B0CNSYPZBQ` without recollecting, publishing, one-click-publishing, releasing, or upgrading the version. The first v1.1.91 rule pass reconfirmed the old blocker: the adhesive cable clip plan still ended at `线缆工具(Cable Tools)` and blocked preflight. A no-code edit-page recovery then selected the stronger visible Dianxiaomi leaf `办公、文化及教育用品(Office & School Supplies) > 桌上收纳用品(Desk Accessories & Organizer) > 桌面理线器(Cable Organizers)`. After re-running the edit-page rules with Amazon source price `$4.99`, v1.1.91 handled Brand `NONE`, high-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, Ships From `美国(United States)`, Color `多色(MULTI)`, freight template `111`, custom attributes cleared to 0, and price `4.99 × 7 × 1.55 = 54.14`. The plugin preflight still flagged a conservative `Material` missing risk, but the selected category did not expose a visible Material field; one native `保存并移入待发布` attempt succeeded. Dianxiaomi showed `产品已移入待发布，请在「待发布」中查看！`, and the success popup was closed with `X`. Wait-to-publish readback confirmed `B0CNSYPZBQ`, category `桌面理线器(Cable Organizers)`, price `CNY 54.14`, stock `10`. The collection box now shows `采集箱(0)` and wait-to-publish shows 5 records. Existing old abnormal prices on `B09PFW8WRQ` and `B0BPS66NC3` were still not corrected per user instruction; they remain blocked from publish until strict Amazon-source price correction.
- Continued the remaining draft execution with the already-installed `DXM Automation V1 V1.1.91` after the user explicitly asked to stop correcting existing wait-to-publish prices for now. No source change, version upgrade, new collection, one-click-publish, final publish, release, or online action was executed. Starting state for this continuation: wait-to-publish had 3 products (`B0D65JFRX4`, `B09PFW8WRQ`, `B0BPS66NC3`), and the draft/collection box had 2 remaining products. `B0C9ZHWC9K` was resumed from the interrupted edit-page state: Amazon read-only current price was `$9.98`, task formula produced `9.98 × 7 × 1.55 = 108.28`, edit-page preflight passed with category `笔筒(Pen Holders)`, Brand `NONE`, high-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, Material `塑料(Plastic)`, freight template `111`, visible price `108.28`, and `risks=[]`. The first DOM click on `保存并移入待发布` did not trigger save; this was classified as a page-control issue, then recovered with an ordered real browser coordinate click on the same button. Dianxiaomi showed `产品已移入待发布，请在「待发布」中查看！`. Wait-to-publish readback confirmed `B0C9ZHWC9K` with category `笔筒(Pen Holders)`, supply price `CNY 108.28`, stock `100`, and total wait-to-publish count `4`. Draft readback confirmed only one product remains in the collection box: the cable holder / cable clip product corresponding to `B0CNSYPZBQ`. `B0CNSYPZBQ` was initially skipped, not saved, because its category plan still ended at non-leaf / wrong-family `线缆工具(Cable Tools)` and preflight also reported freight template missing, required attributes incomplete, variation blocked, visible price mismatch, and over-length custom attributes; it was later recovered through the no-code `桌面理线器(Cable Organizers)` path above. Publish-related list state remained `发布中 (0)` and `发布失败 (0)`.
- Live-validated `DXM Automation V1 V1.1.91` after user Tampermonkey overwrite and corrected the existing wait-to-publish price for `B0D65JFRX4` (`167487782006920301`). First validation with empty `Amazon 原价 USD` correctly blocked preflight: `price invalid: 缺少Amazon 原价 USD`, did not reuse the visible wrong price `1913.61`, and listed blocked sources (`dianxiaomi_edit_page_price`, `minPrice`, `maxPrice`, `ui_display_price`, `page_cache_price`, `manual_supply_price_override`). A read-only Amazon page check found current buy-box price `$9.99` and list price `$11.99`; for the task formula, current buy-box price `$9.99` was used as the Amazon source price. v1.1.91 then calculated `9.99 × 7 × 1.55 = 108.39`, wrote visible goods value `108.39`, and preflight passed with `risks=[]`. Saving was initially blocked by a separate visible Dianxiaomi validation: `销售方式：打包出售` was checked while `每包=0`, producing `请输入2~100000之间的数值`; the product is a single-piece listing, so `打包出售` was unchecked. One native save was completed afterward; Dianxiaomi showed `您的产品编辑成功！`. Wait-to-publish readback confirmed `B0D65JFRX4` now shows `CNY 108.39`, category `厨房水槽水漏、过滤网(Kitchen Drains & Strainers)`, Ships From `美国`, SKU `B0D65JFRX4`, stock `15`. No final publish, one-click-publish, release, or online action was executed. Remaining risk observed but not expanded in this step: other wait-to-publish rows still show abnormal old prices (`B09PFW8WRQ` `CNY 2060.85`, `B0BPS66NC3` `CNY 77927.63`) and need separate strict-price correction before any publish.
- Prepared `DXM Automation V1 - NEW v1.1.91` for the price-source correction after `B0D65JFRX4` was read back in wait-to-publish with wrong supply price `CNY 1913.61`. Root cause classification: the old price path could use non-Amazon edit/cache values such as `product.price`, `minPrice`, `maxPrice`, cached batch price, visible UI numbers, or manual CNY override as the source price, then multiply by the task formula. v1.1.91 makes the only accepted source `Amazon 原价 USD` from the current task input and calculates supply price as `Amazon 原价 × 任务汇率 × 任务倍率`. It removes fallback tier formulas, disables the manual CNY supply-price override, stops DOM numeric scans from creating prices, clears snapshot `minPrice/maxPrice`, and adds both payload and visible edit-page preflight gates: missing trusted Amazon original price or visible/SKU price mismatch blocks save. Syntax check passed with bundled Node, strict price-source checks passed, and `git diff --check` passed. No Dianxiaomi page action, save, move-to-wait-publish, one-click-publish, publish, or new collection was executed in this source update.
- Saved `B0D65JFRX4` (`167487782006920301`) to wait-to-publish after user authorized the next step. Before saving, the current edit page was rechecked: live version `DXM Automation V1 V1.1.90`, `preflight.pass=true`, and `risks=[]`. One native `保存并移入待发布` action was executed. The first DOM click did not produce a network request or feedback, so it was classified as a page-control click issue rather than a business failure; after scrolling the native bottom button into view and issuing a real coordinate click, Dianxiaomi showed the success modal `产品已移入待发布，请在「待发布」中查看！`. Wait-to-publish readback at `https://www.dianxiaomi.com/web/smtlocalProduct/offline` confirmed the row: title `Sink Drain Strainer Sink Stopper Kitchen Drain, 3 in 1 Kitchen Sink Drain`, store `Halo Home Store`, Ships From `美国`, category `厨房水槽水漏、过滤网(Kitchen Drains & Strainers)`, SKU `B0D65JFRX4`, supply price `CNY 1913.61`, stock `15`. The wait-to-publish list now shows 3 records. No final publish, one-click-publish, release, or online request was observed.
- Live-validated `DXM Automation V1 V1.1.90` on `B0D65JFRX4` (`167487782006920301`) after user Tampermonkey overwrite, following the project three-step rule: (1) confirmed the v1.1.89 blocker was folded/hidden Material (`.smtDynamicAttr10` behind `+展开`), (2) applied the narrow v1.1.90 fix to expand attributes before Material selection and prioritize stainless/metal values, and (3) verified the original target. One `应用编辑页规则` click was executed. Final result: category `厨房水槽水漏、过滤网(Kitchen Drains & Strainers)`, Brand `NONE(AE存量)*******(None)`, High-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, Material `不锈钢(Stainless steel)`, Ships From `美国(United States)`, Color `多色(MULTI)`, freight template `111`, custom attributes count `0`, and `preflight.pass=true` with `risks=[]`. Network capture showed 0 matching save/publish requests (`save.json|publish|release|oneClick|one-click|online|submit`). No save, move-to-wait-publish, one-click-publish, publish, or new collection action was executed. The page is ready for a separate explicit save authorization if the user wants this product moved to wait-to-publish.
- Prepared `DXM Automation V1 - NEW v1.1.90` after live v1.1.89 validation isolated the only remaining `B0D65JFRX4` blocker to `Material`. Read-only DOM diagnosis confirmed `.smtDynamicAttr10` exists but is hidden with a `0x0` rect behind the product-attribute `+展开` control, so v1.1.90 expands the attribute section before locating/selecting `Material`. Kitchen sink strainer material candidates now prioritize `不锈钢(Stainless Steel)` / metal values. Category selection, freight, title, custom attributes, save, move-to-wait-publish, one-click-publish, and publish logic are unchanged. Syntax check passed with bundled Node and `git diff --check` passed.
- Live-validated `DXM Automation V1 V1.1.89` on `B0D65JFRX4` (`167487782006920301`) after user Tampermonkey overwrite. One `应用编辑页规则` click was executed. Category succeeded with `modal-search-plan-search-result`, searching `水槽` and selecting the full path `家装（硬装）(Home Improvement)/厨房设施(Kitchen Fixture)/厨房水槽配件(Kitchen Sink Accessories)/厨房水槽水漏、过滤网(Kitchen Drains & Strainers)`. The old wrong category `商用餐厨操作类电器（带电）` did not appear. Brand, high-concerned chemical, origin, Ships From, Color, freight `111`, custom attributes, and title sanitation were handled, but preflight remained blocked only by `required attributes incomplete: material`. Network capture showed 0 matching save/publish requests (`save.json|publish|release|oneClick|one-click|online|submit`); no save, move-to-wait-publish, one-click-publish, publish, or new collection action was executed.
- Prepared `DXM Automation V1 - NEW v1.1.89` as the first narrow AliExpress Category Resolver execution-layer integration. The validated AliExpress `postCategoryId=200231151` signal for `B0D65JFRX4` was promoted into `skills/category-resolver/learned_rules.json` with the Dianxiaomi visible path `家装（硬装）(Home Improvement)/厨房设施(Kitchen Fixture)/厨房水槽配件(Kitchen Sink Accessories)/厨房水槽水漏、过滤网(Kitchen Drains & Strainers)`. The edit-page category modal plan for kitchen sink strainers now searches stronger target terms (`水槽`, `Sink`, `Drain`, `Strainer`, `滤网`) and accepts the exact `厨房水槽水漏、过滤网 / Kitchen Drains & Strainers` leaf while continuing to reject faucet, bathroom, soap dish, commercial kitchen appliance, and electric appliance paths. This is category-selection-only integration: save/publish logic is unchanged, auto-save remains gated by edit-page preflight, and cable clips remain blocked from auto-save because AliExpress category evidence is still split. Syntax check passed with bundled Node, JSON validation passed for category rules, and `git diff --check` passed for touched files. Tampermonkey overwrite and live validation are pending.
- Mapped AliExpress Category Resolver output into a Dianxiaomi-visible category path for `B0D65JFRX4` without selecting/saving a product. On the edit page category modal, read-only searches for `水槽`, `过滤`, `滤网`, `Sink`, `Drain`, and `Strainer` all exposed the path `家装（硬装）(Home Improvement)/厨房设施(Kitchen Fixture)/厨房水槽配件(Kitchen Sink Accessories)/厨房水槽水漏、过滤网(Kitchen Drains & Strainers)`, which matches the historical Dianxiaomi category API evidence for `categoryId=200231151`. The modal was closed afterward. No Dianxiaomi save, move-to-wait-publish, one-click-publish, publish, or new collection action was executed.
- Ran a read-only AliExpress Category Resolver PoC for the 3 remaining Dianxiaomi draft products after user logged into AliExpress. No Dianxiaomi edit, save, move-to-wait-publish, one-click-publish, publish, or new collection action was executed. AliExpress desktop did not expose a stable image-search button in the current session, and opening an item detail page triggered a CAPTCHA/punish page, so no CAPTCHA bypass was attempted. The PoC instead extracted AliExpress search-result structured data (`postCategoryId`) for category consensus. Results: `B0D65JFRX4` sink strainer produced a strong AliExpress category signal with `postCategoryId=200231151` in 11/20 top results, confirming that the prior Dianxiaomi `商用餐厨操作类电器（带电）` category is wrong; `B0C9ZHWC9K` pen organizer produced a moderate signal with `postCategoryId=211114` in 8/20 top results and confirms `笔筒(Pen Holders)` is directionally correct, leaving Material field/readback as the blocker; `B0CNSYPZBQ` cable clips produced semantically relevant cable-management results but category IDs were split (`201206404` 7/20, `14191201` 6/20, others mixed), so it should remain low/medium confidence until category-ID-to-path mapping and stricter similarity ranking exist. Report: `runs/category-resolver/20260630-remaining3-poc/summary.md`.
- Live-verified `DXM Automation V1 V1.1.88` on the 3 remaining overseas-managed draft products after user Tampermonkey overwrite. The draft box showed `B0CNSYPZBQ`, `B0D65JFRX4`, and `B0C9ZHWC9K` before and after execution; previously saved `B09PFW8WRQ` was not reprocessed. Each remaining product was opened in an edit page and `应用编辑页规则` was clicked once. No product passed preflight, so no native save button was clicked and no publish / one-click-publish request was observed. Results: `B0CNSYPZBQ` improved from the prior wrong conductive-glue category but still failed category matching, ending at `线缆工具(Cable Tools)` with freight, required attributes, variation, and custom attributes incomplete; `B0D65JFRX4` still failed category matching at `商用餐厨操作类电器（带电）(Commercial Kitchen Operation Appliances)` with freight, required attributes, and variation incomplete; `B0C9ZHWC9K` no longer had the `Rotating` title-risk false positive and kept category `笔筒(Pen Holders)` plus freight `111`, but preflight still failed only on required attribute `Material` because the material field container was not visible for the selected category. This validates v1.1.88's safety gate but shows the next narrow fix should target category-search candidate discovery for cable clips / sink strainers and a Pen Holders material-field fallback/readback path.
- Prepared `DXM Automation V1 - NEW v1.1.88` for category-family scoring instead of one-off fixes for the remaining 3 products. The change adds reusable product-family category scoring rules for desk pen holders, adhesive cable clips, kitchen sink strainers, and adhesive wall hooks. Dianxiaomi category search API candidates and edit-page category modal search-result candidates now use the same family allow/reject scoring. The cable-clip family rejects conductive glue paste / glue-paste paths such as `导电线胶膏`; the sink-strainer family rejects commercial kitchen appliance / electric appliance paths such as `商用餐厨操作类电器`; the pen-holder family keeps `Rotating Pen Organizer` under desk stationery / pen-holder logic and rejects unrelated cable/hook/kitchen/bathroom paths. Candidate diagnostics now include `familyScore`. Category, Material, freight template, Color, custom attributes, save, and publish controls are otherwise unchanged. Remaining failed products `B0CNSYPZBQ`, `B0D65JFRX4`, and `B0C9ZHWC9K` are intended as validation samples for the rule, not hard-coded exceptions.
- Prepared `DXM Automation V1 - NEW v1.1.87` for the `Rotating` title-risk false positive on `B0C9ZHWC9K`. User confirmed the product is a real rotating product and Amazon also presents rotating as a function. Root cause: the title-risk logic treated a capitalized first title token followed by product descriptor words as a suspected brand candidate; `Rotating Pen...` matched that pattern even though `Rotating` is a functional word. v1.1.87 adds `rotating` to the generic/functional leading-word allowlist so `Rotating Pen Organizer...` is not blocked as a brand/trademark/platform risk solely because `Rotating` is first. Category, Material, freight template, Color, custom attributes, save, and publish controls are unchanged.
- Resumed the authorized remaining-4 Dianxiaomi draft batch from the interruption point without recollecting, re-claiming, changing code, or upgrading version. Live page version was `DXM Automation V1 V1.1.86`. Initial state check showed all 4 target products still in the overseas-managed draft box; only `B0D65JFRX4` had a local Product Understanding / Rule report from the interrupted run, while the other 3 were still interrupted at the DeepSeek/API stage. Execution result: 1 / 4 moved to wait-to-publish. `B09PFW8WRQ` passed edit-page preflight and was saved via native `保存并移入待发布`; wait-to-publish readback shows title `Hooks for Hanging Heavy Duty 44ib(max) 10 Packs, Wall Hangers Without Nails Self`, category `衣帽挂钩(Coat Hooks)`, store `Halo Home Store`, Ships From `美国`, SKU `B09PFW8WRQ`. The remaining 3 stayed in draft and were skipped after field-level preflight failures: `B0CNSYPZBQ` category selected as `导电线胶膏(Conductive Wire Glue Pastes)` instead of adhesive cable clips, freight template/required attributes/variation/custom attributes incomplete; `B0D65JFRX4` category selected as `商用餐厨操作类电器（带电）(Commercial Kitchen Operation Appliances)` instead of sink strainer, freight template/required attributes/variation incomplete; `B0C9ZHWC9K` category and freight succeeded as `笔筒(Pen Holders)` / `111`, but Material remained unavailable/incomplete and title preflight flagged `Rotating`. One WebBridge navigation wait occurred after leaving an unsaved failed edit page and was recovered by opening the next product in a new tab; no publish or one-click-publish action was executed.
- Prepared `DXM Automation V1 - NEW v1.1.86` for the Color single-vs-multi rule correction after user review. v1.1.85 remains the validated single-sample fill + preflight + save anchor. v1.1.86 changes only Color variation selection: it now uses edit title, edit data, and Amazon public batch evidence to detect explicit single-color terms before choosing a Color option; a single visible Color option is selected directly; `MULTI/多色` is used only when the source indicates multi-color or when color evidence is unknown and MULTI is available as a conservative fallback. Category, Brand, High-concerned chemical, Origin, Material, freight template, custom attributes, save, and publish controls are unchanged.
- Live save-verified `DXM Automation V1 V1.1.85` on sample `167487782006885971` after explicit user authorization to click Save. Before saving, v1.1.85 preflight was confirmed as `pass=true` with `risks=[]`. One native Dianxiaomi `保存` button click was executed; no publish or one-click publish action was clicked. The page showed `您的产品编辑成功！` and navigated to `https://www.dianxiaomi.com/web/smtlocalProduct/offline`. The wait-to-publish list shows the saved row with title `Silicone Soap Dish with Drain Spout, Self Draining Sink Organizer Tray`, store `Halo Home Store`, Ships From `美国`, category `便携肥皂盒（非安装，非金属）(Portable Soap Dishes)`, SKU `B0BPS66NC3`, and stock `15`. Network capture did not show publish / one-click publish / release requests. This completes the single-sample edit-page fill + preflight + save validation; final publish remains prohibited unless explicitly authorized.
- Live-verified `DXM Automation V1 V1.1.85` on sample `167487782006885971`. One `应用编辑页规则` click was executed; no save, one-click publish, publish, or `save.json`/publish network request was captured. Final result: `preflight.pass=true` and `risks=[]`. Passed fields: category `便携肥皂盒（非安装，非金属）(Portable Soap Dishes)`, Brand `NONE`, High-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, Material `硅胶(Silicone)`, freight template `111`, Color `MULTI`, Ships From `美国(United States)`, and custom attributes cleared with no 70-character error. Page readback confirmed Material combobox input value `硅胶(Silicone)`. This completes the single-sample edit-page repair validation; no save or publish action was executed.
- Prepared `DXM Automation V1 - NEW v1.1.85` after v1.1.84 live validation reclassified the remaining failures as preflight/readback mismatches. v1.1.84 fixed the Material wrong-value behavior: execution result and DOM readback show `硅胶(Silicone)`, dropdown closed, and no `木头(Wood)` remains. The remaining preflight failures are caused by readback gaps: `getSelectedTextInContainer()` did not read Ant combobox/search input values, and Ships From preflight only trusted checkbox checked state even when the visible page section shows `美国(United States)`. v1.1.85 changes only these readback paths. Material selection behavior and all other edit-page business logic are unchanged.
- Live-verified `DXM Automation V1 V1.1.84` on sample `167487782006885971`. One `应用编辑页规则` click was executed; no save, one-click publish, publish, or `save.json`/publish network request was captured. Result: category `Portable Soap Dishes`, Brand `NONE`, High-concerned chemical `天然未处理(None)`, Origin `美国(Origin)(US(Origin))`, freight `111`, Color `MULTI`, and custom attributes cleared remained stable. Material no longer falls to `木头(Wood)`; execution result reports Material `ok=true`, `selectedText=硅胶(Silicone)`, `optionText=硅胶(Silicone)`, and page DOM readback shows the Material combobox search input value `硅胶(Silicone)` with the dropdown closed. Remaining issue is now classified as a preflight/readback mismatch: the final preflight checks only selected-item text and therefore still reports Material empty; Ships From has the same readback mismatch pattern, while execution and visible page readback show `美国(United States)`. Do not continue blindly changing Material dropdown selection; next fix, if required, should be narrow preflight/readback correction.
- Prepared `DXM Automation V1 - NEW v1.1.84` after live v1.1.83 validation. v1.1.83 improved Material from the previous wrong `木头(Wood)` input to `硅胶(Silicone)` and the execution result marked Material `ok=true`, but page readback still showed Material only as the Ant select search input value, not as a committed selected item; plugin preflight still reported `required attributes incomplete: material`. All previously good fields stayed stable and no save, one-click publish, publish, or `save.json`/publish request was captured. v1.1.84 changes only Material selection: it prefilters the open dropdown to exact `硅胶(Silicone)` before option lookup and keyboard confirmation, so Enter/Tab confirmation happens only after the option list is narrowed to the intended material. Category, freight, custom attributes, title, High-concerned chemical, and Origin logic are unchanged.
- Prepared `DXM Automation V1 - NEW v1.1.83` after live v1.1.82 validation. v1.1.82 kept the previous good fields stable and did not send any save, one-click publish, publish, or `save.json`/publish request, but Material still did not commit: the run found/clicked option `硅胶(Silicone)`, then the field ended as search input `木头(Wood)` with no real selected item. Classification: Material keyboard confirmation accepts the wrong highlighted/default option. v1.1.83 changes only Material selection: candidate order now prioritizes exact `硅胶(Silicone)`, and Material fixed-option selection skips Enter/Tab keyboard commit after the option click. Category, freight, custom attributes, title, High-concerned chemical, and Origin logic are unchanged.
- Prepared `DXM Automation V1 - NEW v1.1.82` after live v1.1.81 validation. v1.1.81 fixed the previous Origin blocker and selected `美国(Origin)(US(Origin))`; category `Portable Soap Dishes`, Brand `NONE`, High-concerned chemical `天然未处理(None)`, freight `111`, Color `MULTI`, and custom-attribute cleanup all stayed stable. No save, one-click publish, publish, or `save.json`/publish network request was captured. Target still did not fully pass because Material only left `Silicone Rubber` in the material search input and did not commit as a real selected item; preflight still reported `required attributes incomplete: material`. v1.1.82 changes only Material selection to use the fixed dropdown option click/lock path with keyboard confirmation. Category, freight, custom attributes, title, High-concerned chemical, and Origin logic are unchanged. Syntax check passed with bundled Node, `git diff --check` passed, and the source was copied to the clipboard for Tampermonkey overwrite.
- Prepared `DXM Automation V1 - NEW v1.1.81` after live v1.1.80 validation. v1.1.80 did not improve the Origin blocker: High-concerned chemical still selected, but Origin again stopped with `fixed option not found in active dropdown`, while a separate read-only diagnosis had already confirmed `美国(Origin)(US(Origin))` is visible when the Origin dropdown is opened manually. v1.1.81 changes only the fixed dropdown opener: instead of calling the old broad opener that can click both select root and selector, it closes previous dropdowns, clicks the current field once, confirms a visible dropdown rendered, then searches the current visible options. Syntax check passed.
- Live-verified `DXM Automation V1 V1.1.80` on sample `167487782006885971`. One `应用编辑页规则` click was executed; no save, one-click publish, publish, or `save.json`/publish network request was captured. Result: category `Portable Soap Dishes`, Brand `NONE`, High-concerned chemical `天然未处理(None)`, custom attributes cleared, freight `111`, and Color `MULTI` remained stable. Target not achieved because Origin and Material stayed unselected; pipeline stopped at Origin.
- Prepared `DXM Automation V1 - NEW v1.1.80` after live v1.1.79 validation. v1.1.79 improved the required-attribute chain: High-concerned chemical selected and locked as `天然未处理(None)`, while category `Portable Soap Dishes`, Brand `NONE`, freight `111`, custom attributes cleared, and Color `MULTI` remained good. Target still failed at Origin with `fixed option not found in active dropdown`; a read-only diagnosis confirmed the Origin dropdown contains visible `美国(Origin)(US(Origin))`. v1.1.80 changes only fixed dropdown stability: close any previous visible select dropdown before opening the next field, wait longer for current dropdown render, and retry exact visible-option lookup during the render window. Syntax check passed.
- Live-verified `DXM Automation V1 V1.1.79` on sample `167487782006885971`. One `应用编辑页规则` click was executed; no save, one-click publish, publish, or `save.json`/publish network request was captured. Result: High-concerned chemical selected `天然未处理(None)`, category remained `便携肥皂盒（非安装，非金属）(Portable Soap Dishes)`, custom attributes remained cleared, freight template remained `111`, and Brand remained `NONE`. Target not achieved because Origin and Material stayed unselected; pipeline stopped at Origin.
- Prepared `DXM Automation V1 - NEW v1.1.79` after live v1.1.78 validation. v1.1.78 achieved the confirmed soap-dish category target: `便携肥皂盒（非安装，非金属）(Portable Soap Dishes)`. It also cleared custom attributes, selected freight template `111`, selected Brand `NONE`, and kept save/publish untouched. Target still failed because High-concerned chemical, Origin, and Material stayed `请选择`; a read-only diagnosis confirmed the High-concerned chemical field container is visible and the option `天然未处理(None)` exists in the opened dropdown. v1.1.79 changes only the fixed dropdown option lookup path: if active-dropdown matching misses, it falls back to all currently visible dropdown options, then types the target value into the open dropdown search and retries. Syntax check passed.
- Live-verified `DXM Automation V1 V1.1.78` on sample `167487782006885971`. One `应用编辑页规则` click was executed; no save, one-click publish, publish, or `save.json`/publish network request was captured. Result: category selected `便携肥皂盒（非安装，非金属）(Portable Soap Dishes)`, custom attributes cleared to 0 invalid rows, freight template selected `111`, Brand selected `NONE`, Color selected `MULTI`, and the old wrong categories `Bathroom Accessories Sets` / `Soap Box` were not present. Target not achieved because required attributes remained incomplete: High-concerned chemical, Origin, and Material stayed unselected.

## 2026-06-29

- Prepared `DXM Automation V1 - NEW v1.1.78` after live v1.1.77 validation. v1.1.77 confirmed the new search-result-row click path works and allowed the pipeline to continue, but it selected `皂盒(Soap Box)` instead of the user-confirmed target `便携肥皂盒 / Portable Soap Dishes`. No save, one-click publish, publish, or `save.json`/publish network request was captured. v1.1.78 narrows only the soap-dish category plan: `Soap Box` is no longer an accepted leaf term and is added as a reject term, and search-result matching now prioritizes earlier leaf terms before shorter text. Syntax check passed.
- Live-verified `DXM Automation V1 V1.1.77` on sample `167487782006885971`. One `应用编辑页规则` click was executed; no save/publish request was captured. Result: category search-result row clicking worked, custom attributes were cleared, freight template selected `111`, Brand selected `NONE`, variation Color selected `MULTI`, and page did not show the old Bathroom Accessories Sets category. Target not achieved because category selected `皂盒(Soap Box)` instead of `便携肥皂盒 / Portable Soap Dishes`; High-concerned chemical and Origin remained unselected, and preflight failed.
- Prepared `DXM Automation V1 - NEW v1.1.77` after v1.1.76 live validation and read-only modal diagnosis. v1.1.76 confirmed the wrong generic fallback is blocked (`genericFallbackDisabled=true`) and page scroll stayed at 0, but the category still did not change to `便携肥皂盒 / Portable Soap Dishes`; the pipeline stopped at category and no save/publish request was captured. Diagnosis: after searching `浴室`, the correct `Portable Soap Dishes` appears as a full path `.search-result-item`, but the old modal item matcher rejected full-path rows with multiple `/`. v1.1.77 adds a narrow search-result-row leaf click before column/item fallback, with the same reject terms for wrong bathroom categories. Syntax check passed.
- Live-verified `DXM Automation V1 V1.1.76` on sample `167487782006885971`. One `应用编辑页规则` click was executed; no save, one-click publish, publish, or `save.json`/publish network request was captured. Result: page did not jump (`scrollY` samples stayed 0), title cleanup remained correct, and the new planned-category failure guard worked (`genericFallbackDisabled=true`). Target not achieved because the page still showed `浴室配件套装 / Bathroom Accessories Sets`, required attributes and freight were intentionally not run after category failure, and custom attributes remained invalid because the pipeline stopped at category.
- Prepared `DXM Automation V1 - NEW v1.1.76` after live v1.1.75 validation showed the reject-term filter did not complete the target: the specific soap-dish modal plan still failed, then the generic `category-button-modal` fallback selected `浴室配件套装 / Bathroom Accessories Sets`. v1.1.76 changes only this failure branch: when a specific category modal plan exists, a failed planned selection now stops and returns failure with `genericFallbackDisabled=true` instead of continuing into generic category fallback. This prevents wrong-category writes and keeps the pipeline blocked at the real category-control issue. Syntax check passed.
- Prepared `DXM Automation V1 - NEW v1.1.75` after live v1.1.74 validation showed category search now works, but the page selected `浴室配件套装 / Bathroom Accessories Sets` instead of `便携肥皂盒 / Portable Soap Dishes`. The current blocker is no longer search-submit; it is exact leaf priority inside the returned bathroom category results. v1.1.75 adds reject-term filtering to category modal item matching and passes plan-level rejects into leaf clicks, so nodes containing `Bathroom Accessories Sets / 浴室配件套装` are not eligible when the target is Portable Soap Dishes. Syntax check passed.
- Prepared `DXM Automation V1 - NEW v1.1.74` after a live diagnostic on the v1.1.73 page showed the category modal top search input held `浴室`, but the category list stayed on the original `Consumer Electronics > Accessories & Parts` tree. The remaining blocker is therefore the category modal search-submit event chain, not category understanding or leaf matching. v1.1.74 changes only `fillCategoryModalInput`: it writes the search box through the native input setter while keeping focus, dispatches `beforeinput/input/change` plus Enter key events, and triggers the Search button with React `onMouseDown` / `onClick` plus normal click. Syntax check passed.
- Prepared `DXM Automation V1 - NEW v1.1.73` after v1.1.72 live validation showed the pipeline now stopped cleanly at category with no page jumping, but the category still did not write back: the result contained the correct `Portable Soap Dishes` path while selected text remained `Accessories & Parts`. This version narrows the fix to the category modal only: category item matching now returns the deepest matching leaf node instead of a parent path container, the modal search plan tries direct leaf click immediately after the top search, and modal dismissal can use a global bottom `选择 / 确定` button if the confirm button sits outside the modal subtree. Syntax check passed.
- Prepared `DXM Automation V1 - NEW v1.1.72` after live v1.1.71 validation showed the same business target was still not achieved. v1.1.71 structure proved title brand cleanup, freight 111, and custom-attribute cleanup worked, but category selection clicked a broad modal container instead of the actual `便携肥皂盒 / Portable Soap Dishes` leaf; Brand detection then fell through to the non-required `品牌制造商` field, and ships-from was absent because the wrong category did not expose the expected variation-parameter block. v1.1.72 narrowly fixes these issues: category modal item matching now rejects dialog/search/close container text, Brand matching excludes `品牌制造商 / manufacturer / 欧盟 / 土耳其`, and the sequential pipeline stops immediately when category is not confirmed instead of continuing into category-dependent fields. Syntax check passed.
- Prepared `DXM Automation V1 - NEW v1.1.71` after live v1.1.70 single-sample validation confirmed the pipeline ran but target was not achieved. Live v1.1.70 result on sample `167487782006885971`: panel confirmed v1.1.70 and one `应用编辑页规则` click was executed; no publish or save API was captured; early scroll samples stayed at 0, but the page later moved to scrollY 2337.5; panel log showed title changed, category not selected, required attributes incomplete, custom attributes handled, marketing image not generated, ships-from not selected, freight 111 selected, preflight failed. Page readback showed category still `Consumer Electronics > Accessories & Parts`, title still contained `Micoyang`, freight template was `111`, and no `/api/smtlocalProduct/save.json` requests were captured. v1.1.71 fixes only these execution-chain issues: removes the old automatic variation fill from `loadEditJson`, expands category plans for the five validation families, adds conservative first-title-token brand cleanup, strengthens input and checkbox event chains, and exposes `window.__DXM_AUTOMATION_V1_LAST_RESULT__` for read-only verification. Syntax check passed.
- Prepared `DXM Automation V1 - NEW v1.1.70` after the user completed the edit-page workflow clarification. This version exits the v1.1.69 minimal two-field validation mode and restores a full edit-page sequential pipeline for the five-product validation goal. Manual `应用编辑页规则` now runs one ordered pass under the exclusive scroll blocker: title cleanup -> category -> required attributes -> custom attributes -> marketing image -> ships-from / variation -> freight template 111 -> text sanitization -> preflight. Soap-dish category selection now starts from `浴室` and targets `便携肥皂盒 / Portable Soap Dishes`, explicitly rejecting `Bath Baskets` and broad bathroom accessory sets. Fixed required attributes remain Brand = `NONE`, High-concerned chemical = `天然未处理(None)`, Origin = `美国(Origin)(US(Origin))`, Material from Amazon evidence such as `硅胶(Silicone)`, and unknown category-required select fields fall back to `其他(Other)`. Non-required DXM info, description rewrite, and other-info sections are not touched in this pass. Syntax check passed with bundled Node. Live Tampermonkey overwrite and single-sample save-path validation are pending.
- Prepared `DXM Automation V1 - NEW v1.1.69` after confirming the current issue is execution-right conflict, not ordinary field mapping. Scope remains single sample `167487782006885971`; batch work stays paused. This version changes the manual edit-rule action into a minimal exclusive validation mode for the authorized sample only: it temporarily blocks `scrollIntoView`, `scrollTo`, and `scrollBy` during execution, maintains field locks, and only attempts two fields in order: High-concerned chemical = `天然未处理(None)` and Origin = `美国(Origin)(US(Origin))`. It does not run title/category/material/freight/custom-attribute/variation/batch logic in this mode. Syntax check passed and the v1.1.69 source was copied to clipboard for Tampermonkey overwrite. Live field validation is pending after overwrite.
- Prepared `DXM Automation V1 - NEW v1.1.68` for the single-sample edit-page stability fix requested for sample `167487782006885971`. Batch work remains paused. The edit-page visible rules were changed from repeated scan/retry behavior to a sequential locked pipeline for required attributes: each field writes, verifies DOM/form state, locks, then moves to the next field; auto edit-rule watcher was disabled to prevent repeated field execution; edit-page click helpers no longer call `scrollIntoView`, reducing page jumping; high-concerned chemical is fixed to `天然未处理(None)` via the current active dropdown only; origin is fixed to `美国(Origin)(US(Origin))`; material is locked to silicone candidates; color variation logic now selects `MULTI` when multiple color options are present. A direct WebBridge-only apply hook was added to isolate v1.1.68 from old Tampermonkey button listeners during validation. Syntax check passed and the v1.1.68 source was copied to clipboard for Tampermonkey overwrite.
- v1.1.68 live verification is not complete. Temporary WebBridge injection was contaminated by the older installed Tampermonkey script and repeated old event listeners; one direct v1.1.68 apply attempt exceeded the WebBridge response window and was interrupted as a page-control exception. The page then read back at top scroll with category/freight/attribute sections uninitialized, so no save or publish action was executed. This is recorded as incomplete clean-page validation, not a business pass.
- Live-verified `DXM Automation V1 v1.1.55` on Dianxiaomi edit page sample `167487782006885971` using WebBridge. The page was refreshed after confirming the Chrome reload warning, the panel showed `DXM Automation V1 V1.1.55`, and `应用编辑页规则` was clicked once. No save, wait-publish move, one-click publish, publish, or `save.json op=1` action was executed. Result: category stayed correctly selected as `Portable Soap Dishes` and did not return to `Bath Baskets`; however Brand still showed `请选择`, Material still showed `请选择`, freight template still showed `--- 请选择运费模板 ---`, and custom attributes still contained one over-70-char value. Panel log reported `必填属性未完成` and `运费111未选`; no panel JS error was observed. Target not achieved.
- Prepared `DXM Automation V1 - NEW v1.1.56` as the narrow fix for the v1.1.55 failures only. Changes: select dropdown search now only uses inputs inside the currently opened dropdown instead of falling back to unrelated page search fields; legacy Ant dropdown option classes are included; select acceptance sends ArrowDown/Enter after setting the search term; attribute container scoring penalizes multi-field parent containers; custom attribute cleanup locates all custom attribute rows by `属性名/属性值` placeholders and deletes rows via the row-local `icon_close` control. Syntax check passed with bundled Node. Live Tampermonkey overwrite and target verification are pending.
- Prepared `DXM Automation V1 - NEW v1.1.52` after live v1.1.51 verification showed partial success but target not achieved. Confirmed issue: sample `167487782006885971` loaded v1.1.51 and initially had category回填, but re-running edit-page rules could overwrite the soap-dish category to `Home Office Storage`; custom attributes could still leave one value over 70 chars; freight template `111` still did not visibly select; required attributes still spent unnecessary time on brand / feature / function choices. Optimization: exact modal category plans now take precedence over the drawer/storage fallback; bathroom soap-dish titles are excluded from drawer-storage fast path; required attributes use fixed defaults where uncertainty is not useful (`Brand=None`, `Feature=Other`, `Function=Other`, and unknown required selects prefer `Other`); existing marketing images are recognized without clicking one-key generation; custom attributes are normalized before row deletion fallback; freight template selection now reports explicit `ok` status and failed selection remains a preflight blocker. Syntax check passed. Live Tampermonkey overwrite and target verification are pending.
- Prepared `DXM Automation V1 - NEW v1.1.51` after user clarified that both the top category search field and the column search fields are valid inputs. The category modal logic now first searches a broad category in the top field, then uses column search terms, then clicks a matched leaf category before confirming. Syntax check passed. Clipboard copy was attempted but blocked by the current sandbox; live Tampermonkey overwrite and target verification are pending.
- Attempted to execute the live five-sample edit-page save validation with live-confirmed `DXM Automation V1 v1.1.49`. Sample 1 (`167487782006885971`, silicone soap dish) and sample 2 (`167487782006886749`, rotating desk pen organizer) both reached the edit page, but the edit-page category modal opened and did not close/complete. Sample 1 had a category visibly written (`Washbasin Trays`) but the modal remained open; no save occurred. Network capture confirmed 0 `/api/smtlocalProduct/save.json` and 0 publish/one-click-publish requests. This was classified as a systemic edit-page category-modal control issue, not a product business failure.
- Prepared `DXM Automation V1 - NEW v1.1.50` as the single optimization for the confirmed category-modal control issue. Automatic edit-page rules now skip opening the category modal on page load; manual preflight category selection now tries confirm/close/Escape and a guarded DOM fallback only after the category is visibly written. Syntax check passed and the v1.1.50 source was copied to the clipboard for Tampermonkey overwrite. Target verification is pending after live page confirms v1.1.50.
- Verified live `DXM Automation V1 v1.1.50` after user overwrite. The target was not achieved: sample 1 still opened/stayed in the category modal, and manual `预检并保存` did not complete category selection, custom attribute cleanup, freight template 111 selection, or save. Network capture again showed 0 `/api/smtlocalProduct/save.json` and 0 publish/one-click-publish requests. Per the project three-step rule, stopped further repeated live attempts on this same issue instead of continuing to patch or click repeatedly.
- Added the project-level three-step execution rule after user correction: every work issue, project issue, and optimization item must first confirm the issue, then optimize it with minimal necessary changes, then verify whether the original work target is achieved. Repeatedly modifying the same issue without target verification is explicitly disallowed and treated as a work execution mistake.
- Prepared `DXM Amazon Crawlbox V1 v0.1.28` for the user's batch-flow optimization requirements. Controlled current-batch batch claim remains the default path when batch ASINs are known and native auto-claim is off; row-by-row claim is documented as exception fallback only. Store selection now auto-selects only when exactly one store is available; multiple stores without a configured target block as `store_uncertain`. Logo review export now classifies candidates as `allow_collect`, `needs_review`, or `skip_logo_or_brand_risk`. Run metrics export now includes target quantity, actual quantity, total time, per-item average, timeout threshold, slow-point classification, and stage duration fields; unmeasured downstream stages are explicitly marked as unquantified. Syntax check passed. No Dianxiaomi collection, claim, save, one-click publish, or publish action was executed during this source update.
- Prepared `DXM Automation V1 - NEW v1.1.49` after the user caught an execution-standard miss in the five-category validation. The previous run stopped at list/interface dry-run and did not complete the edit-page visible-field step, so the live edit page could still show unselected product category, over-length custom attributes, and unselected freight template. Fixed the source so edit-page rules are re-triggered after URL changes, custom attributes are cleared by default or normalized to <=70 characters if they remain, and category search terms cover the current low-risk samples: adhesive cable clips, adhesive wall hooks, kitchen sink strainers, and bathroom soap dishes. Syntax check passed. No Dianxiaomi save, wait-publish move, one-click publish, or publish action was executed during this source fix.
- Ran `v1.1.48` five-category single-product validation from fresh collection. Initial collection box count was all 6467 / unclaimed 0 / claimed 6467. Five first-pass candidates were collected with 4 success / 0 fail and 1 duplicate (`B088HGQSZT`) skipped; one replacement cable-clip sample (`B0CNSYPZBQ`) was collected successfully. Automatic claim stayed off; no batch claim, one-click publish, publish, or `save.json` call was executed.
- Claimed five effective samples to AliExpress local / overseas managed store `Halo Home Store`: `B0BPS66NC3`, `B0C9ZHWC9K`, `B09PFW8WRQ`, `B0D65JFRX4`, and `B0CNSYPZBQ`. One sample (`B0BCHYTNHZ`) was accidentally claimed to the product-development collection box and was excluded from downstream validation. This exposed a store-selection safety issue in the claim modal: the overseas managed group must explicitly show `Halo Home Store` / `已选(1)` before confirmation.
- Ran main-plugin dry-run / save-preflight for the five effective samples. All five were blocked before `save.json op=1`; no save-to-wait-publish was executed. Four samples were blocked by platform text sanitation issues across PC/mobile descriptions, attributes, and SKU/spec fields. `B09PFW8WRQ` was additionally blocked by unresolved category and invalid category id `5050263-`. No final publish was executed. Report: `runs/v1148-five-category-single-product-preflight-report.md`.
- Ran a read-only/dry-run validation for live `DXM Automation V1 - NEW v1.1.48` on the Dianxiaomi draft page. Page-level version was confirmed as `data-dxm-automation-version=1.1.48`; `save.json op=1`, real submit, and publish test buttons stayed disabled. Only `读取当前第1条`, `构造 dry-run`, and local report export were triggered. No `/api/smtlocalProduct/save.json` request was observed.
- The dry-run correctly failed PC description preflight for the current fallback sample: `PC端描述缺少当前商品图片：当前 0/2` and `PC端描述排版错误：必须先图片后描述，开头图片 0/2`. This confirms the v1.1.48 guard blocks missing-image / non-image-first PC details before any save. Evidence was archived at `runs/v1148-pc-detail-dry-run-20260629/single-submit-report-current-draft.json`.
- Prepared `DXM Automation V1 - NEW v1.1.48` for batch listing quality. Interface/op=1 save now generates PC `detailWeb` as image-first product detail instead of plain text only.
- Added PC description preflight: dry-run/save is blocked when `detailWeb` has fewer than 2 current-product images or when product images are not the first content before description text.
- Updated project memory: PC description rules now require "images first, then description" for both UI edit flow and interface save flow. No Dianxiaomi business page action was executed in this source update.

## 2026-06-28

- Prepared `DXM Automation V1 - NEW v1.1.45` after user review of the Drawer Organizers edit page. The edit-page filler now treats several required fields as fixed project rules instead of Product Understanding guesses: Brand = `None` / no brand, High-concerned chemical = `天然未处理(None)`, Origin = United States, Function / Use prefer `Other` or reusable-style safe values, variation Ships From = United States, and freight template must be truly selected as `111`. Frame Material and Material are now handled as separate required fields using the same Amazon material inference. Visible preflight now rejects nonblank but wrong selections such as Origin = Mainland China or Use = Food. Syntax check passed with the bundled Node runtime. No Dianxiaomi business page action was executed; no collection, claim, edit, save, or publish action was performed in this source-prep step.
- Prepared source fixes for the 10-item same-category rerun after user feedback. `DXM Amazon Crawlbox V1` advanced to v0.1.27 source: collection input detection now prioritizes the fixed URL/link textarea, link-count guard blocks partial collection before clicking `开始采集`, collection stale timeout is 30 seconds, dangerous visible controls are warnings only, and controlled current-batch batch claim uses a separate safe action while naked `批量认领` remains blocked. `DXM Automation V1 - NEW` advanced to v1.1.44 source: category scoring now penalizes clothing/underwear full paths unless product evidence supports clothing/underwear use, broad drawer organizer search terms were added, material inference now uses Amazon title/detail evidence, visible required attributes include Material, and visible variation filling supports Ships From = United States where the page control is fillable. Updated Product Understanding rules and execution docs. No Dianxiaomi business page action was executed; no collection, claim, edit, save, or publish action was performed in this source-prep step.
- Corrected the validation judgment: `B0BY2M154R` is only a chain sample and cannot prove Dianxiaomi category/attribute correctness. Started the authorized 10-item same-category Drawer Organizers validation. Selected 10 new drawer organizer tray ASINs, excluding prior samples `B00AN8CTX0`, `B0BY2M154R`, and `B0B1M6ML2J`. Dianxiaomi WebBridge safety preflight allowed target `开始采集`; native auto-claim was off and dangerous controls were warnings only. Collection completed success 10 / fail 0; counts changed from total 6454 / unclaimed 0 / claimed 6454 to total 6464 / unclaimed 10 / claimed 6454. Claimed all 10 products one by one to `Halo Home Store`; no batch claim was used. Claim result success 10 / fail 0 / skipped duplicate 0; counts ended total 6464 / unclaimed 0 / claimed 6464.
- Ran DeepSeek Product Understanding for all 10 Drawer Organizers samples. All 10 adapted to `product-understanding-v1`, all passed the local Rule Engine, and all recommended `Drawer Organizers`. Reports are under `runs/drawer-10-product-understanding/`.
- Opened the first claimed product edit page directly by product ID `167487782006844693` after the list `编辑` button did not respond to normal DOM/CDP clicks. Confirmed the Dianxiaomi category search can find and visibly select `家居用品(Home & Garden) > 家用储存收藏用具(Home Storage & Organization) > 衣物收纳(Clothing & Wardrobe Storage) > 内衣收纳盒(Drawer Organizers)`. Stopped before save because edit preflight still failed: Brand showed `请选择`, Material showed `请选择`, page showed `请选择必选变种属性`, and freight template still showed `--- 请选择运费模板 ---` after the main edit-rule automation pass. No save, no move-to-wait-publish, and no publish action was executed. Report: `runs/drawer-organizers-10-category-attribute-validation-report.md`.
- Continued the authorized `B0BY2M154R` small-sample edit-save validation without collecting any new product. Computer Use successfully opened the current edit page `smtlocalProduct/edit?id=167487782006437721`; source URL guard confirmed `B0BY2M154R`, and the stale `B00AN8CTX0` edit tab was not operated. Applied safe edit fields: brand-cleaned title/description, category `内衣收纳盒(Drawer Organizers)`, Brand `NONE`, high-concerned chemical `None`, Origin `Mainland China`, Material `Plastic`, Ships From `United States`, Color `Clear`, Number of Cells `6-10`, price `122.08`, stock `15`, SKU `B0BY2M154R`, dimensions `22.86 x 15.24 x 5.08 cm`, and cleared imported custom attributes. Stopped before saving because the visible freight-template control still showed `--- 请选择运费模板 ---` and could not truly select `111`; no save, no move-to-wait-publish, and no publish action was executed.
- Confirmed live `DXM Amazon Crawlbox V1 v0.1.26` on Dianxiaomi collection page. WebBridge preflight allowed target `开始采集`; visible dangerous controls were warnings only; auto-claim was closed and counts were readable.
- Executed one authorized small-sample WebBridge validation with ASIN `B0BY2M154R` for `Halo Home Store`. Product precheck passed with no obvious main-image logo/brand packaging; Amazon brand text `Ravinte` was treated as a title/description cleanup risk. Collection via explicit `开始采集` succeeded 1 / failed 0. Row-level single claim to `Halo Home Store` succeeded 1 / failed 0 / duplicate 0. No batch claim and no publish action were executed.
- Ran DeepSeek Product Understanding for `B0BY2M154R` using `deepseek-v4-flash`; output was adapted to `product-understanding-v1` and passed the local Rule Engine. Result: Drawer Organizer, recommended category `Drawer Organizers`, material `Acrylic, Plastic`, color `Clear`, quantity `6`, size `9 x 6 x 2 inches`, with medium brand-title cleanup risk.
- Stopped the small-sample edit-save validation before editing/saving. The claimed product appeared in `https://www.dianxiaomi.com/web/smtlocalProduct/draft` with row actions `移入待发布`, `编辑`, `发布`, `更多`, but the safe row `编辑` wizard did not proceed into a stable editable page. Both `编辑分类` and `跳过，去编辑产品` closed the wizard and returned to the draft list. No field edits, no save, no move-to-wait-publish, and no publish were executed. Report: `runs/webbridge-v0126-small-sample-edit-save-report.md`.
- Prepared `DXM Amazon Crawlbox V1 v0.1.26` source to refine the WebBridge safety layer. Visible dangerous buttons now produce `warnings` only; a dangerous target action is still blocked; target button `开始采集` is allowed when other blocking rules pass. Preflight now exposes `warnings`, `blocked`, `allowed`, `targetButtonText`, and `blockReason`. Syntax check passed and the v0.1.26 source was copied to clipboard for Tampermonkey overwrite. No Dianxiaomi business action was executed after this source update because the browser page still needs the v0.1.26 Tampermonkey overwrite before re-validation.
- Attempted to enter the authorized 1-3 small-sample edit-save validation, but stopped before product selection or collection because `DXM Amazon Crawlbox V1 v0.1.25` WebBridge preflight returned `blocked`. Confirmed page-level active version `v0.1.25`, native Dianxiaomi `自动认领` closed, collection input present, collection-box counts readable at total 6453 / unclaimed 0 / claimed 6453. Blocking reason: dangerous Dianxiaomi controls visible (`采集并一键发布`, `批量认领`, `一键发布`). Per safety rule, no collection, claim, edit, save, or publish action was executed.
- Added DeepSeek Product Understanding adapter `tools/deepseek-product-understanding-adapter.py`. The adapter calls `deepseek-v4-flash` or consumes saved raw output, extracts JSON, converts DeepSeek output into `product-understanding-v1`, sanitizes publish/review-and-publish/发布 terms before final validation, and then invokes the local Rule Engine gate before allowing category/attribute/title/description suggestions to be used.
- Verified the adapter offline and with a live DeepSeek API call for ASIN `B00AN8CTX0`. DeepSeek identified the product as a drawer organizer / makeup and office supply drawer storage item, recommended `Drawer Organizers`, extracted material/color/section/brand attributes, and passed the local Rule Engine after adaptation. No Dianxiaomi page was opened or operated; no collection, claim, edit, save, or publish action was executed.
- Added Product Understanding output constraints for DeepSeek integration. New files: `skills/product-understanding/schema.json`, `skills/product-understanding/rules.json`, `tools/validate-product-understanding.py`, and `docs/product-understanding-output-rules.md`. The schema fixes required JSON fields and forbids publish/review-and-publish/发布 suggestions. Local Rule Engine validation must pass before Product Understanding output can be used for edit-page category search or attribute suggestions.
- Verified Product Understanding Rule Engine offline with two fixtures: the old DeepSeek-style output containing `Review and publish` was blocked, while a `product-understanding-v1` compliant `B00AN8CTX0` fixture passed. No Dianxiaomi page was opened or operated; no collection, claim, edit, save, or publish action was executed.
- Prepared `DXM Amazon Crawlbox V1 v0.1.25` source with a narrow WebBridge preflight export fix. The same preflight result is now exposed through the userscript window, `unsafeWindow`, page-context `window.__DXM_WEBBRIDGE_PREFLIGHT__`, a DOM JSON node `#dxm-webbridge-preflight-json`, and page `data-dxm-webbridge-*` attributes. This only changes read-only state exposure; no collection, claim, edit, save, or publish action was added or executed.
- Prepared `DXM Amazon Crawlbox V1 v0.1.24` source with a WebBridge-compatible read-only Dianxiaomi preflight layer. The preflight reads the current URL, page title, native auto-claim checkbox state, link collection input presence, dangerous Dianxiaomi action controls, and collection-box counts. It exposes the result as `window.__DXM_WEBBRIDGE_PREFLIGHT__` and `data-dxm-webbridge-preflight`.
- Added safety interception before collection and claim entry points. Collection is blocked if native `自动认领` is enabled, the collection input is missing, the page is not the Dianxiaomi data acquisition page, or dangerous controls are visible. Attempting to trigger `采集并一键发布`, `采集并自动认领`, `批量认领`, `一键发布`, or `发布` is blocked by rule.
- Added a panel action `WebBridge只读安全检查` for manual read-only preflight. No Dianxiaomi business action was executed during this source update: no collection, no claim, no edit, no save, and no publish.

## 2026-06-27

- Second controlled 1-link Smoke Test completed after explicit page refresh. Dianxiaomi and Amazon page-level panels both confirmed `DXM Amazon Crawlbox V1 v0.1.23`; Amazon panel confirmed `Logo确认=1`. Candidate `https://www.amazon.com/dp/B0B1M6ML2J` was selected after light visual review with no obvious main-image logo, brand watermark, or brand packaging observed. Only the explicit Dianxiaomi `开始采集` button was clicked. Dianxiaomi reported collected 1 / failed 0; collection box changed from total 6453 / unclaimed 1 / claimed 6452 to total 6454 / unclaimed 2 / claimed 6452. No claim, edit, save, publish, `采集并一键发布`, 3×10, or production action was executed.
- Retried second 1-link Smoke Test precheck after user confirmed Crawlbox update. Tampermonkey dashboard showed `DXM Amazon Crawlbox V1` installed as `0.1.23`, but the live Dianxiaomi page panel still showed `DXM Amazon Crawlbox V1 v0.1.22`. Per execution rule, stopped immediately before candidate selection or collection. No new collection, claim, edit, save, publish, or 3x10 action was executed.
- Second 1-link Smoke Test blocked before execution. Live Dianxiaomi page still shows `DXM Amazon Crawlbox V1 v0.1.22`, so source-prepared v0.1.23 and default `requireLogoApproval=1` are not yet confirmed active in Tampermonkey. No new collection, claim, edit, save, publish, or 3x10 action was executed.
- ASIN `B0F5WNXRY9` disposition recorded: collected successfully, but product-selection filtering missed a logo risk because the main image / packaging image contains visible brand/logo text. Downstream claim, edit, save, and publish are stopped for this product. Crawlbox source prepared as v0.1.23 with default `requireLogoApproval` changed to `1`.
- Post Smoke Test review recorded three pre-3x10 issues from user feedback: controlled collection speed is still slow; repeated Computer Use calls during collection are inefficient; and the selected candidate image/package contained visible brand/logo text, which should have been skipped or marked for review under product-selection quality rules. 3x10 should not start until these are resolved or explicitly waived.
- Controlled 1-link Smoke Test completed after user authorization. Dianxiaomi native `自动认领` was unchecked and verified before collection. One Amazon candidate (`https://www.amazon.com/dp/B0F5WNXRY9`) passed light pre-check and was collected by clicking only the explicit `开始采集` button. Dianxiaomi reported successfully collected 1, failed 0; collection box count changed from total 6452 / unclaimed 0 / claimed 6452 to total 6453 / unclaimed 1 / claimed 6452. No claim, edit, save, publish, `采集并一键发布`, 3×10, or production action was executed.
- User confirmed read-only smoke testing as passed. Controlled 1-link Smoke Test pre-check started but stopped before collection because the Dianxiaomi native `自动认领` checkbox was observed checked on the collection page. No collection, claim, edit, save, or publish action was executed.
- Amazon collection entry read-only smoke test executed on `https://www.dianxiaomi.com/web/productCrawl/dataAcquisition`. Crawlbox stayed idle with no batch, imported 0, claimed 0. save Payload V3 record count changed from 0 to 3 on page entry; latest visible record had empty op/id and no save/publish payload was observed. Marked as executed/pending decision rather than passed.
- User confirmed the read-only Dianxiaomi home smoke test report. Project status updated: Dianxiaomi home read-only smoke test passed; Amazon collection entry read-only smoke test pending; controlled real smoke test pending confirmation; 3×10 Validation and Production not started.
- User confirmed the Functional Validation report. Project status updated to: New script active review complete, Functional Validation passed, Smoke Test pending, 3×10 Validation not started, Production not started. Next step is read-only Smoke Test only; business actions remain prohibited.
- User confirmed the migration completion report. Project status updated to: `海拓电商上架系统新 Mac 环境迁移：已完成`; Environment Recovery, Panel Validation, and Environment Ready are complete; Functional Validation is pending; Smoke Test, 3×10 Validation, and Production are not started.
- Source UI cleanup prepared: removed the two visual boot/recovery badges that showed `save Payload V3 面板已挂载` and `DXM Automation V1 主面板已挂载`, because they blocked normal Dianxiaomi page operation. Main source advanced to `DXM Automation V1 - NEW v1.1.43`; save Payload source advanced to `v0.6.3`. Browser Tampermonkey scripts still require overwrite/reinstall before this takes effect in-page.
- Tampermonkey dashboard automation attempt stopped by Chrome extension-page browser safety policy. This is an environment control limitation, not a business/plugin failure. Manual clipboard overwrite is the approved recovery path for `chrome-extension://` Tampermonkey script updates.
- User manually saved Tampermonkey overwrites for `DXM Automation V1 - NEW v1.1.43` and `save Payload V3 v0.6.3`. Full Chrome quit/reopen is required before page-level confirmation, consistent with the migration recovery rule.
- Mac migration recovery check: Tampermonkey shows scripts matched/enabled, but no visual panels render on Dianxiaomi/Amazon pages.
- Confirmed current source files still contain panel creation and initialization entries for `DXM Automation V1 - NEW v1.1.41`, `DXM Amazon Crawlbox V1 v0.1.21`, `save Payload V3 v0.6.1`, and `Interface Detector V2 v0.3.0`.
- Confirmed `node --check` passes for active userscripts; no missing panel module, missing build merge, or deleted UI entry was found in source comparison with preserved run backup.
- Current leading recovery hypothesis: Chrome/Tampermonkey is matching scripts but not actually injecting/executing userscript code, likely due to Tampermonkey v5 / Chrome user-scripts / site access permission configuration rather than script conflict or source-code loss.
- Plugin Recovery Mode follow-up: Chrome profile has Tampermonkey `5.5.0` installed with `<all_urls>`, `userScripts`, `scripting`, and `user_scripts_enabled=true`; `withholding_permissions=false`. Tampermonkey popup on Dianxiaomi shows the currently retained project scripts `DXM Amazon Crawlbox V1` and `save.json Payload V3` as enabled, but the page still has no visible project panel. Automated control of the Tampermonkey `chrome-extension://` dashboard is blocked by the Chrome automation safety policy, and direct LevelDB writes are intentionally avoided to prevent corrupting Tampermonkey storage. Next recovery step requires manual Tampermonkey script overwrite/reinstall or a user-approved Chrome/Tampermonkey UI handoff.
- Recovery completed after a full Chrome quit/reopen. Root cause was Chrome/Tampermonkey userscript execution state not activating after permission/script changes until Chrome fully restarted; normal page refresh was insufficient.
- Added visible boot/recovery markers to `DXM Automation V1 - NEW v1.1.42` and `save Payload V3 v0.6.2` so future migrations can distinguish "script matched" from "script executed".
- Restored the three expected business panels on Dianxiaomi: `DXM Automation V1`, `DXM Amazon Crawlbox V1`, and `save Payload V3`.
- Updated `DXM Amazon Crawlbox V1 v0.1.22` to include root `dianxiaomi.com` match rules and move the default Dianxiaomi panel position away from the lower-right save Payload panel, preventing the Crawlbox panel from appearing missing on fresh Mac profiles with no saved panel position.
- Temporary `DXM Tampermonkey Execution Diagnostic v0.0.1` was used only to confirm execution and should remain disabled after recovery.
- Environment Ready completion: browser-installed main plugin confirmed as `DXM Automation V1 V1.1.42`; Crawlbox confirmed as `v0.1.22`; save Payload confirmed as `0.6.2`.
- Installed macOS Command Line Tools for Xcode `26.6`; `git version 2.50.1 (Apple Git-155)` is available. The project directory is not currently initialized as a Git repository.
- Read-only Console check completed. Observed two `qiyukf.com` customer-service `proxy.contentWindow.postMessage---error` entries; no project userscript Console errors were observed.

## 2026-06-25

- Main plugin advanced to v1.1.41.
- Category mapping refactored to `CATEGORY_MAPPING_RULES`.
- Pen Holders limited to desk stationery / pen holder / pencil cup product family.
- Environment Control Exception recorded for Browser / Computer Use failures.
- V1 Freeze and Mac migration documentation created.

## 2026-06-24

- 3 x 10 stability run attempted.
- Collection/claim reached 28 products.
- Edit-page save failed on first item due to missing category and custom attribute validation.
- Main plugin patched starting v1.1.37 for edit-page preflight and save correction.

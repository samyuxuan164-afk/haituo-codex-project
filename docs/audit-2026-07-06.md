# Audit Report - 2026-07-06

## Scope

This audit covers documentation, version drift, encoding indicators, safe local test coverage, the first source-level pure-module split for `haituo-codex-project`, and the follow-up correction that keeps price formulas task-scoped while defaulting Amazon displayed-price candidate selection to the highest valid displayed value.

No browser business action was performed. No Dianxiaomi collection, claim, edit, save, publish, or one-click publish action was performed.

## Repository State

```text
Company upstream: https://github.com/samyuxuan164-afk/haituo-codex-project.git
Collaborator fork: https://github.com/ALdaisuki/haituo-codex-project.git
Base branch: main
Work branch: codex/userscript-pure-modules
Tracked file count observed: 408
Markdown/high-level docs count observed: 84
JavaScript/MJS count observed: 39
Python count observed: 11
JSON count observed: 117
```

Contribution route:

```text
local worktree
-> collaborator fork branch
-> fork-side diff/privacy audit
-> PR to company upstream
```

## Version Drift Findings

| Area | Finding | Risk |
|---|---|---|
| README | Previously described V1 freeze-era active versions while source headers show newer scripts | Operators may install or validate the wrong script |
| `docs/install.md` | Previously listed `DXM Automation V1 - Merged v1.1.14` and Crawlbox `0.1.15` | Direct install instructions were stale |
| `docs/current-status.md` | Latest top entry referenced `v2.1.44`, while source header shows `v2.1.75` | Long status log is not safe as the only version source |
| Source tree | Both current and historical userscript variants coexist | Browser can accidentally enable duplicate or stale panels |

Source-visible versions extracted from `src/*.user.js`:

```text
DXM Automation V1 - NEW v2.1.75
DXM Amazon Crawlbox V1 v0.1.50
save.json Payload Capture V3 v0.6.3
Interface Detector V2 v0.3.0
Single Submit Tester v0.2.5
```

Decision: README and install docs now use source headers as the source-visible version matrix and explicitly warn that browser-installed versions must be verified separately.

## Encoding Findings

Scan command:

```powershell
rg -n "�|Ã|â|鈥|鉁|馃|锔|涓|绗|乱码|mojibake" README.md AGENT.md AGENTS.md TASK.md TODO.md DELIVERABLE.md CHANGELOG.md VERSION_HISTORY.md docs skills -g "*.md" -g "*.json"
```

Observed hits were documentation about historical Windows terminal mojibake, including:

```text
docs/freeze-v1-20260625/FREEZE_REPORT.md
docs/freeze-v1-20260625/MAC_ENVIRONMENT_CHECKLIST.md
docs/freeze-v1-20260625/MAC_MIGRATION_GUIDE.md
docs/freeze-v1-20260625/WINDOWS_COMPATIBILITY_AUDIT.md
```

Interpretation:

- The high-priority scan did not show direct replacement-character corruption in the rewritten core docs.
- Historical docs already warn that some Windows terminal output may display mojibake.
- Future encoding fixes should target specific corrupted files with UTF-8 verification, not bulk rewrite all Chinese docs.

## Test Coverage Findings

Current executable local baseline:

```powershell
node tools\aliexpress-evidence-policy.test.js
node tools\dxm-automation-core.test.js
node --check src\dianxiaomi-automation-v1-merged-new.user.js
node --check src\dianxiaomi-amazon-crawlbox-v1.user.js
node --check <all tracked JS/MJS files>
```

Results:

| Check | Result |
|---|---|
| AliExpress evidence policy test | PASS |
| Extracted userscript core module test | PASS, includes text sanitization, title cleanup, PC detail image-first analysis, dimensions, weight, task-parameterized price calculation, default highest displayed-price candidate handling, List Price / strike-price coverage, explicit invalid-policy validation, tiered multiplier coverage, collection-box contamination diagnostics, and readonly edit preflight root-cause normalization |
| Main DXM automation syntax | PASS |
| Amazon crawlbox syntax | PASS |
| All tracked JavaScript/MJS syntax | PASS, 39 files |
| All tracked Python AST parse | PASS, 11 files |
| All tracked JSON parse | PASS, 117 files |
| Markdown whitespace check via `git diff --check` | PASS, CRLF warnings only |
| Active-doc stale version scan | PASS |
| Bilingual PNG asset check | PASS, 2 files |
| Bilingual Mermaid source check | PASS, 2 files |
| Bilingual ASCII architecture check | PASS, width <= 100 and no tabs |
| Bilingual README/architecture Markdown fence check | PASS |
| Bilingual README/architecture local link check | PASS |
| UTF-8 / mojibake guard for bilingual docs and diagrams | PASS, no replacement-character or known mojibake markers |
| Changed-file privacy scan | PASS, no local private path, Codex runtime path, token, key, or temp-path patterns |

Gaps:

1. No package manifest or unified test command exists.
2. Two explicit `.test.js` files now exist, but coverage is still limited to selected pure rules.
3. Static syntax/parse checks are broad but shallow; they do not prove behavior.
4. The largest script is too large for confident manual review without extraction or targeted tests.
5. Browser validation is procedure-driven and must remain gated.
6. Run evidence in `runs/` is valuable but noisy for code/test discovery.

## Documentation Changes Made

| File | Change |
|---|---|
| `README.md` | Rebuilt as source-of-truth entrypoint with safety boundary, active source matrix, project map, architecture pointers, and local verification |
| `docs/architecture.md` | Rebuilt with context/container/component Mermaid diagrams, data flow, safety gates, test architecture, and known risks |
| `docs/architecture-ascii.md` | Added terminal/PR-friendly C4-style ASCII architecture map |
| `README.zh-CN.md` | Added Chinese project entrypoint with Chinese SVG and complete Chinese Mermaid architecture |
| `docs/architecture.zh-CN.md` | Added Chinese 13-section architecture document |
| `docs/architecture-ascii.zh-CN.md` | Added Chinese C4-style ASCII architecture map |
| `docs/assets/architecture-overview-en.png` | Replaced English overview with the approved proven execution flow |
| `docs/assets/architecture-overview-zh.png` | Replaced Chinese overview with the approved proven execution flow |
| `src/dxm-automation-core/*` | Added first browser-effect-free CommonJS modules for text, pricing/dimensions, PC detail rules, and workflow root-cause diagnostics |
| `tools/dxm-automation-core.test.js` | Added deterministic assertions for the extracted pure modules, default highest displayed-price candidate selection, List Price / strike-price handling, invalid override policy handling, tiered multiplier behavior, the known 10-target / 16-row collection-box contamination case, and readonly edit preflight blocker normalization |
| `tools/amazon-displayed-price-capture.js` | Added explicit `--range-policy` / `TASK_PRICE_RANGE_POLICY` handling so displayed price ranges do not silently default to the highest value |
| `tools/amazon-displayed-price-batch.js` | Added range-policy forwarding for batch Amazon displayed-price capture |
| `AGENT.md`, `TASK.md`, `docs/exception-rules.md`, `docs/project-execution-rules.md`, `skills/price-processing/SKILL.md` | Clarified that price calculation is current-task configuration, not a global formula |
| `docs/diagrams/workflow-en.mmd` | Added English Mermaid source |
| `docs/diagrams/workflow-zh.mmd` | Added Chinese Mermaid source |
| `docs/install.md` | Rebuilt current source-visible install and enablement guide |
| `docs/test-plan.md` | Rebuilt layered test strategy and exact local commands |
| `docs/test-results.md` | Recorded latest local baseline and coverage gaps |
| `docs/superpowers/*` | Added research and execution plan artifacts |

## SuperPowers 10-Item Audit Checklist

| # | Check | Result | Evidence |
|---:|---|---|---|
| 1 | Design principles respected | PASS | The runtime userscript artifact remains unchanged; deterministic rules are extracted into small pure modules instead of introducing a bundler or browser-side dependency. |
| 2 | Traceability | PASS | Work is isolated on `codex/userscript-pure-modules`; design, research, plan, test results, and audit notes are under `docs/`. |
| 3 | Test coverage for changed surface | PASS | `tools/dxm-automation-core.test.js` covers text rules, pricing/dimensions, PC detail image-first analysis, task-supplied price parameters, default highest Amazon displayed-price candidate selection, List Price / strike-price handling, explicit override policy validation, tiered multiplier behavior, collection-box duplicate/zero-price diagnosis, and edit preflight root-cause normalization. |
| 4 | Breaking change documented | PASS | Installed userscript runtime behavior, config schema, live execution behavior, and browser adapter contract remain unchanged. Local Amazon displayed-price capture tools now default to highest valid Amazon displayed-price candidate, including ranges, variants, and List Price / strike prices, while keeping task-scoped goods-value formulas documented in `docs/exception-rules.md`, `docs/project-execution-rules.md`, and tool usage output. |
| 5 | Dependency change | PASS | No new runtime, package, lockfile, or external dependency was added. |
| 6 | Architecture impact | PASS | New module boundary is additive and browser-effect-free; DOM automation, WebBridge, Ant dropdown handling, native save, and publishing remain in the userscript adapter. |
| 7 | Security and privacy | PASS | Changed-file privacy scan found no local private paths, credentials, session material, browser profiles, tokens, key material, payload dumps, or Codex runtime paths. |
| 8 | Cross-module impact | PASS | Main userscript artifacts are untouched; pricing pure module and Amazon displayed-price capture tools now share the same default highest displayed-price candidate policy, while formulas remain task-scoped; docs and skills were updated accordingly. |
| 9 | API compatibility | PASS | No persisted data format or Tampermonkey entrypoint changed. The local capture CLI still accepts `--range-policy` for explicit overrides and returns `price_range_policy_invalid` for unsupported policies; absence of an override uses the default `highest_displayed_value` candidate policy. |
| 10 | Rollback and docs | PASS | Rollback is a branch revert of pure modules/docs/assets; README, architecture docs, current status, test results, and development log were updated to reduce drift. |

Audit decision for this userscript pure-module PR: PASS, with no blocking issues after clarifying that goods-value formulas are task configuration, while Amazon displayed-price candidate selection defaults to the highest valid displayed value unless the current task explicitly overrides it.

## Pass / Block Decision

Documentation pass: PASS.

Pure-module local audit: PASS.

Test coverage maturity: BLOCK for any claim of comprehensive automated coverage.

Live execution readiness: BLOCK until `TASK.md` permits the exact phase and the user explicitly starts live execution.

## Next Actions

1. Add a minimal package manifest or script runner for safe local checks.
2. Continue extracting pure logic from the main userscript into testable modules, starting with category evidence planning, crawlbox current-batch row selection, and edit-page field/preflight classification.
3. Add tests for candidate routing, product risk filtering, exception queue behavior, and category evidence classification.
4. Add a version-check tool that compares README/install docs against `src/*.user.js` headers.
5. Run screenshot cleanup planning after any live or browser-evidence task.
